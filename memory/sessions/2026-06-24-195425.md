# Session (2026-06-24T19:54:25.000Z)

**Agent:** Claude Code

## What happened

**Shipped v4.20.1 (PATCH)** — folded the first-run self-init into `copilot-instructions.md` so GitHub
Copilot CLI self-inits on a fresh clone like Claude Code does.

**Trigger (fresh-clone re-test by the maintainer).** Re-cloned **both** repos and ran `start`:
- **agent-memory (Claude Code): self-init WORKED** — Claude read the `AGENTS.md` self-init note, checked
  `core.hooksPath` (unset), and ran `bash .githooks/init.sh` proactively → hook active + 25 adapters
  synced. Verified on disk: `core.hooksPath=.githooks`, post-commit executable+active, clean tree.
- **simple-proxy (Copilot / Gemini 3.1 Pro): self-init did NOT trigger** — Copilot's `start` read the
  memory files (via `copilot-instructions.md`'s front-loaded list) and **summarized**, but never ran
  `init.sh` or activated the hook. Verified on disk: `core.hooksPath` UNSET, adapters absent
  (`.github/skills/greeting` missing). The v4.20.0 self-init (in `AGENTS.md`) didn't reach Copilot's
  behavior. (Version was correctly 4.20.0 — Copilot's "v4.19.0" summary was a misread of a session log.)

**Root cause.** Copilot's bootstrap is driven by `copilot-instructions.md` (load memory → summarize); it
doesn't act on the AGENTS.md self-init step. Claude reads + *acts on* `AGENTS.md` deeply, so it self-inits.

**Fix.** Put the first-run init at the **top of `copilot-instructions.md`** as the first action (*if
`core.hooksPath` unset / adapters absent, run `bash .githooks/init.sh` before summarizing*) — the file
Copilot reliably front-loads. Honest: still prompt-adherence (non-deterministic) — improves the odds;
`bash .githooks/init.sh` is the one-command fallback and CI is the zero-config floor regardless.

**Lockstep (PATCH).** `templates/.github/copilot-instructions.md` (first-run-init block at top),
`VERSION`→4.20.1, `UPGRADE.md` (4.20.0→4.20.1 rung + table), `CHANGELOG`, `README`,
`docs/DESIGN-ritual-triggers.md` (status note). Re-synced to simple-proxy. `AGENTS.md` unchanged (its
note already works for Claude); `DECAY`/`REVIEW`/`SKILLS` unchanged.

**Note.** Both repos were re-cloned by the maintainer; a separate Claude Code session had already
self-inited the agent-memory clone (hook active). simple-proxy's clone was left **un-initialized** on
purpose so the maintainer can re-test whether Copilot now self-inits from the updated steering.

## Memory References

- Referenced (updated): `ritual-init-v4200` — added the v4.20.1 follow-up (Copilot didn't self-init from
  `AGENTS.md`; fixed by folding the init into `copilot-instructions.md`)
- Referenced: `vision-agent-memory`, `no-build-step-agent-run`, `backlog-real-work-dogfood` (the
  fresh-clone re-test that surfaced the gap)
