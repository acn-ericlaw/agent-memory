# Session (2026-06-15T23:15:02.000Z)

**Agent:** Claude Code

## Summary

A real-work finding from enabling a larger client repo: it carried user-defined **Claude
skills** under `.claude/skills/`. The tool shares memory + steering across vendors but has
no story for **skills**, and `.claude/` is gitignored so committed project skills don't even
travel. Captured this as a new Blueprint gap and drafted its design — **did not build it**
(human-gated forward work; maintainer approved capturing the gap + design only).

## What happened

- **Diagnosed the gap.** Repo-wide grep confirmed "skill" appears nowhere in the protocol —
  genuinely unhandled. The shared layer has memory (`memory/`) and steering
  (`instructions.md` via the `AGENTS.md` hub) but no third leg for *capabilities*.
- **Settled the approach with the maintainer.** Recommended **Option A**: a neutral,
  committed `skills/<name>/SKILL.md` source of truth + an `AGENTS.md` "Skills" baseline
  (agent-as-runtime — works for any vendor, no per-vendor code) + optional thin per-vendor
  adapters that are **regenerated locally** (adapter dirs `.claude/`/`.gemini/`/`.cursor/`
  stay gitignored). Rejected Option B (committed `!.claude/skills/` exception) as default —
  re-couples the shared layer to one vendor. Maintainer agreed with A.
- **Drafted the design:** `docs/DESIGN-skills-layer.md` (sibling to the evolving-memory and
  VBDI design docs) — why, principles, the neutral-source/baseline/adapters design, the
  committed-vs-personal decision, migration (promote into `skills/`, don't flatten;
  preserve under `legacy/`), invariant alignment, scope/non-goals, integration points, open
  questions. No implementation yet (no ENABLE/MIGRATE/template/gitignore changes).
- **Opened the Blueprint gap** `bp-skills-layer` (serves `vision-agent-memory`) in
  `continuity.md` — the third leg of the shared layer (memory + steering + **skills**).
- Ran the write-time contradiction check (DECAY §10): the skills layer is additive and
  contradicts no existing fact/invariant. None raised.

## Notes / open items

- **Review is OVERDUE** (flagged, not run): `sessions_since_last_review` ≫ `review_every`
  (10) — `last_review` is 2026-06-13 through `2026-06-13-223743` with 24 session files now —
  and `continuity.md` is ~430 lines, over the `continuity_max_lines: 300` cap. Both triggers
  are met. A `REVIEW.md` run is due on this tool's own dogfooded memory.
- `bp-skills-layer` is a forward gap awaiting the next human gate (approve building it →
  an additive MINOR rung, e.g. 4.x).
- Reminder: commit `memory/` + `docs/DESIGN-skills-layer.md`.

## Memory References

- **Created:** `bp-skills-layer` (Blueprint gap; tier working; serves vision-agent-memory)
- **Referenced:** `vision-agent-memory`, `no-code-markdown-only`, `never-pick-a-winner`,
  `target-repo-scope-only`, `never-delete-vendor-files`, `upgrades-additive`,
  `gitignore-propagation-v310`

## Memory Review (2026-06-15, through 2026-06-15-231502)
- Reactivated:   0
- Superseded:    0  (no fact became false this window)
- Archived:      0  facts (faded) — nothing aged past archive_window; this review's
                    archive-candidates are flagged, not yet moved
- Swept threads: 17 completed Open Threads → memory/archive/2026-Q2.md (3 groups: 7-item
                 "Design improvements" + 8-item "Refinements from agent feedback" +
                 2 early milestones v1/v2; all completed 2026-06-12, > archive_window 20)
- Tier changes:  17 (8 working→active; 9 →archive-candidate [5 working→archive-candidate,
                 4 active→archive-candidate])
- Invariants:    prompted — 1 Open Thread raised to re-confirm 5 core invariants
                 (target-repo-scope-only, never-delete-vendor-files, never-pick-a-winner,
                 no-code-markdown-only, upgrades-additive) + the Vision (vision-agent-memory);
                 last_invariant_check was unset and 24 session files ≥ verify_invariants_every 20.
                 last_invariant_check stamped 2026-06-15 | through 2026-06-15-231502.
- Promoted core: 0  (auto-core off; the 5 core facts were human-set, tier untouched)
- Drift:         1 flagged (not fixed) — Blueprint subsection header says "gaps from Current
                 State (v3.7.0)" while status is v4.0.0; raised as ot-drift-blueprint-baseline-20260615.
- Notes: `last_review` stamped 2026-06-15 | through 2026-06-15-231502. The
  archive/sweep path was exercised for the first time on this repo's own memory
  (previously only re-tiering had run). No fact prose was edited — only footers
  (uses/last_used/tier) and the wholesale thread sweep.
