# Session (2026-06-22T23:47:15.000Z)

**Agent:** Claude Code
**User:** Eric Law — make the ADR pattern self-honoring across upgrades; ship it as v4.15.0 and validate by upgrading a target.

## What We Did

Shipped **v4.15.0 (MINOR): ADR log upkeep trigger.** v4.14.0 documented an *optional*
`docs/ADR.md` but nothing told a future session to *maintain* it — the per-session ritual
covered continuity-fact supersession yet never tied it to a linked ADR. Closed that gap by
adding the maintenance/supersession trigger to the protocol sources:

- `templates/AGENTS.md` + root `AGENTS.md` — ADR paragraph gains "**If the log exists, keep it
  alive**": a new durable architecture decision, or superseding/invalidating an `(ADR-NNNN)`-tagged
  fact, → **propose** a ledger update (add newer ADR; mark old `Superseded`/`Deprecated`, never
  delete; keep `formalizes:` ↔ `(ADR-NNNN)` in sync); human-gated.
- `templates/.agent/schema.md` — new "**When to maintain it**" paragraph in the `docs/ADR.md`
  section (on-demand to adopt, but kept in sync once it exists).
- `DECAY.md` §12 — the *Design* primitive notes the ADR lifecycle is kept in sync with fact
  supersession.

Lockstep: `VERSION` → 4.15.0; `CHANGELOG.md` 4.15.0 entry; `UPGRADE.md` version-table row + new
`4.14.1 → 4.15.0` rung (re-sync the guidance; **merge** into a repo-customized ADR note rather than
overwrite — the case a real target hits); `README.md` capability-table row.

**Validated propagation end-to-end (same session):** ran the new rung against the target
`mercury-composable` — merged the upkeep trigger into its (customized, adopted-at-`docs/arch-decisions/ADR.md`)
AGENTS.md, re-synced its `.agent/schema.md` + `DECAY.md §12`, stamped its `.agent/version.md`
→ 4.15.0. (Recorded in that repo's own memory.) This is the proof the user asked for: a protocol
change made in the tool flows to a target via the upgrade ladder.

## Decisions Made

- **MINOR, not PATCH** — adds a behavioral protocol step (maintenance trigger), additive +
  backward-compatible; same classification as 4.14.0 (the ADR feature itself).
- **No new tool ADR** — this is a protocol refinement, not a new Architectural Invariant of the
  tool, so `docs/ADR.md` here is unchanged (the version ladder records it).
- **Merge, don't overwrite, on customized targets** — the rung explicitly preserves a target's
  repo-specific ADR adoption text + path (mercury-composable adopted at a non-default path).

## Context for Next Session

- v4.15.0 is shipped + propagated to one target (mercury-composable). If pushing the tool remote,
  the GitHub repo is `acn-ericlaw/agent-memory`.
- Future targets get the upkeep trigger automatically on their next `4.14.x → 4.15.0` upgrade.

## Memory References

- Referenced: (none — version-ladder work; no continuity fact ids relied upon)
