# Session (2026-08-22T17:48:08.000Z)

**Agent:** Claude Code

## Summary

**Invariant walkthrough — all six never-decay facts confirmed by Eric, one by one, against
live-tree evidence** (closing the human-gated `ot-reverify-invariants-20260814` thread,
open since 2026-08-14; cadence itself was not due at 16 of 40 sessions — Eric ran it on
command, the same pattern as mercury-composable's 2026-08-21 walkthrough).

Per-item evidence and verdicts:

1. **target-repo-scope-only** — CONFIRMED, strengthened: `safe_target_path` realpath
   escape guard, home-target refusal, symlink/self-target/newer-target boundary tests in
   both runtimes; the v4.38.0 fork check also rejects non-repo-local consumer links.
2. **never-delete-vendor-files** — CONFIRMED: MIGRATE.md rule #1 (originals →
   `legacy/<vendor>/`), reconcile "never deletes anything", extras reported never deleted.
3. **never-pick-a-winner** — CONFIRMED, strengthened twice since the last check: §5i
   arbitration + Contradiction doctrine, plus the v4.37.0 fail-closed PRE-APPLY boundary
   (apply refuses with zero writes rather than picking) and the v4.38.0 sanctioned fork
   (legitimate customization converges instead of fighting).
4. **no-build-step-agent-run** — CONFIRMED with a wording enrichment at Eric's direction:
   the letter predated v4.35.0 and sanctioned only skill-bundled scripts, while the tool
   now also ships the operator-side reconcile twins, committed hook fragments, and forge
   CI wrappers. The refreshed text names the grown helper family and the real no-runtime
   fallback (MANIFEST walkable by hand); substance and footer unchanged — the same
   enrichment class as mercury-composable's stack-messaging-kafka refresh.
5. **upgrades-additive** — CONFIRMED: reconcile guarantees enforce it mechanically; the
   managed-built-ins exception list matches the seven verbatim-dir MANIFEST rows exactly.
6. **vision-agent-memory** — CONFIRMED: the week's arc is evidence for it (VBDI trace ran
   field report → blueprint → v4.38.0 → review; adoption-in-minutes proven by two fresh
   enables today; multi-vendor/multi-contributor validated by two external PRs); every
   non-goal intact including never-requires-code (fallback real) and never-a-database.

No supersessions. `last_invariant_check` stamped through this log.

## Memory References

- referenced: target-repo-scope-only, never-delete-vendor-files, never-pick-a-winner,
  no-build-step-agent-run, upgrades-additive, vision-agent-memory (each verified against
  the live tree in the walkthrough)
- referenced/updated: ot-reverify-invariants-20260814 (closed — confirmed by Eric)
