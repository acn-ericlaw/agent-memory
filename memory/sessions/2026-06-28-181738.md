# Session (2026-06-28T18:17:38.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.26.1 (PATCH)** — the pinned-thread refinement from the mercury sanity check.

**The signal:** sanity-checking mercury after Copilot's review, the v4.26.0 `[stale-metadata]` advisory
flagged every `working`-tagged pinned `- [ ]` open thread (5 of them: `bp-*`, `thread-*`) as "should be
`active`." That's **noise** — a pinned thread never decays *regardless* of its tier label; its **pinned-ness**
(being unchecked) is the protection, not the label. The comparison against Copilot's `update-metadata.py`
(which *skipped* pinned threads) made the cleaner rule obvious.

**Refinement:** both `expected_tier`s (memory-lint check 9 + refresh-metadata) now return a pinned thread's
**stored** tier instead of forcing `active` — so `memory-lint` won't flag it and `refresh-metadata` won't
rewrite it. Factual fields (`uses`/`last_used`) still refresh on pinned threads; only the cosmetic tier label
is left alone.

**Built:** 4 code sites (memory-lint.py/.mjs + refresh-metadata.py/.mjs), tests added/updated (memory-lint
34, refresh-metadata assertion flipped: pinned `open-fact` now stays `working`, last_used still refreshes),
`DECAY.md` rule 4 clarified, both SKILL.md notes, `VERSION`→4.26.1, `CHANGELOG`, `README` (table), `UPGRADE`
(row + rung). Skill descriptions unchanged → adapters untouched.

**Verified:** all suites green (memory-lint 34, refresh-metadata 5, archive-fact 6); py↔node parity on both
lint and refresh dry-run; `memory-lint` on this repo 0 errors, 0 warnings.

**Also recorded from the sanity check (no action needed):** mercury's review was sound — **no data loss**
(mercury never used `supersedes`/`superseded-by`/`formalizes` footer fields, so `update-metadata.py`'s lossy
rebuild had nothing to drop), **correct archival** (0 lint errors, no id in both places), and the metadata
pass *was* applied (the review commit re-tiered + recounted). `refresh-metadata` remains a strict, safer
superset of `update-metadata.py` (preserves all footer fields; reads `decay-policy.md`; clamps at
archive-candidate; dry-run; parity + tests).

**Next:** upgrade mercury 4.25.0 → 4.26.1 for the final validation round.

## Memory References
- Created: pinned-tier-refinement-v4261
- Referenced: refresh-metadata-builtin-v4260 (refines); vision-agent-memory (serves)
