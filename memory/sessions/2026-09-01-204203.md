# Session (2026-09-01T20:42:03.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.39.1 (PATCH) — same-thread merge claim: honest boundary**, from an
independent CoPilot assessment of v4.39.0 delivered the day it shipped (overall verdict
8/10; finding #2 actionable). The docs claimed "both sides editing the same thread
conflicts per-file"; **verified before acting** (live git rehearsals, three cases): git
conflicts only on adjacent/overlapping hunks — same-thread edits separated by even one
unchanged line merge cleanly, in a 5-line thread file as much as a 16-line one, with
**both sides kept**. Low severity: nothing is lost (keep-both is MERGE.md's own Tier 1
outcome), the coexisting-statements case belongs to the write-time contradiction check
(DECAY.md §10), and the pre-4.39.0 layout merged identically — no regression. But the
claim must not outrun the mechanism (honest-signal doctrine), so:

- MERGE.md / .agent/schema.md / docs/DESIGN-merge-scale.md now state the measured
  boundary and route the clean-merge case to the contradiction check.
- tests/test_thread_layout_merge.sh pins the boundary with two new cases — separated
  edits merge clean keeping both sides (documented behavior, not a bug); adjacent edits
  conflict (4 → 6 cases, all green).
- Lockstep: VERSION 4.39.1, CHANGELOG, UPGRADE ladder row + 4.39.0→4.39.1 rung, README
  row (table re-trimmed to 10). No script/protocol/shape change → no semantic step, no
  adapter re-sync; targets converge by plain re-copy of MERGE.md + schema.md.

CoPilot findings NOT actioned, with reasons: field effectiveness (time supplies it —
bp-multi-user stays open), archive-union discipline + continuity-remains-shared +
process-serialized reviews (accepted trade-offs already recorded in the design doc),
migration care (mitigated by the lint gates; four clean field runs).

## Memory References

- Referenced: merge-scale-v4390 (the release this refines), bp-multi-user (the external
  assessment is its first independent field signal — noted), honest-signal doctrine
  applied (user-preference class, not a repo fact id)
- Created: merge-honesty-v4391 (tier: working — the ship record)
