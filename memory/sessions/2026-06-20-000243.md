# Session (2026-06-20T00:02:43Z)

**Agent:** Claude Code
**Type:** full

## What happened

Assessed and **ported back a `memory-lint` bug fix** the maintainer had made in `~/sandbox/simple-proxy`,
shipped as **v4.12.1 (PATCH)**.

**How it surfaced.** After committing/pushing v4.12.0, I diffed simple-proxy's committed `memory-lint`
(commit "…+ fix memory-lint dangling-link check") against the tool's and found a real **script** fix
that existed only in the target. The simple-proxy AI session hadn't flagged it for porting; the
maintainer asked me to assess and port it.

**The bug.** `check_dangling` resolved `superseded-by`/`supersedes` links against footers from
`continuity.md` + the archive only. A fact superseded by a target whose footer lives in another
`memory/*.md` — notably `vision.md` — false-flagged as `[dangling] … which has no footer anywhere`.
(This is the same warning I had mis-reported in the first turn as *data* rot; it was a verifier bug.)

**Assessment (endorsed).** The maintainer's fix: `load_repo` pools footers from other `memory/*.md`
(excluding `continuity.md`/`decay-policy.md`, and subdirs via an isfile check) into an `extra` set used
**only** in `check_dangling` — never counted as continuity/archive facts, so counts/overdue/
over-archived are untouched. Correct, minimal, and parity-preserving (both `.py` and `.mjs` mirror,
code-point sort). Its one gap: **no regression test**.

**Ported + tested.**
- Copied the fixed `memory-lint.py` + `.mjs` into the tool (clean superset of the tool's 4.11.x
  scripts per the diff). Added `export` to `load_repo` + `check_dangling` in `.mjs` (additive,
  test-enabling; the `.py` already exposed them via its dynamic loader).
- Added a **cross-file dangling regression test** to both suites: builds a temp `memory/` with a
  continuity fact `superseded-by` a `vision.md` fact → asserts no `[dangling]`; plus a negative
  control (a genuinely missing target still warns) and that the vision fact lands in `extra` but not
  `cont`/`arch`. Exercises `load_repo` end-to-end (the bug site), not `check_dangling` alone.

**Verification.**
- Both suites **8/8** (`python3 -m unittest` ; `node --test test_memory_lint.mjs`).
- **Counterfactual proof** the guard is genuine: on the pre-fix pool `{cont,arch}` the scenario yields
  `[dangling] old-fact superseded-by new-fact, which has no footer anywhere`; on the fixed pool
  `{cont,arch,extra}` → none.
- Real scripts on the scenario: **byte-identical** `.py` vs `.mjs`. `memory-lint` on the tool's own
  memory: **0 errors, 0 warnings**, parity identical.

**Lockstep (PATCH):** scripts + tests; `VERSION`→4.12.1; `CHANGELOG` 4.12.1 (Fixed); `UPGRADE.md`
4.12.0→4.12.1 rung + version-table row; `README` table; `continuity.md` status + new thread.
`SKILL.md` unchanged (no description change → no adapter regeneration). `AGENTS.md`/`DECAY.md`/
`REVIEW.md` unchanged.

## Decisions Made

- **Port the maintainer's fix verbatim, add the missing regression test.** The fix was correct; the
  tool's contribution is the test (the project norm — every memory-lint fix gets a guard, cf. 4.10.1 /
  4.10.4 / 4.11.1) + the cross-runtime parity check.
- **Test `load_repo` end-to-end, not `check_dangling` alone.** The bug lived in `load_repo`; a
  `check_dangling`-only unit test would pass on the buggy code, so it wouldn't be a regression guard.
- **Export `load_repo`/`check_dangling` from `.mjs`** to make the integration test possible — additive,
  matches the `.py` accessibility, keeps the shared test contract symmetric.
- **PATCH (4.12.1).** A built-in script bug fix + test, no shape/description change.

## Memory References

- Created: ot-memlint-dangling-crossfile-v4121 (tier: active — shipped this session)
- Referenced: ot-enforce-sync-adapters-v4120, lint-skill-v490, memlint-node-runtime-v4110,
  backlog-real-work-dogfood, upgrades-additive, vision-agent-memory
