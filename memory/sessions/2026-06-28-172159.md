# Session (2026-06-28T17:21:59.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.25.0 (MINOR) — `archive-fact`, a deterministic safe archive-move helper (6th built-in).**
Applied point 3 of a cross-vendor critique (Copilot / Gemini 3.1 Pro, `review-scratch/critique.md`).

**The critique (3 points), and disposition:**
1. *Upgrade complexity → script Mode B* — valid kernel, but rungs are semantic merges that must stay
   agent-judged; chosen mitigation is deterministic guardrails, not automation. **Logged as a backlog
   thread** (`ot-mode-b-automation-backlog`) — scope only the mechanical steps *if* revisited.
2. *Auto-fix, not just lint* — **declined for semantic decay** (auto-deciding what to archive violates
   `never-pick-a-winner`). The `REVIEW.md` ritual **is** the agent-driven fix; the v4.24.0 advisory triggers
   it; and `archive-fact` now makes the *move* deterministic while leaving the *decision* to the agent.
3. *Harden the memory-writing mechanism (a CLI helper for safe writes)* — **acted on.** Strongest point;
   it names our most-repeated bug (the truncate-before-read trap that wiped this repo's archive 50→6).

**Built `agent-skills/archive-fact/`** (`provenance: agent-memory-builtin`):
- Executes `REVIEW.md` step 4's move: extract a fact's block (column-0 bullet → `<!-- id: … -->` footer,
  whole) → **append** to the quarter archive + `INDEX.md` (append mode) → rewrite `continuity.md` **without**
  it (read whole file into memory, write once — truncate-before-read is structurally impossible).
- **Python + Node at output parity**, mirror tests (`test_archive_fact.py` 7 / `.mjs` 6, all green).
- Guards (refuse, exit 1, all-or-nothing): id with no footer, id already archived, move that would empty
  continuity. `--dry-run` previews. Flags: `--id` (repeatable), `--reason`, `--quarter`, `--note`, `--root`.
- **Meaning/mechanics split** (same as `memory-lint`): the agent decides *what*; the helper does the *move*.

**Verified:** py vs node byte-identical on a fixture (message + continuity + archive + INDEX); both test
suites green; multi-line block extraction + clean removal confirmed. `memory-lint` on this repo: 0 errors.

**Lockstep:** skill + 4 script files, `REVIEW.md` step 4 (preferred path + manual fallback), `ENABLE.md` §5i
(6 built-ins), `README`/`ADR`/continuity built-in lists, `sync-adapters` (7 skills → 42 adapters),
`VERSION`→4.25.0, `CHANGELOG`, `UPGRADE` (row + `4.24.0 → 4.25.0` rung).

**Dogfooded in anger (first real use):** right after shipping, `memory-lint` flagged
`fresh-enable-advisory-depth-v4230` as `[overdue]` (sslu 21 > 20 — it crossed the window as this session's
logs advanced it). Used the brand-new `archive-fact` helper to move it (`--dry-run` first, then real) →
lint back to **0 errors, 0 warnings**. So the tool is no longer just unit-tested — its first production move
worked end-to-end on this repo.

## Memory References
- Created: archive-fact-builtin-v4250; ot-mode-b-automation-backlog
- Referenced: vision-agent-memory (serves)

## Memory Review (2026-06-28, partial — single decay-archival via archive-fact)
- Archived: 1 (`fresh-enable-advisory-depth-v4230` — faded, sslu 21 > archive_window 20) → `memory/archive/2026-Q2.md` + INDEX, via the `archive-fact` helper (its first in-anger use).
- Archive-verify: pass (`memory-lint` 0 errors, 0 warnings; no id in both places).
- Not a full review (last_review stamp unchanged — through 2026-06-28-162543); just the one fact that crossed the window.
