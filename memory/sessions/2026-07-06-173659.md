# Session (2026-07-06T17:36:59.000Z)

**Agent:** Claude Code
**Lightweight:** single-fix session; no `continuity.md` facts touched.

## Summary

Actioned a **second** field report from `mercury-composable`
(`/tmp/agent-memory-continuity-bloat-note.md`): on a 29-module Maven reactor, after a by-the-book
review the *fact* count is healthy (25 < 30) but the `continuity_max_lines` half of
`[continuity-bloat]` trips (708 > 600) on genuinely-active, information-dense Key Decisions — and the
review has **nothing to archive**, so the warning is unclearable. Worse, its generic "a review is due
to lean it down" wording pressures archiving an *active* fact (REVIEW.md's costliest error). Same
failure class as v4.28.2 (an unclearable warning erodes signal), on the line axis.

**Reviewed before implementing** (the user asked for a review first). Verified two of the note's
claims against code: `continuity_max_lines` is *already* per-repo tunable (so the "acknowledgement"
ask mostly exists), and `check_overdue` already yields the archivable-now facts (so an honest message
is cheap). Triaged the 5 suggestions → recommended the decay-aware message (#3) + a doc note (#5's
intent), decline goalpost-moving (#1/#2), **defer the "condense" lever (#4) until it recurs in the
field.** User agreed, incl. the #4 deferral.

**Fix (v4.28.3):** `memory-lint` now computes `archivable` = facts overdue for decay
(`sslu > archive_window`, excl. core/superseded/pinned) + superseded facts, and passes it to
`check_continuity_health`. When lines exceed the cap **and** `archivable == 0`, the message names the
real lever — *"nothing is archivable yet … condense shipped decisions, or raise continuity_max_lines"*
— instead of prescribing a review that can't help; when something *is* archivable the original message
stands. `check_continuity_health` gains an optional `archivable` arg (default unknown → generic;
backward compatible) in both runtimes at parity. `decay-policy.md` comment notes the cap is meant to
be raised for a legitimately large repo. Fact-count check (v4.28.2) untouched.

**Validation:** two new mirror tests per suite (`archivable > 0` → actionable; `archivable == 0` →
active-verbosity); 36/36 pass each. Ran the tool's 4.28.3 lint against mercury's live memory (37 facts
/ 708 lines / 0 archivable) — both runtimes produce byte-identical output with the new honest message.

**Lockstep:** VERSION → 4.28.3; CHANGELOG; README recent-releases (trimmed to 10); UPGRADE.md
capability row + `4.28.2 → 4.28.3` rung; decay-policy template comment. Not yet committed.

## Memory References
(none — no `continuity.md` facts referenced or created. Second in the mercury continuity-bloat arc
after v4.28.2; the deferred "condense shipped decisions" lever is the open follow-on if it recurs.)
