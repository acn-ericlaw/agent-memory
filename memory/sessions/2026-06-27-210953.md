# Session (2026-06-27T21:09:53.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.22.0 (MINOR) — curious knowledge harvest at enable**, in response to a
client-team enablement complaint relayed by the maintainer.

**The complaint (another team, first run on their repo):** discovery "was less curious
than expected." Three concrete symptoms:
1. A canonical `docs/` folder was **skipped entirely** on the first run.
2. When re-asked to recursively analyze that folder, the agent grabbed only the folder's
   **top-level** files — it did **not** descend into subfolders.
3. A root-level markdown **kanban board** and a **decision log** were both missed.
The reporter noted these "are probably not part of the Claude Code project pattern" but
expected a *vendor-neutral* tool to find them.

**Root cause:** `ENABLE.md` **Step 4 ("Analyse the Target Repo")** was a *classifier*, not
a *knowledge harvester* — it read a fixed manifest list (README/package files) and its
"Structure signals" looked only at **top-level** folder names (depth-1). Nothing instructed
the agent to find and read the team's own human-authored documentation. The seeding steps
that depend on Step 4 (5a instructions, 5f smoke-test, 5g vision) inherited the blind spot.

**Scoping (confirmed with the maintainer before authoring):**
- Harvest scope: **markdown knowledge artifacts** (decision logs, ADRs, kanban/roadmap,
  architecture/design notes, CONTRIBUTING, RFCs across `.md/.markdown/.mdx/.rst/.txt/.adoc`),
  excluding code/vendored/generated output.
- Bounding: **budgeted with disclosure** (cap reads, prioritize root→docs→recent; overflow
  recorded as a `(knowledge-harvest)` Open Thread so nothing vanishes silently).

**Changes (all operator-side — `ENABLE.md` only; no installed-file shape change):**
- `ENABLE.md` Step 4: "Structure signals" now says to **descend** doc trees, not stop at
  top-level names.
- `ENABLE.md` **new Step 4b — Harvest existing project knowledge (be curious)**: recursive
  doc-tree descent + repo/module-root sweep by location and name; budgeted read with
  disclosure; **distill don't transcribe** (map-don't-mirror into instructions / Architectural
  Invariants / Open Threads / smoke-test / vision; conflicts → `Contradiction:` thread).
- Seeding steps 5a / 5f / 5g now draw on the Step 4b harvest.
- Step 8 verify: new check #7 (harvest ran; a `docs/` tree leaving no trace ⇒ re-run 4b).
- `VERSION` → 4.22.0.
- `UPGRADE.md`: added the missing **4.21.0** table row + the new **4.22.0** row, and a
  `4.21.0 → 4.22.0` rung — a human-gated **optional re-harvest** to backfill a repo enabled
  by the older shallow scan (additive, no file re-copy, no adapter sync).

**Verification:** `memory-lint` → 0 errors (5 pre-existing review-cadence overdue warnings,
unrelated). No template/skill/adapter touched, so no sync needed.

**Not done (deferred to maintainer):** commit/push (deliberate, human-initiated); README/
CHANGELOG/deck updates if the maintainer wants this line reflected there; whether to run a
memory review (lint shows 5 overdue facts ≥ archive_window — review cadence, the maintainer's
call).

## Memory References

- Created: `knowledge-harvest-curious-v4220` (continuity Open Thread, completed)
- Referenced: vision-agent-memory (serves); `no-build-step-agent-run`, `upgrades-additive`
  (the change respects both — operator-side, additive)
