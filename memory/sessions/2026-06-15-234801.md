# Session (2026-06-15T23:48:01.000Z)

**Agent:** Claude Code

## Summary

Built and shipped **v4.1.0 — the cross-vendor skills layer**, implementing the
`bp-skills-layer` Blueprint gap from its design (`docs/DESIGN-skills-layer.md`). Maintainer
opened the Design→Implementation gate and chose **all-vendor adapter scope** (Claude +
Gemini + Cursor) and **tool-build-only** this session (the client upgrade is a separate
step). Additive MINOR; `DECAY.md`/`REVIEW.md` untouched.

## What happened

- **The layer.** Skills are the shared layer's third leg (beside memory + steering):
  committed, vendor-neutral `skills/<name>/SKILL.md` (name + when-to-use description +
  procedure + optional `scripts/`). The `AGENTS.md` "Skills" section is the universal
  runtime — the agent reads the skill, so it works on any vendor with no engine.
- **Adapters (Option A).** Per-vendor native adapters are thin, regenerated pointers in
  gitignored dirs — Claude `.claude/skills/<name>/SKILL.md`, Gemini
  `.gemini/commands/<name>.toml`, Cursor `.cursor/rules/<name>.mdc`. Only the neutral
  `skills/` is committed; adapters are per-machine.
- **Migration.** `MIGRATE.md` gained principle 6 + **Section B2** (promote vendor skill
  bundles into `skills/`, not flatten into steering) + the Claude Code skills protocol +
  `.claude/skills/` in the detection table + a continuity review thread. `ENABLE.md` Step 5h
  defines the canonical adapter-generation spec (referenced by MIGRATE + UPGRADE).
- **Version ladder.** `VERSION`→4.1.0; `UPGRADE.md` 4.0.0→4.1.0 rung (re-sync schema/AGENTS,
  promote existing `.claude/skills/` in place, no `.gitignore` entry change) + version
  table. `README` version table + vendor table + Migration Behaviour. `CHANGELOG` v4.1.0.
- **Files touched:** `VERSION`, `ENABLE.md`, `MIGRATE.md`, `AGENTS.md` (root),
  `templates/AGENTS.md`, `templates/.agent/schema.md`, `templates/.gitignore`, `UPGRADE.md`,
  `README.md`, `CHANGELOG.md`, `docs/DESIGN-skills-layer.md` (status → implemented).

## Notes / open items

- **Not yet validated on a real target.** Next step (separate session): run the Mode B
  upgrade on the client repo (4.0.0→4.1.0), which promotes its `.claude/skills/` → `skills/`
  and regenerates adapters. That real run is the dogfood validation (expect it to surface
  refinements, like every prior version).
- **Review:** not re-run. We ran the full review earlier this session (last_review
  2026-06-15 through 2026-06-15-231502); this build is +1 session (<review_every 10).
  `continuity.md` is ~405 lines (>300 cap), but the over-size is the known recent-shipped-
  history situation (not yet past archive_window) already flagged — nothing new is
  sweepable, so a re-run would be a no-op.
- Reminder: commit `memory/` + the v4.1.0 protocol/doc changes.

## Memory References

- **Created:** `skills-layer-v410` (the v4.1.0 ship record; tier working)
- **Referenced:** `bp-skills-layer` (closed — realized by skills-layer-v410),
  `vision-agent-memory`, `no-code-markdown-only`, `never-pick-a-winner`,
  `target-repo-scope-only`, `never-delete-vendor-files`, `upgrades-additive`,
  `gitignore-propagation-v310`
