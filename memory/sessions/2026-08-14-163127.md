# Session (2026-08-14T16:31:27.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.34.1 (PATCH) — secret-guard output readability**, from the maintainer's own
regression test of the v4.34.0 enforcing guard (run in mercury-composable on a scratch AWS
config; the guard **blocked correctly** — the feedback was purely presentational): every
`[secret-material]` finding line repeated the same advisory tail, burying the findings.

**Changed:** finding lines are findings only — they end at `(N hit(s), first at line N)`.
The redact/rotate/history guidance now appears **once per run**, per consumer:

- the pre-commit hook's `-> fix it` footer — now preceded by a **blank separator line** (also
  requested) and carrying the consolidated wording ("Committed files are shared — a credential
  that already reached a remote is EXPOSED: rotate it; git history keeps the original");
- a single trailer line in `--scan-files` mode;
- a single trailer after the warnings in a full `memory-lint` run (printed by `report()`).

The guidance line deliberately carries no `[secret-material]` tag, so the hook's and CI
wrappers' `grep` contracts are unaffected. Output-format only — no detection change.

**Verified:** 49/49 mirror tests both runtimes (no fixture asserted the old tails — checked
before changing); byte-parity re-verified; dogfood lint 0/0; the maintainer's exact regression
scenario reproduced in a scratch repo showing the new format in both the enforcing default
and the `AGENT_MEMORY_SECRET_GUARD` advisory opt-down.

**Lockstep:** `memory-lint` runtimes (SECRET_GUIDANCE constant + tail removal), hook footer,
`VERSION` → 4.34.1, `CHANGELOG`, `README` (row + 10-cap trim, drops 4.30.0), `UPGRADE` (row +
`4.34.0 → 4.34.1` rung — re-copy built-in + hook, stamp, verify with a staged-scratch print).
SKILL.md unchanged → adapters untouched.

## Memory References

- referenced: pre-commit-secret-guard-v4340 (the guard whose output this polishes — its first
  field regression test validated the blocking and produced this feedback)
- created: (none — recorded as a fold-up note on the v4340 thread)
