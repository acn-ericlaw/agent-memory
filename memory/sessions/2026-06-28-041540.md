# Session (2026-06-28T04:15:40.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.23.1 (PATCH)** — added a **`last_harvest`** marker so `harvest-knowledge` runs scope
incrementally. From the mercury-composable cross-vendor test drive of v4.23.0.

**The signal:** mercury's own agent ran `harvest-knowledge` and executed the protocol cleanly (a healthy
no-op — memory was already current; check-existing-first + map-don't-mirror + session log + memory-lint all
correct). But it had to **infer the harvest window** ("since enable, 2026-06-20") because nothing recorded
*when the last harvest ran*.

**Decision (maintainer agreed with my reasoning):** keep `last_harvest` in **`continuity.md` Project
State** — with `last_review` / `last_invariant_check`, the same family ("when did this periodic memory
ritual last run") — **not** in `.agent/version.md`. version.md is the install manifest (version-gating +
`last_upgraded` for tool events); mixing a memory-ritual cadence into it conflates concerns and clutters the
field `memory-lint`'s `check_version_manifest` keeps clean. Like-with-like + separation of concerns.

**Built:**
- `templates/.agent/schema.md` — Project State gains an optional `last_harvest: YYYY-MM-DD | through
  <session>` field (omitted until first run).
- `agent-skills/harvest-knowledge/SKILL.md` — **reads** `last_harvest` to scope the next run (full first
  pass if absent), and **stamps** it on completion (even a no-op — records "docs checked through here").
  The harvest owns `last_harvest` as the review owns `last_review`. `last_harvest` only *scopes*; the
  step-4 check-existing-first guard still prevents duplicates, so a re-scan is always safe.

**Note on what the test drive proved (honest):** it validated the **no-drift / no-op** branch of
`harvest-knowledge` (graceful, no manufactured facts) and its portability to a different vendor — but **not**
the "fold a genuinely-new durable fact / resolve a contradiction" branch (no new facts existed). That path
is still unproven in the wild.

**Lockstep:** schema + skill, `VERSION`→4.23.1, `CHANGELOG`, `README` (table +4.23.1 / −4.20.1, still 10),
`UPGRADE` (row + rung). Skill description unchanged → adapters unchanged (no re-sync needed). `memory-lint` OK.

## Memory References
- Created: last-harvest-marker-v4231
- Referenced: harvest-knowledge-skill-v4230 (the skill this refines); vision-agent-memory (serves)
