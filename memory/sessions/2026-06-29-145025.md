# Session (2026-06-29T14:50:25.000Z)

**Agent:** Claude Code

## Summary

**Created a full user-facing documentation site** — MkDocs + Material theme, deploying to
GitHub Pages via the `gh-pages` branch. This is the first proper human-reader docs surface for
the project (distinct from the agent-facing protocol files and the leadership whitepaper/deck).

**Toolchain:** the `/tmp/mkdocs-venv` the user pointed at was a bare/corrupt venv (Python only,
no mkdocs, missing METADATA files). Recreated it fresh with `uv venv --python 3.14` (Python
**3.14.5**, per the user's hint) + `uv pip install mkdocs-material` (**9.7.6**).

**Built (committed `885f266`):**
- **`mkdocs.yml`** — Material theme, deep-purple/cyan palette w/ light-dark toggle, tabbed nav,
  search, mermaid-ready superfences, `pymdownx.snippets` (base_path `.`) to include the root
  `CHANGELOG.md` verbatim. `exclude_docs` for `assessments/` + `optional-ritual-hook.md`;
  `not_in_nav` for the deck HTML (still served + linked). `site_url`/`repo_url` →
  `acn-ericlaw.github.io/agent-memory` / `github.com/acn-ericlaw/agent-memory`.
- **21 content pages** under `docs/`: Home, Getting Started, Concepts (evolving-memory,
  vbdi-loop, skills, decay, reliability), Guides (enable/upgrade/review/merge/author-a-skill),
  Reference (built-in-skills, decay-parameters, vendor-support, protocol-files), + section
  index cards. Existing whitepaper / 5 DESIGN notes / cognitive-framework / ADR log surfaced
  under a **Background** nav section. Changelog page = snippet include.
- **`.github/workflows/docs.yml`** — `mkdocs gh-deploy --force --strict` on push to main
  (paths-filtered to docs surface) + `workflow_dispatch`; `permissions: contents: write`.
- **`docs-requirements.txt`** — pinned `mkdocs-material==9.7.6`.
- **`.gitignore`** — added `site/` (build output).

**Verified:** `mkdocs build --strict` → exit 0, **zero warnings**; deck copied to site; changelog
snippet resolved (v4.26.1 present). User previewed locally (`mkdocs serve` at
`127.0.0.1:8000/agent-memory/`) and approved.

**Content sourcing:** decay parameters taken from the live `memory/decay-policy.md` (3/8/20,
review_every 10, continuity_max_facts 30, continuity_max_lines 600, verify_invariants_every 40);
the 7 built-ins, 6 adapters, and judgment-vs-arithmetic framing mirror the just-refreshed
whitepaper. Docs-only — no protocol, memory-shape, skill, or `VERSION` change.

**Also (CI):** first `docs` workflow run failed — `setup-python`'s `cache: pip` glob matched no
`requirements.txt`/`pyproject.toml`; fixed with `cache-dependency-path: docs-requirements.txt`
(commit `60ebb27`). Re-run **succeeded**; `mkdocs gh-deploy` populated the `gh-pages` branch.

**Operator follow-up (theirs, not the tool's):** enable GitHub Pages once — Settings → Pages →
Deploy from a branch → `gh-pages` / root. The `gh-pages` branch now exists; Pages is **not yet
enabled** (the gh token here lacks `pages` write scope, so it couldn't be done via API). Until
then `https://acn-ericlaw.github.io/agent-memory/` returns 404.

## Memory References
(none — documentation collateral; no continuity facts created or referenced)
