# Session (2026-06-14T02:20:30.000Z)

**Agent:** Claude Code
**User:** Keep going for P2 (invariant-verification cadence).

## What We Did

Shipped **P2 as v3.4.0**: never-decay facts (`core` / Architectural Invariants) can
quietly go "confidently wrong" when circumstances change (assessment gap #2). The
review now periodically prompts a human to re-confirm them. Pairs with v3.3.0
supersession — P1 retires a fact you *notice* is false; P2 makes you *check*.

Mechanism (reuses existing primitives, no new machinery): a `verify_invariants_every`
knob (default 20) in `decay-policy.md` + a `last_invariant_check` tracker in continuity
Project State. During a review, `REVIEW.md` step 6 checks the cadence and, when due,
raises **one** Open Thread listing every never-decay fact for a human to confirm-or-
supersede, then stamps `last_invariant_check`. The review **never auto-invalidates** an
invariant — it only prompts. Checked as a sub-step of the review, so it never fires more
often than reviews.

## Decisions Made

- Prompt-only (raise an Open Thread), not per-fact `last_verified` metadata — minimal,
  reuses Open Threads + supersession; no metadata bloat.
- Verify rides on the review (sub-step), so verify_every ≥ review_every in practice.
- MINOR → **3.4.0** (additive: a policy knob + a tracker field); `UPGRADE.md`
  3.3.0→3.4.0 rung.

## Files Changed

`decay-policy.md` (template + tool's own: `verify_invariants_every`), `REVIEW.md` (step
6 + trigger + summary line), `DECAY.md` (§6 "never-decay ≠ never-checked" note),
`.agent/schema.md` (`last_invariant_check` field + knob), `templates/memory/continuity.md`
(tracker field), `examples/evolving-memory-example/` (first invariant prompt: decay-policy
knob, before/after Project State, a re-verify Open Thread, summary line, README),
`VERSION`→3.4.0, `UPGRADE.md` (rung + tables), `README`/`CHANGELOG`, assessment gap #2
⬜→✅. Dogfooded: tool continuity (status, P2 checked off, `last_invariant_check` added).

## Context for Next Session

v3.4.0 implemented, **not committed** — commit + push (fast-forward on `origin/main`).
Backlog now: P3 (write-time contradiction flag — can lean on supersession), P4 (memory
eval), P5 (provenance/retrieval), the "done" tier-state observation, and the
still-unexercised archive/sweep + (now) invariant-verify cadences (both need aged data).
Two assessment gaps (#1, #2) are now ✅; #3 (P3) is the remaining "Partial".

## Memory References
- Referenced: backlog-invariant-verify-cadence, supersession-v330, upgrades-additive, no-code-markdown-only
- Created: invariant-verify-v340
- Reactivated: (none)
- Superseded: (none)
