# Session (2026-06-16T00:13:42.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.1.1** — pre-adoption refinements to the v4.1.0 skills layer (built earlier
this session, pushed, not yet consumed), per the maintainer's review follow-ups (a)+(b) and
gate (6). Verified adapter formats against current vendor docs, renamed the folder to a
collision-safe name, fixed the Cursor adapter, added a collision guard + double-duty
clarification, and resolved the Blueprint-baseline drift gate.

## What happened

- **(a) Adapter formats verified** (WebSearch, current docs): Claude `.claude/skills/<name>/SKILL.md`
  (`name`+`description`) ✓; Gemini `.gemini/commands/<name>.toml` (`description`+`prompt`,
  `{{args}}`) ✓; **Cursor needed a fix** — `.cursor/rules/<name>.mdc` must use the
  *agent-requested* rule type: `description` + **empty `globs:`** + `alwaysApply: false`
  (the empty `globs:` was missing). Fixed in `ENABLE.md` Step 5h.
- **(b1) Folder renamed `skills/` → `agent-skills/`** across all living docs (perl, safe
  pattern that preserves vendor `.claude/skills/` and is idempotent). Reason: `skills/` is a
  common top-level dir → collision risk; `agent-skills/` is namespaced + still a visible
  peer to `memory/`. Maintainer chose `agent-skills/` over `portable_skills/` / `.agent/skills/`.
- **(b2) Collision guard** added (ENABLE Step 5h + MIGRATE B2): never overwrite a
  pre-existing `agent-skills/`; raise a `Contradiction:` thread (`never-pick-a-winner`).
- **(b3) Vendor-dir double-duty clarified** (MIGRATE B2): `.cursor/rules/`, `.gemini/` are
  both migration sources and adapter targets — archive to `legacy/` first, then generate
  adapters, so they never collide.
- **(6-drift) resolved:** refreshed the Blueprint header `Current State (v3.7.0)` → `(v4.1.1)`;
  checked off `ot-drift-blueprint-baseline-20260615`.
- **Version ladder:** `VERSION`→4.1.1; `UPGRADE.md` 4.1.0→4.1.1 rung + version table;
  `README` + `CHANGELOG` v4.1.1 (v4.1.0 entry left historical). `DECAY.md`/`REVIEW.md` unchanged.

## Notes / open items

- **(6-invariants) STILL OPEN — needs maintainer input.** The invariant-verification gate
  (`ot-reverify-invariants-20260615`) was answered "some need revising," but *which* and
  *how* is pending. Likely candidate: `no-code-markdown-only` may need refining now that
  skills can carry `scripts/` (sh/python) — but do NOT assume; ask. Thread left unchecked;
  handle via supersession (DECAY §9) once specifics are given.
- **Review:** not re-run (full review done earlier this session, last_review through
  2026-06-15-231502; this is +2 sessions < review_every 10). continuity is over the 300-line
  cap — known recent-history situation, leans as v3.x/v4.x ages past archive_window.
- Reminder: commit `memory/` + the v4.1.1 protocol/doc changes.

## Memory References

- **Created:** `skills-layer-v411-fixes` (v4.1.1 refinements; tier working)
- **Resolved:** `ot-drift-blueprint-baseline-20260615` (checked off — header refreshed)
- **Referenced:** `skills-layer-v410`, `bp-skills-layer`, `never-pick-a-winner`,
  `no-code-markdown-only`, `upgrades-additive`, `vision-agent-memory`
