# Session (2026-06-18T21:17:24.000Z)

**Agent:** Claude Code
**User:** investigate then perform migration of the GitLab repo to a new public GitHub repo
(`agent-memory`); maintainer approved public publication (release-candidate status, company protocol
followed, Apache-2.0).

## What happened

**Migrated the project from GitLab to public GitHub, folding in the long-planned `agent-memory-tool`
→ `agent-memory` rename.** New home: `git@github.com:acn-ericlaw/agent-memory.git` (PUBLIC, Apache-2.0).

- **Pre-publish hygiene (read-only) first.** Scanned working tree **and** full 73-commit history for
  secrets/keys, PII, real home paths, and company references — **all clean.** The only `/Users/...`
  hit was a `/Users/jane/` *mock* in a log describing the path-cleanup; the only `acn` hits were the
  maintainer's own commit-author identity (`…+acn-ericlaw@users.noreply.github.com`), which is
  appropriate on their own GitHub. Confirmed target repo visibility = **PUBLIC** before pushing.
- **Cleared the two gates with the maintainer:** (1) public publication approved — RC status, company
  protocol followed, Apache-2.0; (2) the `simple-proxy` / `mercury` codenames are themselves Apache-2.0
  OSS projects, so referencing them in a public repo is compliant.
- **Chose full-history mirror** over a clean-slate single commit — preserves the CHANGELOG / continuity
  / traceability that is the project's whole point, and the history scanned clean.
- **Folded the rename into the migration commit:** updated the two live `agent-memory-tool` references
  (`docs/agent-cognitive-framework.md` prose; the "Publish to GitHub" continuity thread + its remote
  URL). README clone examples already used a generic `your-org/agent-memory` placeholder (left as-is —
  intentionally generic for downstream cloners). Session logs keep the old name (immutable history).
- **Pushed** all history to the GitHub remote, then populated `~/sandbox/agent-memory` (the empty
  GitHub clone) with the full tree + history.

## Decisions Made

- **Public publication is the approved "publication" event** the `agent-memory-tool-personal-research`
  rule gated on — updated that memory accordingly. This public GitHub repo is a **staging step**; one
  more migration to **enterprise GitHub** remains for the official repo (new Open Thread).
- **Rename lands via the migration** — the new repo + working dir are `agent-memory`; the `-tool`
  suffix only ever lived on the old folder/repo name (the canonical name was already `agent-memory`).
- **Preserve, don't squash** — full 73-commit history mirrored.

## Context for Next Session

- **Work in `~/sandbox/agent-memory` going forward** (origin = GitHub). The old
  `~/sandbox/agent-memory-tool` (GitLab) is being retired by the maintainer.
- **Adapters / `review-scratch/` / `__pycache__` are gitignored** and did NOT transfer — run
  **"sync skill adapters"** in the new dir to regenerate the per-vendor adapters before relying on
  native skill invocation there.
- **Auto-memory carry-over:** the user-level `MEMORY.md` is keyed to the old path
  (`~/.claude/projects/-Users-eric-law-sandbox-agent-memory-tool/`); opening the new dir keys a fresh
  project. Carry it over if continuity is wanted (flagged to maintainer).
- Optional: tag a release (e.g. `v4.10.2`) on GitHub now that it's public RC — not done unprompted.
- Remaining migration: **enterprise GitHub** (the official repo).

## Memory References

- Created: ot-publish-github-done (implicit in the updated Publish thread), ot-enterprise-github-migration
  (remaining step)
- Referenced: vision-agent-memory, no-absolute-home-paths, agent-memory-tool-personal-research (gate now satisfied)
