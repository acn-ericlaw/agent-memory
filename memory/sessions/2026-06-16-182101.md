# Session (2026-06-16T18:21:01.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.3.0 — skill authoring convention + "adopt skill" safety-net**, closing a footgun
the maintainer spotted: a skill authored natively in a vendor folder (e.g. a built-in skill
creator → `.claude/skills/`) is gitignored, so it's never shared and isn't the source of
truth. Additive (docs/protocol only). Built across a Claude-server outage — confirmed the
mid-flight edits landed, then continued. Maintainer chose: do #1 (convention) + add #2 (adopt
safety-net), wired into session close.

## What happened

- **#1 Authoring convention** — `AGENTS.md` (root + template) "Skills" → **"Authoring a skill"**:
  create `agent-skills/<name>/SKILL.md`, then "sync skill adapters"; **never** author in a
  vendor folder (gitignored, regenerated pointers).
- **#2 "Adopt a skill" operation** — `AGENTS.md` "Skills" → **"Adopt a skill"**: promote a
  vendor-folder-authored skill into `agent-skills/`, then sync. The on-demand reverse of sync /
  ongoing complement to migration's one-time promote. `MIGRATE.md` cross-references it.
- **Session-close safety check** — "After Every Session" gains a step (+ checklist line):
  detect a skill stranded in a vendor folder (no matching `agent-skills/<name>/`) and prompt
  adopt before commit. Wired into both `AGENTS.md` files.
- **Ladder/docs:** `VERSION`→4.3.0; `UPGRADE.md` 4.2.0→4.3.0 rung + table; `.agent/schema.md`
  note; `README`/`CHANGELOG`; `docs/DESIGN-skills-layer.md` (status + reverse-path note).
  `DECAY.md`/`REVIEW.md` unchanged.
- **Dogfooded end-to-end:** created an orphan `.claude/skills/demo-adopt/`; the detection
  flagged it; adopted it (promote → `agent-skills/demo-adopt/` + regenerate 3 adapters);
  re-detection resolved; cleaned up the throwaway (never committed).

## After-session ritual

- **Skills safety check (new step): PASS** — no stranded vendor-folder skills in this repo;
  the only vendor-folder files are the `hello-world` adapters, which map to
  `agent-skills/hello-world/`.
- **Review cadence:** not re-run — 6 sessions since `last_review` (2026-06-15-231502) <
  `review_every` 10. `continuity.md` remains over the 300-line cap (known standing item; leans
  as history ages out / could be condensed editorially).
- Reminder: commit the v4.3.0 protocol/doc changes + `memory/`.

## Memory References

- **Created:** `authoring-adopt-v430` (v4.3.0 ship record; serves vision-agent-memory)
- **Referenced:** `sync-adapters-v420`, `dogfood-hello-world-skill`, `skills-layer-v410`,
  `bp-multi-user`, `vision-agent-memory`
