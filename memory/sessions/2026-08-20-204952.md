# Session (2026-08-20T20:49:52.000Z)

**Agent:** Codex

## Summary

Started the maintainer-requested full `work-loop` for relocating the memory protocol
from root `AGENTS.md` to `memory/PROTOCOL.md`. The maintainer confirmed that both the
tool checkout and installed targets use an exact one-line root pointer; the tool keeps
its dual-mode operator routing, targets keep a target-only protocol, existing targets
upgrade through v4.35.0, and the relocated protocol is optimized for activation,
efficiency, instruction accuracy, and terseness without unrelated changes.

Created the durable draft at `docs/specs/memory-protocol-pointer/` and registered it in
`docs/specs/README.md`. The draft makes semantic preservation, a 14,500-byte ceiling,
direct bootstrap activation, additive/idempotent lifecycle handling, and customized-target
protection verifiable.

Three adversarial spec-review passes were run. The first two rounds tightened destination
collision handling, repeated customized-target idempotence, documentation/example lockstep,
pre-existing worktree preservation, rollback claims, exact shim bytes, exhaustive directive
inventory, and recovery without an assumed Git baseline. The third pass still found two
issues: fresh-enable collision cases are not explicit in the verification matrix, and
positional AC references drifted after inserting the recovery criterion. The `new-spec`
three-pass limit requires a human steer rather than a fourth silent repair. No product
implementation has begun and no approval status has been advanced.

## Memory References

- referenced: vision-agent-memory, upgrades-additive, no-build-step-agent-run
- created: protocol-pointer-enterprise-activation
