# Session (2026-08-21T05:27:03.943Z)

**Agent:** Claude Code

## Summary

Actioned the multi-vendor compatibility review on PR #24 (the v4.37.0 protocol-pointer
release). Three requested changes, one of them a design decision.

**Dry-run hint contradicted apply.** On a target with an unconverged protocol/shim boundary
the dry-run closed with `re-run with --apply for the mechanical part`, but `--apply`
correctly refuses with zero writes. The dry-run is the consent artifact, so its closing line
now reuses `pre_apply_state` and names the real next move: the PRE-APPLY refusal set when a
hard boundary exists, `--apply --pre-apply-complete` when only confirmations remain, and the
old mechanical/agent-work hints otherwise. The refusal set printed by the dry-run is now the
same set `--apply` prints. Both runtimes changed together and re-verified byte-identical,
including on a boundary target; three mirror tests added per runtime (35/35 each, was 32).

**Continuity `status:` had a leaked WIP marker.** It read "v4.37.0 protocol-pointer conflict
resolutions and gates are complete; awaiting human rebase continuation" — a mid-rebase note
in place of the standing current-state descriptor, which is the exact v4.22.0 anti-pattern.
Restored the house line from the v4.36.0 baseline, bumped to v4.37.0, and added one clause
for the new activation boundary.

**Protocol-row propagation policy: kept `seed-copy`, made the cost explicit.** The review
correctly identified that the old `templates/AGENTS.md` hub was `verbatim`, so protocol-text
edits reached every enabled repo automatically, and that `seed-copy` silently ends that for
the most-edited installed file. Kept `seed-copy` — the `4.36.0 → 4.37.0` rung merges the old
root's local directives *into* `memory/PROTOCOL.md`, and a mechanical re-copy could drop
team-authored steering irrecoverably once the old root is gone; that also holds AC-MP-07/08's
"preserved without loss". The cost is now named rather than accidental: `MANIFEST.md`'s row
notes and a new `UPGRADE.md` manifest-lockstep bullet make a Semantic steps row a **standing
release obligation** for any release that edits `templates/memory/PROTOCOL.md` (re-copy a
still-stock target protocol, arbitrate a customized one per `ENABLE.md` §5i). The alternative
— move the row to `verbatim` and lean on §5i drift arbitration — is left open for the
maintainer as `ot-protocol-text-propagation`, since it trades an approved AC for the house
model.

Also raised `ot-agents-native-activation-canary` from the review's multi-vendor verdict:
activation on AGENTS.md-native runtimes (Codex, Kiro, Antigravity) is reasoned, not observed,
and should be treated as unvalidated until a field canary confirms it. And untracked the
work-loop harness's run-local `engine-state.json` / `state.json` under `docs/specs/`, adding
a path-scoped `.gitignore` rule so workflow residue stops accreting there; the spec, plan,
and walkthrough stay tracked.

Deferred deliberately: the 16 lint advisories (5 `[review-overdue]`, 11 `[stale-metadata]`)
are metadata, and the protocol forbids editing `uses`/`last_used`/`tier` mid-session — one
`refresh-metadata` pass plus the due review belongs after merge, as the review itself
proposed. The `templates/` nested-auto-load bleed is the pre-existing v4.29.1 residual, no
regression, no action.

## Verification

- Python and Node reconcile suites: 35/35 each.
- Python and Node `--check-manifest`: manifest OK (43 sources, 16 semantic steps).
- Byte parity re-confirmed on a fresh target, a 4.36.0 boundary target, and a
  confirmation-only target — dry-run output identical between runtimes.
- Dry-run refusal set verified equal to the `--apply` refusal set on the boundary target;
  `--apply` still writes nothing there.
- Python and Node `memory-lint`: no new errors.
- `mkdocs build --strict`: pass.

## Memory References

- Referenced: `vision-agent-memory`, `bp-reconcile-target-state`, `git-hook-fragment-dispatch`
- Referenced/updated: `protocol-pointer-enterprise-activation`
- Created: `ot-protocol-text-propagation`, `ot-agents-native-activation-canary`
