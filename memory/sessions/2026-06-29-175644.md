# Session (2026-06-29T17:56:44.000Z)

**Agent:** Claude Code

## Summary

**Shipped v4.27.0 (MINOR) — standardized PR descriptions: lead with What / Why.** From a
maintainer suggestion to standardize the look-and-feel of pull-request descriptions created under
the protocol, and to **propagate it to every AI-enabled repo**.

**Why it fit cleanly:** *why* is already a first-class artifact throughout agent-memory (VBDI
intent trace, ADR rationale, supersession reasons, the changelog's own `> summary + Why` shape),
and the content projects straight from the session log(s) in a PR (What from the change, Why from
the Blueprint gap / decision). Confirmed there was **no prior PR guidance** (only the commit
`Co-Authored-By` trailer) — a real gap.

**Shape (decided):** two sections — **What** (the change) and **Why** (substantive intent, not a
restatement of What) — each **1–2 short paragraphs** (flexible, not rigidly "two paragraphs", so a
trivial PR isn't padded), closing with a self-identifying **`Co-Authored-By:`** footer (extends the
commit/session-log authorship convention to the PR altitude — added on maintainer review).
**Advisory, never a gate** (`guide-don't-prescribe`).

**Built (lockstep):**
- `.github/pull_request_template.md` — this repo **+ `templates/`** (so enabled repos inherit it).
- `AGENTS.md` (root + `templates/`) — the commit step gains an "Opening a PR? → What / Why"
  convention + a checklist line. This is the **vendor-neutral backstop** (the `.github/` template
  only covers GitHub web UI / `gh pr create`; the steering line covers agents composing a PR body).
- `ENABLE.md` Step 6 installs the template (tracked — it travels).
- `UPGRADE.md` — `4.26.1 → 4.27.0` rung (install template + re-sync `AGENTS.md` from
  `templates/AGENTS.md`) + version-table row.
- `CHANGELOG.md` top entry; `README.md` version-table row + file-tree entry; `VERSION` → 4.27.0.
- **Docs site:** `getting-started.md` — a "PRs lead with What & Why" tip + a row in the
  "what gets created" table. (Auto-redeploys via the `docs` workflow on push.)

**Verified:** `mkdocs build --strict` exit 0; `memory-lint` **0 errors** (2 pre-existing advisories
on the tool's own memory — `[overdue] ot-reverify-invariants-20260627` and `[stale-metadata]
knowledge-harvest-curious-v4220` — unrelated to this change; the repo is due for its own review,
a separate maintainer call). No memory-file shape change.

## Memory References
- Created: pr-what-why-convention-v4270
