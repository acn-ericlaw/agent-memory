# Session (2026-06-18T17:11:33.000Z)

**Agent:** Claude Code
**User:** install memory-lint + second-opinion + apply-critique in the ENABLE and UPGRADE paths

## What happened

Maintainer revised the v4.10.0 install posture: the built-in skills are **essential/important**,
so install them by default rather than offer them opt-in. Folded into the still-unreleased
v4.10.0 (no version bump — refined the same rung).

- **ENABLE Step 5i** rewritten "opt-in offer" → **"install (all modes)"**: every enabled repo
  (including a fresh Mode A) now gets `memory-lint` + `second-opinion` + `apply-critique` copied
  into `agent-skills/` with adapters regenerated and `review-scratch/` gitignored. Idempotent;
  never touches unrelated `agent-skills/` content. Step 8 verify + Step 9 report lines updated.
- **UPGRADE 4.10.0 rung** rewritten to **install** all three built-ins into existing repos
  (so `memory-lint`, left tool-local at v4.9.0, now reaches targets on upgrade — the review
  ritual relies on it).
- **Propagated the posture flip** through `README` + `CHANGELOG` + `UPGRADE` table + the design
  doc (§9 non-goal reframed: *installed, but zero-overhead-by-default — installing ≠ running*;
  fork #6 + §12 updated) + `continuity` (status line, `fresh-review-v4100`, the Blueprint
  delivery note).
- **Superseded memory-lint's v4.9.0 stance.** `lint-skill-v490` said "not auto-installed —
  deliberate future option"; added a forward-pointer that v4.10.0 **took** that option.
- Regenerated `memory-lint`'s gitignored adapters (now a shipped skill — keep the dogfood
  consistent). memory-lint: clean.

## Decisions Made

- Built-in skills (`memory-lint` + the review pair) are **installed into every enabled repo**
  via ENABLE + UPGRADE — not opt-in. Rationale: essential/important to the core workflow;
  installing a skill ≠ running it, so zero-overhead-by-default still holds.
- `hello-world` stays a demo — **not** shipped into targets.

## Context for Next Session

- A fresh Mode A enable now creates a **populated** `agent-skills/` (never empty) — worth a
  glance on the next real enable to confirm the copy + adapter-regen + `review-scratch/` gitignore
  flow end-to-end.
- Still not dogfooded end-to-end (the `second-opinion` → `apply-critique` loop) — the design §11
  test-drive remains the next validation before publish.

## Memory References

- Referenced: vision-agent-memory, fresh-review-v4100, lint-skill-v490 (substance updated — install posture), bp-fresh-context-review, review-verify-v480, no-build-step-agent-run, never-pick-a-winner
