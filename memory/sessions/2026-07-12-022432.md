# Session (2026-07-12T02:24:32.000Z)

**Agent:** Claude Code

## Summary

Post-v4.29.0 follow-up: the maintainer asked why only `CLAUDE.md`/`GEMINI.md` gained the
`@`-import treatment — what about the other vendors' entry points? Answered with the
three-class analysis and, at the maintainer's direction, recorded the future levers as a
backlog Open Thread (`ot-before-session-cursor-kiro-backlog`):

1. **Import-capable entry files** (Claude Code `@path`, Gemini CLI `@./path.md`) — done in
   v4.29.0; the only two vendors whose entry file defines an import syntax.
2. **AGENTS.md-native runtimes** (Codex/ChatGPT, Kiro, Antigravity) — the hub is already
   auto-loaded by the runtime; only the `AGENTS.md → memory/*` hop stays voluntary. No
   import syntax goes into `AGENTS.md` itself — it stays vendor-neutral.
3. **No-mechanism vendors** (Copilot `copilot-instructions.md`, `.cursorrules`,
   `.windsurfrules`) — prose pointer stands; the only inline lever is the v4.20.1
   front-load pattern, reserved for small stable snippets, never protocol copies (drift).

**The recorded levers:** Cursor modern rules (`.cursor/rules/*.mdc`, `@`-file references)
and Kiro steering (`.kiro/steering/*.md`, `#[[file:…]]` inclusion) could carry committed
steering imports — blocked today by our own wholesale `.gitignore` of `.cursor/`/`.kiro/`,
not by the vendors; adoption needs a path-scoped carve-out (the `.github/skills/` pattern)
plus a committed steering file per vendor. Deliberately **not built speculatively** —
trigger is a Cursor/Kiro team reporting the context-read failure class (complaints =
adoption signal).

Also this working session (logged separately): mercury-composable upgraded 4.28.3 → 4.29.0
and pushed (first field deployment of the presence fix).

Also: ran `refresh-metadata` at the maintainer's direction (dry-run previewed, then applied) —
5 footers recomputed from the reference log (`coauthor-stable-identity-v4280` →active,
`pinned-tier-refinement-v4261`/`refresh-metadata-builtin-v4260`/`archive-fact-builtin-v4250`
→archive-candidate, `bp-before-session-presence` →active uses 2). `[stale-metadata]` cleared;
lint now 0 errors / 6 `[overdue]` advisories, which need a review's archival judgment, not
arithmetic. (Metadata recompute — not usage; no new Memory References.)

## Memory References

- referenced: bp-before-session-presence (the backlog thread extends its per-vendor reach)
- created: ot-before-session-cursor-kiro-backlog
