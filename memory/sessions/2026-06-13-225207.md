# Session (2026-06-13T22:52:07.000Z)

**Agent:** Claude Code
**User:** Relocate `DESIGN-evolving-memory.md` from the repo root into `docs/`.

## What We Did

Moved the design-rationale doc to `docs/DESIGN-evolving-memory.md` via `git mv`
(history preserved). It's not a control/installed file (unlike `DECAY.md`/`REVIEW.md`/
`ENABLE.md`, which must stay at root and ship into targets), so this is pure repo
housekeeping — **no version bump, no UPGRADE rung**. Updated the live references
(`CHANGELOG.md`, `continuity.md` ×2) and corrected the doc's own stale "doc location"
note (it had said it would be *removed* after the DECAY/REVIEW split; it was kept as
rationale and now lives under `docs/`). Left the immutable session log `024537` as-is
— it correctly records the doc was at root at that time.

## Decisions Made

- Only `DESIGN-evolving-memory.md` moves; the root protocol/control docs stay put.

## Context for Next Session

`docs/` now holds the design doc, the two assessments, and the optional-hook recipe.
Nothing else outstanding from this segment.

## Memory References
- Referenced:  (none — housekeeping; no tracked facts touched)
- Created:     (none)
- Reactivated: (none)
