# Session (2026-06-20T00:52:33Z)

**Agent:** Claude Code
**Type:** full

## What happened

Recorded a **cross-vendor validation of v4.13.0** (tool-provided-skill provenance guardrail), reported
by the maintainer after testing in the target repo `~/sandbox/simple-proxy`.

Examined that repo's latest session log (`memory/sessions/2026-06-20-004718.md`, **agent: Gemini CLI**):
a Gemini agent attempted an in-place cosmetic edit to `agent-skills/memory-lint/SKILL.md`, **recognized
the `provenance: agent-memory-builtin` marker** and the rule forbidding in-place edits to tool-provided
skills, and **refused** — enforcing fork-or-upstream. Its own note: "Guardrail test successful."

**Why it matters.** This is the first real-world, *different-vendor* confirmation that the v4.13.0
guardrail fires: Claude Code authored the marker + guidance; **Gemini CLI independently honored it** with
no out-of-band knowledge — clean-room evidence the marker is self-describing across vendors. It exercises
the *behavior* (a refusal at edit time), not just the presence of the marker — the exact failure mode that
nearly stranded the simple-proxy `memory-lint` fix is now demonstrably caught.

Recorded on the existing `ot-system-skill-provenance-v4130` thread (validation line) + the status line.
No code/doc change to the tool — this is a validation event.

## Decisions Made

- **Record as a cross-vendor validation, append to the shipped thread** (not a new fact) — consistent with
  how prior validations are logged (e.g. the v4.11.0 Gemini reproduction). Left the thread's usage footer
  for the review to own; referenced the id this session.

## Memory References

- Referenced: ot-system-skill-provenance-v4130 (cross-vendor validated this session), vision-agent-memory
