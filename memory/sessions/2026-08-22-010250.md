# Session (2026-08-22T01:02:50.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.38.0 (MINOR) — onboarding efficiency: consumer routing + close-record
economy**, from the mercury-composable team's handoff (`~/Desktop/agent-memory-onboarding-
handoff.md`, extracting a fresh-agent assessment of their repo; maintainer ratified the
full scope). Two verified field costs: agents consuming the repo *as a product* were routed
into contributor memory, and completed-`[x]` ship narratives were 64% of continuity
(measured; the tool's own repo measured 54% — 306 lines by the new counter).

**Shipped:**

1. **Sanctioned contributor/consumer root fork** (assessment P1): `AGENTS.md` accepts two
   forms — the stock one-liner, or a routing fork (canonical read-imperative first line +
   repo-local consumer link, ≤ 16 non-empty lines / ≤ 2 KB). Reconcile `fork-ok` attr on
   the MANIFEST row; `ENABLE.md` Step 6 carries the emission block (owner-declared consumer
   entry only — never fabricated). The target protocol opens with a consumers-exit-here
   step-0. Verified live: mercury-composable's ratified fork now reports
   `ok — sanctioned consumer fork` and its PRE-APPLY hard boundary is gone.
2. **Close-record economy** (P2, the high-leverage one): protocol Update-continuity item 2
   + schema spec close records as 3–6-line stubs (outcome, refs, one durable lesson,
   `origin:`); `REVIEW.md` step 5 condenses oversized records at review time (prose only —
   ids/footers immutable; a condensed thread archives as its stub, retrieval follows
   `origin:`); new `memory-lint` check 11 `[closed-thread-bloat]` with
   `closed_narrative_max_lines` (default 150; knob in decay-policy templates + both live
   policies; absent knob falls back — no target action required).
3. **Optional ready-to-work checkpoint** (P4) in Work-from-intent, both protocols;
   explicitly optional. P3 (vendor identity): no change, concurring with the handoff.
4. **Release mechanics:** the Semantic-steps row for the protocol edit — the **first
   exercise** of the v4.37.0 propagation obligation. Lockstep: MANIFEST (attr doc + row +
   note + semantic row), UPGRADE (row + `4.37.0 → 4.38.0` rung), ENABLE Step 6, CHANGELOG,
   README (row + 10-cap trim — dropped 4.33.1/4.33.0; the table had drifted to 11 rows),
   docs site (built-in-skills eleven checks, protocol-files fork note, decay-parameters
   knob), evolving-memory example policy, VERSION → 4.38.0.

**The lesson that bit again (third strike): verbatim fixtures.** My first fork bound
(≤ 8 non-empty lines, from the handoff's *quoted* two-paragraph fork) passed the suites and
then rejected the live artifact — the team had grown their root to three paragraphs
(11 lines, 945 bytes, a routing-only disambiguation paragraph, ratified). Fixed the bound
to fit the live artifact with headroom (≤ 16 / ≤ 2 KB) and pinned the live file **verbatim**
as `MC_FORK` in both suites, byte-identity proven against disk before shipping.

**Verification:** reconcile suites 38/38 ×2 runtimes; `--check-manifest` OK ×2; lint suites
52/52 ×2; dogfood lint 0 errors (the new advisory fires on our own 306 closed-narrative
lines — honest, condensation lands at the next review); live dry-runs byte-parity on
mercury-composable (fork converged) and mercury; no local mkdocs (content-only page edits,
nav untouched — the docs workflow builds on push).

Left for the field: mercury-composable's local copilot-instructions.md one-line
localization stays ordinary verbatim drift — the v4.38.0 upgrade recopies it; the concept
it expressed now lives in the fork itself. The success-measure canary (re-run the
fresh-agent context-efficiency test after two more reviews, against 64%) belongs to
mercury-composable's memory at its next session there.

## Memory References

- created: onboarding-efficiency-v4380
- referenced: vision-agent-memory, ot-protocol-text-propagation,
  protocol-pointer-enterprise-activation, bp-reconcile-target-state,
  backlog-real-work-dogfood
