# Session (2026-06-14T03:07:29.000Z)

**Agent:** Claude Code
**User:** Upgraded the enabled repos to v3.7.0; set the next major direction (VBDI); paused.

## What We Did

- **Upgraded both enabled repos to v3.7.0** via the Mode B version-aware ladder (3.1.0 →
  3.7.0, cumulative rungs): `~/sandbox/simple-proxy` (mode A, `a651543`) and
  `~/sandbox/mercury-composable` (mode C, `fca2c69b`). Each got the verbatim doc re-sync
  (DECAY/REVIEW/schema/AGENTS), the verify knob + `last_invariant_check`, a seeded
  `memory/smoke-test.md`, stack-altitude notes, and a 3.7.0 stamp. Additive,
  non-destructive, source untouched; verified byte-identical generic docs; idempotent.
- **Set the next major iteration:** a **Vision → Blueprint → Design → Implementation
  (VBDI)** lifecycle layer (see the new Open Thread `vbdi-lifecycle-direction`). Framed
  it as the forward-looking complement to the backward-looking memory layer, reusing the
  primitives shipped in P1–P5 (linkage, supersession, contradiction-check, human gates,
  smoke test). Agreed to **pause at the framing stage** and tackle it methodically next.

## Decisions Made

- vNext backlog (P1–P5) is **complete**; VBDI is the new headline direction.
- Approach for VBDI: dogfood it — Vision first (human's to set), then
  `DESIGN-vbdi-lifecycle.md`, then structured decisions + the version ladder.
- Recorded the direction in memory *before* pausing so resumption is drift-free — the
  tool doing its own job at the meta level.

## Context for Next Session

**Resume here:** start the VBDI work by capturing the **Vision** (the user articulates
it, or I draft a strawman they sharpen), then a `DESIGN-vbdi-lifecycle.md`. The framing,
mission, primitive-reuse map, and hard parts are all in the `vbdi-lifecycle-direction`
Open Thread. Lower-priority loose ends still open: the "done" tier-state tidy-up;
actually *running* the smoke tests; exercising archive/sweep + invariant-verify cadences
on aged data; backfilling `origin` on the v3.3–3.6 shipped-record facts at the next
review; re-running the industry-alignment assessment. All three repos are committed and
in sync (tool at v3.7.0; both enabled repos upgraded to 3.7.0).

## Memory References
- Referenced: upgrades-additive, no-code-markdown-only, dogfood-target-repo-self-contained, provenance-retrieval-v370
- Created: vbdi-lifecycle-direction
- Reactivated: (none)
- Superseded: (none)
