# Session (2026-06-27T22:24:24.000Z)

**Agent:** Claude Code

## Summary

**Consolidated this session's unreleased version bumps into one v4.22.0 release, and encoded the
versioning policy** — from a maintainer observation.

**Context:** HEAD (pushed) was v4.21.0; the working tree had reached v4.25.0, with nothing staged or
committed. So v4.22.0–v4.25.0 were four version numbers + four `UPGRADE.md` rungs minted for work that
never released independently — a four-rung ladder for what is really one additive release.

**Done:**
- `VERSION` → **4.22.0** (one MINOR over the released 4.21.0; all four changes additive).
- `UPGRADE.md`: four table rows → **one** 4.22.0 row; four rungs → **one** `4.21.0 → 4.22.0` rung
  enumerating the four bundled features (a/b curious discovery + fresh-enable advisory/depth, c
  merge-friendliness, d MERGE.md).
- **Policy encoded** in `UPGRADE.md` "Versioning model" — *"One version per release, not per feature":*
  bump for a release event; keep a single **pending** version while unreleased; magnitude = largest
  change in the batch; released baseline = `VERSION` at `HEAD`, consolidate working-tree bumps beyond it
  before committing; per-feature detail lives in session logs + Open Threads, not extra version numbers.
- continuity `status` token → v4.22.0 (+ the design-marker "(v4.24.0)"→"(v4.22.0)").
- The four shipped Open Threads kept as **distinct facts** (distinct ids/origins) but headers relabeled
  "Shipped in v4.22.0 (dev-iter 4.2X)". **Session logs left immutable** — they honestly journal the
  4.22–4.25 iteration; version numbers track releases, memory tracks the work.

**Not changed:** the dev-version slugs in fact ids (e.g. `...-v4230`, `...-v4250`) — ids are stable
identifiers, not release labels; renaming them is discouraged. In-body cross-references between the
features (e.g. "the v4.24.0 union/take-later rules") read as dev-iter references and were left as-is.

**Verification:** see the run after this log — `memory-lint` + a single-rung/single-row check.

**Not done (maintainer's call):** commit/push (now a clean single-release batch: v4.21.0 → v4.22.0);
README/CHANGELOG.

## Memory References
- Created: version-consolidation-policy-v4220
