# Session (2026-06-18T06:54:58.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.9.0 (MINOR)** — **`memory-lint`**, a portable, deterministic verifier skill — the
stronger fix Copilot argued for (v4.8.0's markdown guard still left the primary count to the LLM).
On its **first run it caught a real over-archival that both Copilot and a hand re-check had missed**
(`skills-layer-v411-fixes`), which I reactivated.

## What happened

- **Built the skill.** `agent-skills/memory-lint/SKILL.md` + `scripts/memory-lint.py` (Python 3,
  stdlib only, no install). Deterministic checks: (1) no id in both `continuity.md` + archive;
  (2) no archived-as-faded fact referenced within `archive_window` — the decay-miscount guard;
  (3) advisory overdue (`sslu > archive_window`), excluding `core` / `superseded` / pinned `- [ ]`
  open threads (which never decay); (4) supersession links resolve. Exit non-zero on error.
- **Dogfooded — and it earned its keep immediately.** First run flagged `skills-layer-v411-fixes`
  as over-archived (last referenced 16 sessions ago ≤ archive_window 20). The Copilot review
  archived it *and* my earlier hand re-check had waved it through as "defensibly faded" — both wrong;
  the script's count was right. **Reactivated** `skills-layer-v411-fixes` to `continuity.md`
  (`active`), removed its archive + INDEX entries. Re-run: 0 errors (1 advisory WARN —
  `skills-layer-v410` sslu 30, which Copilot *under*-archived; left for the next review).
- **Refined the lint mid-build:** added pinned-`- [ ]`-open-thread exclusion to the overdue check
  (two false-positive WARNs disappeared, confirming the rule "unchecked threads never decay").
- **Wired in:** `REVIEW.md` step 6 now points to `memory-lint` as the recommended deterministic
  version of the verify ("let the script count"), guarded with "if present."
- **Scope:** built in the tool's own repo (where the Copilot incident happened — so it now guards
  the tool's own memory). **Not auto-installed into targets** — that would add a script to every
  enabled repo, a deliberate future decision. `VERSION`→4.9.0; `UPGRADE.md` 4.8.0→4.9.0 rung + table;
  `README`/`CHANGELOG`. `AGENTS.md`/`SKILLS.md`/`DECAY.md` unchanged.

## Notes / open items

- **Defense-in-depth now complete for the decay class:** deterministic rule → v4.8.0 in-ritual grep
  guard → v4.9.0 deterministic lint → second-agent verification. The lint is the one that removes the
  LLM from the arithmetic.
- Advisory: `skills-layer-v410` (sslu 30) is overdue for archival — next review may sweep it.
- Open option: auto-install `memory-lint` into targets (adds a script per repo) + wire a pre-commit
  hook. Candidate Blueprint thread if pursued.

## Memory References

- Referenced: vision-agent-memory, review-verify-v480, no-build-step-agent-run, skills-layer-v410
- Created: lint-skill-v490 (tier: working)
- Reactivated: skills-layer-v411-fixes (memory-lint caught it over-archived, sslu 16 ≤ archive_window)
