# Session (2026-07-12T01:38:17.000Z)

**Agent:** Claude Code

## Summary

Investigated a child-repo field report (`~/Desktop/ai-context-read-reliability.md`, 2026-07-11):
agents skipped the before-session context read (`CLAUDE.md → AGENTS.md → memory/*`) under task
pressure — skill-unawareness, off-model engagement, rework. The repo patched itself locally
(SessionStart injection + attestation canary + Stop-hook verify) and recommended upstreaming.

**Verdict: structural gap in the tool, not an operational lapse.** The v4.19.0 trigger layer
(`docs/DESIGN-ritual-triggers.md`) reinforces only the *after*-session rituals because its
vendor-neutral substrate — git + CI — has no session-start moment; the before-session read stayed
Layer-0 prompt adherence, the same non-determinism v4.20.1 recorded for Copilot self-init. Every
enabled repo inherits it.

**But the report's headline remedy doesn't upstream as-is:**

- A `SessionStart` hook is Claude-Code-only (the report's own §7 concedes this against its
  "every vendor at once" claim) — Layer-2 in the tool's own taxonomy.
- Its carrier, a **committed `.claude/settings.json`**, conflicts with the installed
  `.gitignore` (all of `.claude/` is ignored by design) and demonstrably leaked a personal,
  machine-specific allowlist entry into the shared file.
- Layers 2–3 (canary beacon + board oracle + verify hook) are per-repo by construction (the
  report calls its own rollout "an adaptation, not a copy") — they stay downstream.

**Agreed upstream shape** (drafted this session as `bp-before-session-presence` + the rung below):

1. Native `@`-imports in `templates/CLAUDE.md` + `templates/GEMINI.md` — presence becomes
   structural for import-capable runtimes; markdown-only; same fix-shape as v4.20.1.
2. An **opt-in** Claude Code `SessionStart` injection recipe in `docs/optional-ritual-hook.md`
   (tool-only doc; never installed by default).
3. Attestation canary stays downstream; possibly a doc pointer as a pattern.

Drafted first (Blueprint gap recorded, human gate honored); the maintainer approved in the same
working session — **shipped as v4.29.0** (see "Ship" below).

## Draft — ladder row + rung for the ship commit (pending version; assumes it releases alone atop 4.28.4 as **4.29.0 MINOR**)

Ladder table row:

```markdown
| 4.29.0 | **Before-session context presence (MINOR):** closes the before-session half of the ritual-trigger asymmetry — v4.19.0 made the *after*-session rituals fire vendor-neutrally, but git/CI has no session-start moment, so "read `AGENTS.md`/`memory/*` first" stayed advisory prose (empirically skipped under task pressure; child-repo field report 2026-07-11). `templates/CLAUDE.md` + `templates/GEMINI.md` now carry native **`@`-imports** (`@AGENTS.md`, `@memory/instructions.md`, `@memory/continuity.md`, `@memory/vision.md`) so the hub + core memory files are structurally present every session on import-capable runtimes (same fix-shape as v4.20.1's copilot-instructions front-load); imports live only in per-vendor bootstrap files — `AGENTS.md` stays vendor-neutral. `docs/optional-ritual-hook.md` (tool-only) gains an **opt-in** Claude Code `SessionStart` injection recipe — never installed by default (a committed `.claude/settings.json` conflicts with the installed `.gitignore` and risks leaking personal allowlists). Attestation canaries remain a downstream per-repo pattern. Honest limits: imports can't express dynamic paths (`memory/sessions/`); Cursor/Windsurf/Copilot keep the prose pointer; imported files enter context every session, so the continuity-bloat controls (v4.24.0/4.28.2/4.28.3) are now load-bearing. No memory-file shape change; skills/adapters unchanged |
```

Rung:

```markdown
## Rung: 4.28.4 → 4.29.0 — before-session context presence: bootstrap `@`-imports + opt-in SessionStart recipe (MINOR)

**What changed:** closes the before-session half of the ritual-trigger asymmetry. v4.19.0 made the
*after*-session rituals fire vendor-neutrally (git hook + CI), but that substrate has no
session-start moment, so the "read `AGENTS.md` / `memory/*` first" instruction remained advisory
prose — prompt adherence, empirically skipped under task pressure (child-repo field report,
2026-07-11: skill-unawareness, off-model engagement, rework). Fix, in philosophy order: **(a)** the
`templates/CLAUDE.md` and `templates/GEMINI.md` bootstrap pointers now carry native **`@`-imports**
(`@AGENTS.md`, `@memory/instructions.md`, `@memory/continuity.md`, `@memory/vision.md`) so the hub
and core memory files are **structurally present** in every Claude Code / Gemini CLI session —
markdown-only, no hooks, the same fix-shape as v4.20.1's copilot-instructions front-load. Imports
live only in the per-vendor bootstrap files; `AGENTS.md` stays vendor-neutral. **(b)**
`docs/optional-ritual-hook.md` (tool-only, not installed) gains an **opt-in** Claude Code
`SessionStart` injection recipe for teams that also want `memory/sessions/` recency injected —
never installed by default (a committed `.claude/settings.json` conflicts with the installed
`.gitignore`, and risks leaking personal allowlist entries into a shared file). Attestation
canaries remain a downstream per-repo pattern, not part of the tool. Honest limits: imports can't
express dynamic paths (`memory/sessions/` newest-N); Cursor/Windsurf/Copilot have no import
mechanism and keep the prose pointer; the imported files enter context every session, so the
continuity-bloat controls (v4.24.0/4.28.2/4.28.3) are now load-bearing, not cosmetic. Presence is
guaranteed; *attendance* remains agent judgment. No memory-file shape change; skills/adapters
unchanged.

**Steps:**

1. **Re-sync the bootstrap pointers additively.** Only for bootstrap files that are *ours*
   (content check: they reference the agent-memory system / `memory/instructions.md`): merge the
   import block from `templates/CLAUDE.md` into the target's `CLAUDE.md`, and from
   `templates/GEMINI.md` into `GEMINI.md`, **preserving** the project name/one-liner and any
   repo-specific lines — merge, never overwrite. Skip a vendor's file the target doesn't have.
2. **Offer (don't install) the SessionStart recipe.** Point the user at the new section in
   `docs/optional-ritual-hook.md`; adopting it is a conscious per-user/per-repo choice.
3. **Stamp** `.agent/version.md` → `version: 4.29.0`, `last_upgraded: <today>`, preserving
   `enabled_with` and `mode`. **Use the Edit tool (or read-into-a-variable then write) — never a
   truncate-first one-liner.**
4. **Verify:** in a fresh Claude Code session the memory files are present without an explicit
   read (the agent can state the current `last_session` / top open thread before opening any
   file); `memory-lint` clean.
5. **Report**: before-session context presence is now structural on import-capable runtimes and
   documented as an opt-in hook elsewhere; the after/before trigger asymmetry is closed at the
   presence level.
```

Ship-time lockstep (from the thread): root `CLAUDE.md`/`GEMINI.md` (dogfood the imports here
too), `templates/CLAUDE.md`/`templates/GEMINI.md`, `docs/optional-ritual-hook.md`, `VERSION`,
`CHANGELOG`, `README` (version table), `UPGRADE.md` (row + rung above), docs site.

## Ship (same working session — maintainer approved)

**Pre-flight:** verified both import syntaxes against current vendor docs before touching
templates — Claude Code: `@path`, relative to the containing file, **max 4 hops** (the draft
said 5 — corrected), imports in code spans/blocks skipped
(code.claude.com/docs/en/memory.md); Gemini CLI: `@./path.md` idiomatic form, **`.md` files
only**, max depth 5, circular-import guard (geminicli.com/docs/reference/memport/). All four
import targets are `.md`, so both restrictions are satisfied; each vendor's file uses its own
documented idiom (`@path` for Claude, `@./path` for Gemini).

**Landed** (branch `feat/before-session-presence-v4290`):

- `templates/CLAUDE.md` + `templates/GEMINI.md` — import block + the presence-vs-attendance
  note + the "sessions/ stays an explicit read" caveat; prose pointer retained (belt and
  suspenders for non-import runtimes reading the same file).
- Root `CLAUDE.md` + `GEMINI.md` — same block, dogfooded (additive; existing text preserved).
- `docs/optional-ritual-hook.md` — retitled "Reinforcing the session rituals with hooks"
  (it now covers a before-session layer); new **Option A0** — Claude Code `SessionStart`
  injection for `memory/sessions/` recency, with the explicit why-opt-in rationale
  (`.claude/` is gitignored-personal; committed `settings.json` accretes personal
  allow-lists; Claude-only = Layer-2).
- `UPGRADE.md` — 4.29.0 table row + the `4.28.4 → 4.29.0` rung (as drafted above, plus the
  Gemini `@./`/`.md`-only detail).
- `VERSION` → 4.29.0; `CHANGELOG` (Added/Changed + Why-blockquote); `README` version table
  (row added, 4.24.0 trimmed to hold the 10-row cap). Site changelog auto-includes the root
  `CHANGELOG.md` (no site-side edit needed); docs deploy stays a separate maintainer step.
- `memory/` — thread `bp-before-session-presence` marked `[x]` shipped; `status` →
  v4.29.0.

**Not changed (scope held):** `AGENTS.md` (root + template — stays vendor-neutral, no import
syntax), `ENABLE.md` (templates are copied as-is; no protocol step change), examples (they
carry no bootstrap pointer files), skills/adapters (no description change → no re-sync).

## Memory References

- referenced: no-build-step-agent-run (the no-code constraint drove @-imports over an installed
  hook), upgrades-additive (shaped the rung's merge-never-overwrite steps)
- created: bp-before-session-presence
