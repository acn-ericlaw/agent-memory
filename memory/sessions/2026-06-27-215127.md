# Session (2026-06-27T21:51:27.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.25.0 (MINOR) — `MERGE.md` conflict-resolution protocol**, from a GitHub Copilot
review of the v4.24.0 conflict-marker check (relayed by the maintainer).

**Copilot's proposal:** don't let the LLM directly edit git conflict markers; instead
`conflict → structured memory objects → classify → AI proposes repair → memory-lint validates
→ human approves`.

**My assessment (gave both sides honestly):**
- *Right:* the principle enforces an existing invariant — **`never-pick-a-winner`** — and maps
  cleanly onto primitives already here (Contradiction thread, supersession, provenance,
  `memory-lint` as the deterministic floor). Same shape as the second-opinion/apply-critique
  human-gated loop.
- *Over-reach for this project:* "never edit the markers" is unachievable (someone deletes them;
  the real rule is *never silently pick a winner*); most `continuity.md` conflicts are **mechanical**
  (v4.24.0's union/take-later rules already cover them — no AI needed); and a convert/classify
  *engine* would cut against **`no-build-step-agent-run`** (no-code, agent-as-runtime).

**Chosen scope (maintainer): tiered markdown protocol** — adopt the principle in this project's
idiom, not a resolver engine.

**Done — new installed root doc `MERGE.md`** (read on demand, like `DECAY`/`REVIEW`/`SKILLS`):
- Loop: classify hunk → resolve by tier → `memory-lint` gate → human approves (never auto-commit).
- **Tier 1 (mechanical, deterministic):** additive → **union / keep-both**; scalar → **take-later**.
- **Tier 2 (semantic clash):** the AI **never** decides — preserve **both** sides + raise a
  `Contradiction` thread, or record an **unambiguous supersession** (else Contradiction); `[ ]`→`[x]`
  race keeps it checked; preserve `origin`/provenance.
- **Gate:** reuses the v4.24.0 **check 7** (no linter change) + the rest of the integrity checks.

**Wiring:** ENABLE Step 6 (install verbatim) + Step 8 verify list + Notes allowlist + Step 9 report
+ the Mode A advisory footprint; `.agent/schema.md` "Concurrency & merge-friendliness" points to it;
`UPGRADE.md` source-of-truth map + table row + `4.24.0→4.25.0` rung. `VERSION`→4.25.0.

**Dogfood:** bumped only the `status` **version token** (4.24.0→4.25.0) with **no changelog prose** —
exercising the v4.24.0 "status is not a changelog" rule live; the detail lives in this log + the Open
Thread + `UPGRADE.md`.

**Verification:** see the run after this log — both `memory-lint` test suites + a live lint.

**Not done (maintainer's call):** commit/push (v4.21→4.25 now stacked uncommitted); the **overdue
memory review**; README/CHANGELOG/deck.

## Memory References

- Created: `merge-conflict-protocol-v4250` (continuity Open Thread, completed)
- Referenced: `continuity-merge-friendly-v4240` (builds on it); vision-agent-memory (serves);
  `never-pick-a-winner`, `no-build-step-agent-run` (invariants the design honors)
