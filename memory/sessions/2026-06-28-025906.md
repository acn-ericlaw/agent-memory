# Session (2026-06-28T02:59:06.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.22.4 (PATCH)** — moved the safe-write safeguard into the **shared, committed layer**, on a
maintainer correction.

**The correction:** after the 2026-06-28 archive-truncation incident (the
`open(f,"w").write(open(f).read()+…)` antipattern wiped `memory/archive/2026-Q2.md` 50→6 during a
review), I had recorded the lesson only in my **personal `~/.claude/` memory** — which is per-machine and
does nothing for teammates or other vendors' agents. A safeguard that protects the **review ritual**
belongs in the **committed shared layer** — exactly the tool's own two-layer principle (personal prefs in
`~/`; shared project knowledge in the repo's `memory/` + protocol docs).

**Fix:** added two rules to **`REVIEW.md` → Safety** (installed verbatim into every enabled repo, so all
contributors + vendors inherit them):
1. **Never truncate a memory file when scripting the move** — append-mode (`>>` / `open(f,"a")`) for the
   archive + `INDEX.md`; read-into-a-variable-then-write for `continuity.md`. Never
   `open(f,"w").write(open(f).read()+…)` (opening `"w"` truncates *before* the read). Same caution for
   `sed -i`-style rewrites.
2. **After any scripted memory mutation, run `memory-lint`** — it catches truncation (count drops, links
   dangle), and git-tracked files recover via `git checkout HEAD -- <file>`.

Kept the personal memory too (broadened it to "any file write/append, hit twice"), but `REVIEW.md` is now
the authoritative **team-facing** safeguard.

**Lockstep:** `REVIEW.md`, `VERSION`→4.22.4, `CHANGELOG.md`, `README.md` (version table +4.22.4 / −4.19.0,
still 10), `UPGRADE.md` (table row + `4.22.3 → 4.22.4` rung). Doc-only — no shape/skill/hook change.

## Memory References
- Created: safe-write-review-safety-v4224
- Referenced: vision-agent-memory (serves)
