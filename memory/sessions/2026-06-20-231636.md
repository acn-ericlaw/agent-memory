# Session (2026-06-20T23:16:36.000Z)

**Agent:** Claude Code
**User:** Validate Copilot's v3.7.0→v4.14.0 upgrade of `mercury-composable`; fix the defect it surfaced in agent-memory.

## What We Did

**Validated** a GitHub Copilot (Gemini 3.1 Pro) Mode B upgrade of `~/sandbox/mercury-composable`
(v3.7.0 → v4.14.0, uncommitted working tree). Most of it was correct: `enabled_with: 2.0.0` + `mode: C`
preserved; `DECAY/REVIEW/SKILLS/.agent/schema.md` byte-identical to canonical; `vision.md` a proper
⚠️ DRAFT stub (not fabricated) + `(vision-bootstrap)` thread; continuity diff additive (+3/−0); all 3
built-ins present & `provenance`-marked with byte-faithful `memory-lint` scripts; `.gitignore` additive;
`docs/ADR.md` correctly **absent** (opt-in respected); `memory-lint` 0/0.

**One real defect:** the upgrade re-synced the target's `AGENTS.md` from the tool's **root** `AGENTS.md`
(the operator/dual-mode dispatcher — "AI-Enable Another Repository", references `ENABLE.md`) instead of
`templates/AGENTS.md`. The git diff confirmed it was correct before and got clobbered — a regression.
The target now presents as an enablement tool and references a non-installed `ENABLE.md`.

**Root cause = tool-side ambiguity (fixed here, v4.14.1 PATCH):** `UPGRADE.md`'s rung notation
"`AGENTS.md` (root + template)" + the standing "copy verbatim from the tool root / templates" guidance
never said, *per file*, which source a target gets — so an AI executing the upgrade grabbed the root
`AGENTS.md`. (`ENABLE.md` Step 6 was already explicit — fresh enable unaffected; only the upgrade path.)

**Fix shipped (v4.14.1):**
- `UPGRADE.md`: new **"Source of truth for re-synced files"** section — per-file map (target `AGENTS.md`
  ← `templates/AGENTS.md`; `DECAY/REVIEW/SKILLS` ← tool root; `schema`/bootstraps ← `templates/`), an
  explicit "never install the root `AGENTS.md` into a target" warning, and a grep self-check
  ("AI-Enable Another Repository" / `ENABLE.md` reference).
- `UPGRADE.md`: new `4.14.0 → 4.14.1` rung that **verifies + repairs** a mis-synced `AGENTS.md`.
- Lockstep: `VERSION`→4.14.1, UPGRADE capability row, `README` row, `CHANGELOG`. No installed-shape
  change (UPGRADE.md is operator-only).

**Guided the maintainer's re-test:** `git reset --hard` alone leaves the upgrade's *untracked* additions
(`SKILLS.md`, `agent-skills/`, `memory/vision.md`); recommended a `git clean -nd` dry-run then `git clean
-fd` for a clean 3.7.0 start (leave gitignored adapter dirs — regenerated). Then re-enable to re-test.

## Decisions Made

- The defect is a tool-doc ambiguity, not just a Copilot slip → fixed in agent-memory so it can't recur
  across vendors (the repo's guardrail pattern). PATCH, since UPGRADE.md is operator-only (precedent: 4.5.2).
- 4.14.1 rung doubles as a **self-heal**: repairs a mis-synced `AGENTS.md` in any already-upgraded target.

## Context for Next Session

mercury-composable will be reset + re-enabled by Copilot against the fixed tool (will land at 4.14.1) —
next round validates the fix end-to-end. agent-memory v4.14.1 committed locally; **not pushed**. Watch
that the re-test's `AGENTS.md` now matches `templates/AGENTS.md` and contains no `ENABLE.md` reference.

## Memory References
- Referenced: upgrades-additive, target-repo-scope-only, no-build-step-agent-run, adr-ledger-v4140
- Created: ot-agents-source-fix-v4141
