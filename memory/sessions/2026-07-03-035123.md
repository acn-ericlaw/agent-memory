# Session (2026-07-03T03:51:23.000Z)

**Agent:** Claude Code
**Lightweight:** single-fix session; no `continuity.md` facts touched.

## Summary

Actioned a field feedback report from a mercury-composable session
(`/tmp/agent-memory-post-commit-hook-feedback.md`): the `.githooks/post-commit`
auto-stub misfired on the recommended two-commit pattern (feature commit →
`chore(memory)` commit) and on long sessions, because it used the newest
session-log **filename timestamp** vs. a 30-min window as the proxy for "a log
exists for this session." When the write→commit gap exceeded the window, an
in-flight (uncommitted) log was deemed stale and a near-duplicate stub was created.

**Fix (v4.28.1):** added a primary guard ahead of the time-window check —
`git status --porcelain -- memory/sessions/` — that suppresses the stub whenever a
session log is staged, modified, or untracked. Directly observing the artifact
state beats the clock. The filename-window check remains the fallback for the
already-committed case. Bash 3.2-compatible; hook stays non-blocking.

Verified end-to-end after shipping: with an uncommitted stub in the tree, running
the hook against `HEAD` (a work commit with no session log) emitted the
enrich-and-commit nudge and created **no** second stub.

Note: the leftover stub this log replaces was itself correct behavior — the fix
commit was a genuine "work committed, no log in flight" case, so the fallback
safety net stubbed one (never silently gap the ledger). The fix prevents the
*duplicate* stub, not the *first*.

Commits: `9d918ab` (fix) — `.githooks/post-commit`, `UPGRADE.md`, `VERSION` (→ 4.28.1).

## Memory References
(none — no `continuity.md` facts referenced or created; a personal auto-memory
`post-commit-uncommitted-log-guard` was recorded outside this repo's ledger)
