# Session (2026-08-21T00:17:27.841Z)

**Agent:** Codex

## Summary

Implemented the approved v4.35.0 memory-protocol relocation through the full work-loop:
root and target `AGENTS.md` are byte-identical 61-byte shims; optimized tool and target
protocols live under `memory/PROTOCOL.md`; bootstrap, enable, upgrade, schema, enforcement,
documentation, examples, and release surfaces now own that boundary. Both protocol variants
remain under 11 KB and preserve the prior operational directive inventory.

Review repairs added pre-write collision classification, exact recovery and tamper checks,
restored several compressed continuity/lightweight/attribution details, and established an
instruction-versus-evidence trust boundary. The latter is now durable in
`docs/architecture/security.md` and AC-MP-13.

## Verification

- Exact shim bytes, link targets, protocol size/scope, bootstrap import order, inert template
  placeholders, release metadata, stale-current-reference searches, shell syntax, spec-status
  lint, and `git diff --check`: pass.
- Strict MkDocs build: pass.
- Python and Node memory-lint: 0 errors; two pre-existing stale-metadata advisories remain
  after this log advanced the session count.
- Python and Node memory-lint test suites: 50/50 pass in each runtime. Existing Python
  resource warnings remain non-failing.
- Manual tool/target, collision-stop, idempotence, and byte-recovery walkthroughs pass within
  the managed environment's read-only Git constraint.

## Open gate

The final adversarial pass correctly identified that AC-MP-13 was added after the original
human approval. The work-loop schedule hash rejected further transition, so the amended spec
and plan require explicit reapproval. A positive walkthrough with a committed target-local
pre-existing protocol also requires a user-run disposable Git fixture because this managed
session may not write Git indexes or refs. No prohibited Git mutation was attempted.

## Memory References

- Referenced: `vision-agent-memory`
- Referenced/reactivated: `protocol-pointer-enterprise-activation`
