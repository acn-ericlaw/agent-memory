# Session (2026-06-14T02:33:01.000Z)

**Agent:** Claude Code
**User:** P4 (minimal memory eval).

## What We Did

Shipped **P4 as v3.6.0** — a cheap, no-code answer to the "memory evaluation is
unsolved" gap (assessment gap #5). Added `memory/smoke-test.md`: a short set of
questions a *fresh* agent should be able to answer from the memory layer **alone**
(no source, no asking the user). Generic orientation questions (project & type, stack,
invariants, recent decisions + why, open threads, conventions, preferences,
supersessions) plus 2–4 project-specific questions seeded at enable. Each run marks
✅/❌ and logs a result row; a ❌ is a *memory* gap to fill, not a question to soften.

New `templates/memory/smoke-test.md`; `ENABLE.md` step 5f generates it (seeding the
project-specific questions from analysis), Step 8 verifies it, Step 9 reports it;
`.agent/schema.md` documents it; `REVIEW.md` notes a review is a natural time to run it.
Dogfooded: created this repo's own `memory/smoke-test.md` (12 questions; baseline row,
not yet run).

## Decisions Made

- The smoke test is a **standard installed file** (not optional), generated at enable —
  small, high-value, and it makes "can a newcomer orient from memory?" checkable.
- Manual scoring only — automated app-level memory eval is unsolved industry-wide; the
  value is the structured, repeatable question set, not a harness.
- MINOR → **3.6.0** (a new installed file); `UPGRADE.md` 3.5.0→3.6.0 rung.

## Files Changed

New `templates/memory/smoke-test.md` + this repo's `memory/smoke-test.md`; `ENABLE.md`
(step 5f + verify + report), `.agent/schema.md`, `REVIEW.md` (run-it note), `VERSION`→3.6.0,
`UPGRADE.md` (rung + tables), `README`/`CHANGELOG`, assessment (gap #5 + Evaluation
scorecard ⬜→✅). Dogfooded continuity (status, P4 checked off, Shipped-v3.6.0 record).

## Context for Next Session

v3.6.0 implemented, **not committed** — commit + push (fast-forward on `origin/main`).
**Four** assessment gaps now ✅ (#1 supersession, #2 invariant-verify, #3 contradiction,
#5 evaluation). Remaining backlog: **P5** (provenance/retrieval — partly inherent via
event-sourcing; assessment gaps #4 lexical-retrieval + #6 provenance) and the **"done"**
tier-state tidy-up. Validation debt persists: archive/sweep + invariant-verify cadences
unexercised on aged data, and the new smoke test hasn't been **run** yet (running this
repo's own would both validate P4 and likely surface any continuity gaps). Good moment to
re-run the industry-alignment assessment for a fresh scorecard.

## Memory References
- Referenced: backlog-memory-eval, no-code-markdown-only, upgrades-additive
- Created: memory-smoke-test-v360
- Reactivated: (none)
- Superseded: (none)
