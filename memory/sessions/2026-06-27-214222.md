# Session (2026-06-27T21:42:22.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.24.0 (MINOR) — `continuity.md` merge-friendliness**, from a maintainer
observation about concurrent teammate edits.

**The problem:** session logs never conflict (timestamped filenames), but `continuity.md`
is one file every teammate edits. Worst offender: the **`status` line had grown to ~20 KB on
a single line** (an accreted per-version changelog) — a single mutable shared line that two
people both rewrite = an unresolvable merge conflict. It also duplicated `UPGRADE.md`
(map-don't-duplicate violation) and pushed continuity to 753 lines (cap is 300).

**Scope chosen (maintainer):** slim status + conventions + lint guard (MINOR) — *not* the
MAJOR file-split, since the status anti-pattern was ~90% of the pain and a split would break
the single-file read contract every vendor bootstrap depends on.

**Done:**
1. **`status` re-spec'd as a SHORT current-state line, not a changelog** —
   `templates/.agent/schema.md` + both `AGENTS.md` (root + template) now say don't accrete
   per-version history; history lives in session logs / the `UPGRADE.md` ladder.
2. **New schema section "Concurrency & merge-friendliness"** — one fact per line; append-only
   sections are independent facts; **conflict = keep-both (union)** by default; scalar bumps
   (`last_session`/`last_review`/version token) **take the later/higher**; only same-thread
   edits need a human; left-behind conflict markers are a lint ERROR.
3. **`memory-lint` check 7** — leftover merge-conflict markers (`<<<<<<<`/`>>>>>>>`/diff3
   `|||||||`) in any `memory/*.md` → **ERROR**, both runtimes at parity. Deliberately does
   **not** flag a bare `=======` line (valid Markdown setext heading underline). 5 mirror tests
   each → `.py` + `.mjs` both 20/20.
4. **Dogfooded** — this repo's own status line slimmed **19,828 → 1,105 chars** via a
   read-then-write script (never truncate-first; the `version-md-stamp-safe-write` lesson).

**Lockstep:** `templates/.agent/schema.md`, `AGENTS.md` (root + template),
`agent-skills/memory-lint/` (scripts + tests + `SKILL.md`), `VERSION`→4.24.0, `UPGRADE.md`
(table row + a `4.23.0→4.24.0` rung). Fixed a table-row split I introduced mid-edit
(orphaned 4.23.0 tail) before moving on.

**Verification:** Python `unittest` 20/20; Node `--test` 20/20 (byte-parity). `memory-lint`
on this repo → next step below.

**Not done (maintainer's call):** commit/push; the **overdue memory review** — continuity is
still 753 lines because of accumulated `[x] Shipped` Open Threads (the review ritual's job to
archive; I did not hand-archive, to protect the decay math); README/CHANGELOG/deck.

## Memory References

- Created: `continuity-merge-friendly-v4240` (continuity Open Thread, completed)
- Referenced: vision-agent-memory (serves); `version-md-stamp-safe-write` (applied — read-then-write
  for the status rewrite); `no-build-step-agent-run`, `upgrades-additive` (respected)
