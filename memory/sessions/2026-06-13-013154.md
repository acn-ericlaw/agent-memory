# Session (2026-06-13T01:29:34.000Z - 2026-06-13T01:31:54.000Z)

**Agent:** Claude Code
**User:** Implement open thread 4: monorepo/multi-module guidance.

## What We Did

- **ENABLE.md Step 4** — expanded item 4 (project type) with per-ecosystem monorepo
  detection signals (JS/TS, Go, Rust, Java/Kotlin, Python). Added a "Monorepo handling"
  paragraph: enumerate modules, default to single root `memory/`, only recommend
  per-module silos if teams are truly independent (and log that as an Open Thread).
- **`templates/memory/instructions.md`** — added optional `## Module Inventory`
  section (table of module path / language / purpose) between Repository Structure
  and Conventions Observed; marked for removal on single-package repos.
- **`templates/.agent/schema.md`** — added `## Module Inventory` line to the
  instructions.md schema listing, with the monorepos-only note.
- **`mercury-composable/.agent/schema.md`** — synced the same schema change.

## Decisions Made

- Default is always single root `memory/` — shared conventions and cross-cutting
  context outweigh per-module isolation in typical monorepos.
- Per-module `memory/` is recommended as an Open Thread only, never auto-created.
- `## Module Inventory` is a table, not prose — easier for agents to parse and update.

## Context for Next Session

All four top-priority design threads are now closed. Remaining: real-world test
runs (Mode C with Cursor+Aider, Continue.dev), DISABLE.md, publish to GitHub.
