# Session (2026-06-13T01:27:13.000Z - 2026-06-13T01:29:34.000Z)

**Agent:** Claude Code
**User:** Implement open thread 2: git-aware migration (use `git mv` for tracked files).

## What We Did

Updated MIGRATE.md Section A (Archive originals) to be git-aware: check whether
the target repo is a git repo, then check each file's tracking status with
`git ls-files --error-unmatch`. Use `git mv` for tracked files (preserves rename
history); plain filesystem move for untracked files. Report distinguishes which
method was used per file.

## Decisions Made

- `git mv` is attempted only for tracked files — untracked files get a plain move,
  no noise about git for files git doesn't know about.
- If `legacy/<vendor>/` doesn't exist yet, create it before retrying `git mv`.
- Report records `git mv` vs plain move per file for auditability.

## Context for Next Session

All three high-value design threads from the mercury-composable test drive are now
closed. Remaining open threads: monorepo guidance, real-world test runs (Mode C
with Cursor+Aider, Continue.dev), DISABLE.md, publish to GitHub.
