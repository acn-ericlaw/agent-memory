# Session (2026-06-28T02:36:54.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.22.2 (PATCH)** — the **agent-side mirror** of v4.22.1. Maintainer accepted the follow-up
I flagged when shipping v4.22.1.

**Problem:** the per-commit-vs-per-session granularity issue had two halves. v4.22.1 fixed the **hook**
(auto-stub). The **agent-behavior** half remained: lightweight mode wrote a lite session log for *every*
memory-neutral tracked-file change, so an agent making several small commits in one sitting minted a
near-duplicate lite log per commit — same clutter + decay session-count inflation, on the agent side.

**Fix (`AGENTS.md` root + `templates/`):** the lightweight "lite log" tier now says — if a session log
already exists for *this* working session, a later **memory-neutral** commit should **enrich that existing
log** (a one-line "also: …" note) rather than spawn another lite log. The agent needs **no time-window**
(unlike the hook, which has no session context): it knows it's the same working session. The existing model
for **memory-relevant** work is explicitly preserved — distinct events each still get their own full log, so
a multi-task conversation can still yield several; the rule only stops *trivial* follow-on commits from
each minting a near-duplicate.

**Immediate dogfood of the distinction:** this v4.22.2 work is *memory-relevant* (a release + a new Open
Thread), so per the new rule it gets its **own** full log (this one) — not an enrichment of the v4.22.1
log. The rule coalesces only memory-neutral follow-ons; it correctly did **not** suppress this log.

**Lockstep:** `AGENTS.md` (root + template), `VERSION`→4.22.2, `CHANGELOG.md`, `README.md` (version table
+4.22.2 / −4.17.0, still 10), `UPGRADE.md` (table row + `4.22.1 → 4.22.2` rung). Wording-only — no shape,
skill, or script change.

Together v4.22.1 + v4.22.2 close the "session = burst of work, not a single commit" principle on **both**
the hook side and the agent side.

## Memory References
- Created: lite-log-per-session-v4222
- Referenced: post-commit-per-session-v4221 (the hook-side mirror this completes); vision-agent-memory (serves)
