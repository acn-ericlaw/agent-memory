# Session (2026-06-26T21:08:14.000Z)

**Agent:** Claude Code

## What happened

**Shipped v4.21.0 (MINOR)** — a **6th** vendor skills adapter, `.agents/skills/<name>/SKILL.md`, for
**Google Antigravity (`agy`)**.

**Trigger.** The maintainer ran **AntiGravity CLI (`agy`)** on an enabled repo. `init.sh` ran and
populated `.gemini/commands/`, but `agy` reported `/memory-lint` as **not found**. Question: is
Antigravity different from Gemini CLI / does it need different treatment?

**Root cause.** Yes — Antigravity is the **Gemini CLI successor**, not a rename. Gemini CLI read custom
commands from `.gemini/commands/*.toml`; Antigravity merged custom commands into the **open Agent Skills
standard** and reads workspace skills from **`.agents/skills/<name>/SKILL.md`** — it ignores
`.gemini/commands/` entirely. So our faithfully-generated Gemini TOML adapter was in a dir `agy` never
reads. Confirmed via migration guides + the maintainer pointing at `antigravity.google/docs/skills`
(the official page is JS-rendered so WebFetch couldn't read it; the open GitHub issue asking this is
still unanswered — worth a live `agy` re-confirm).

**Fix (full rung).** The Antigravity format **is** the Agent Skills `SKILL.md` we already emit for
Claude/Kiro/Copilot, so it's a one-line adapter addition per runtime, no new format.
- **Runtimes:** `.agents/skills/` write + orphan-prune in all three sync-adapters scripts
  (`.py`/`.mjs`/`.sh`), kept **byte-for-byte equivalent** — verified identical output across **all six**
  adapter dirs (5 skills → 30 adapters). Decision: **keep `.gemini/commands`** so Gemini CLI still works
  through the transition (additive, not a replacement).
- **Gitignore:** `.agents/` added to root + `templates/.gitignore` (regenerated, local-only).
- **Docs (five→six):** `SKILLS.md` (recipe block + trigger semantics), `ENABLE.md`, `AGENTS.md`,
  `templates/AGENTS.md`, `templates/.agent/schema.md`, `docs/optional-ritual-hook.md`, sync-adapters
  `SKILL.md`. Historical "five"-era references (past version-ladder rows, session logs) left immutable.
- **Vendor tables:** Antigravity added to README Mode C detection/migration + bootstrap tables (reads
  `AGENTS.md` + `.agents/skills/`).
- **Release:** `VERSION`→4.21.0, `CHANGELOG`, `UPGRADE.md` rung 4.20.3→4.21.0, README ladder row.

**Validation.** Three runtimes byte-identical (all 6 adapter dirs); `memory-lint: OK` (4 pre-existing
overdue warnings, unrelated). Staged tracked files only (no gitignored adapter dirs); committed +
pushed **`27dcfd3`** to `main` (repo convention — releases go straight to main). The post-commit hook
auto-stubbed this log.

## Memory References

- New: `antigravity-adapter-v4210` — Antigravity (`agy`) reads `.agents/skills/`, not
  `.gemini/commands/*.toml`; drove the 6th skills adapter (v4.21.0).
- Referenced: `copilot-adapter-v4170` (the 5th adapter; this continues the cross-vendor adapter arc),
  `sync-adapters-script-v4180` (the runnable script this extends), `ritual-init-v4200`
  (`init.sh` ran but populated the wrong dir for `agy`).
