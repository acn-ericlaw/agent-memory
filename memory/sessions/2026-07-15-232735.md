# Session (2026-07-15T23:27:35.000Z)

**Agent:** Claude Code

## Summary

**Shipped v4.30.0 (MINOR) — stack-aware `.gitignore` build-output seed.** Origin: a spawned
background task from today's `mercury` work (greenfield Mode A enable at v4.29.1, then a Rust
port began) — the installed `.gitignore` is deliberately AI-infrastructure-scoped, so when the
Rust toolchain landed *after* enable, the first `cargo build` polluted `git status` until the
user hand-added `target/`. Complaints = adoption signal.

**Weighed, not rubber-stamped.** Options considered: (a) do nothing — AI-infra purity, but the
enable advisory already promises add-only `.gitignore` edits and a dirty first build undercuts
faithful enablement; (b) a report-line reminder only — zero-risk but easily lost; (c) full
stack-detected seeding at enable — useless for greenfield, where no stack exists yet (the
actual failure). **Adopted a bounded split by when the stack is knowable:**

- **Brownfield (stack detected at enable):** `ENABLE.md` Step 7 gains a **"Stack-aware
  build-output seed"** — append only the missing canonical entries under a second,
  separately-scoped sentinel; add-only, de-duplicating against the whole file (no-op where
  entries exist, i.e. most brownfield repos). Six-row table (Rust/Node/Python/Java-Kotlin/
  .NET/Go-none); an explicit **non-goal** note pins it as a minimal seed, never a gitignore
  manager.
- **Greenfield (the mercury failure):** the fix is *timing*, not enable-time mechanics —
  Step 5b now seeds a `- [ ] Greenfield — no code yet` Open Thread carrying the "seed the
  stack's build-output ignores when the stack lands" action; the agent working in the repo at
  that moment is the runtime that applies the table. (Bonus: the greenfield thread itself was
  previously improvised by the enabling agent — mercury's `ot-greenfield-no-code` — and is now
  protocol.)
- Step 8 verify + Step 9 next-steps line wired.

**Lockstep:** `ENABLE.md` (Step 5b, Step 7 new sub-step, Step 8 bullet, Step 9 line),
`VERSION` → 4.30.0, `CHANGELOG` (What + Why), `README` (version row + 10-cap trim, dropped
4.26.0), `UPGRADE.md` (table row + `4.29.1 → 4.30.0` rung — optional additive target steps:
apply the seed if missing / offer the greenfield thread; version stamp). Templates deliberately
unchanged (AI-infra scoping is by design). No memory-shape/skill/adapter change. Docs site
changelog auto-includes.

Not done here: mercury itself needs no action (its `.gitignore` already has the Rust entries
and its greenfield thread is resolved); its `.agent/version.md` can bump to 4.30.0 on a future
Mode B pass.

> **also:** ran `refresh-metadata` (deterministic re-tier: `coauthor-stable-identity-v4280` +
> `pr-what-why-convention-v4270` → archive-candidate; 2 usage refreshes) clearing both
> `[stale-metadata]` warnings. Six `[overdue]` shipped threads (v4.20.0–v4.22.4 era) remain
> flagged — archive decisions belong to the next review ritual, not this session.

## Memory References

Created:
- `gitignore-stack-seed-v4300` (shipped thread)

Referenced:
- `vision-agent-memory` (serves), `upgrades-additive` (the seed + rung stay add-only),
  `never-pick-a-winner` (de-dup respects user entries), `no-build-step-agent-run` (agent
  applies the seed; the tool runs nothing)
