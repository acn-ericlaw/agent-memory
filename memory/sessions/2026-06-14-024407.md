# Session (2026-06-14T02:44:07.000Z)

**Agent:** Claude Code
**User:** Finish P5 (provenance + retrieval-at-scale) — the last backlog item.

## What We Did

Shipped **P5 as v3.7.0**, completing the vNext backlog. Two parts:

- **Provenance (gap #6 → closed).** Event-sourcing already records where each fact came
  from (its `Created` entry in a session's `## Memory References`); v3.7.0 *surfaces* it
  as an optional **`origin: <session-file>`** footer field — set at creation, repairable
  by review (earliest `Created` event). Provenance is now one hop, and it's the cheap
  defence against memory poisoning (every fact traces to an immutable session).
- **Retrieval-at-scale (gap #4 → ◐ by design).** `DECAY.md` §11 documents retrieval as
  deliberately **lexical + indexed**: grep `continuity.md` → grep `archive/INDEX.md` →
  follow a fact's `origin` → optional `sessions/INDEX.md`. Bounded by project scale;
  vector/semantic retrieval is intentionally out of scope (would break no-code).

## Decisions Made

- `origin` is **optional** and **repairable** (not a forced migration) — new facts get it
  going forward; a review can backfill older ones from the ledger. MINOR → **3.7.0**.
- Retrieval gap is marked **◐ by design**, not ✅ — we deliberately don't add semantic
  retrieval; we mitigate scale with provenance + indexes. Honest status.

## Files Changed

`DECAY.md` (§1 `origin` row + new §11), `REVIEW.md` (origin backfill note),
`.agent/schema.md` (footer field + retrieval note), `AGENTS.md` ×2 (set `origin`;
retrieval pointer), `ENABLE.md` (origin omitted at enable), the worked example (`origin`
on session-created facts + README note), `VERSION`→3.7.0, `UPGRADE.md` 3.6.0→3.7.0 rung +
tables, `README`/`CHANGELOG`, assessment (gap #6 ✅, gap #4 ◐ by-design, Retrieval
scorecard). Dogfooded: continuity (status, P5 checked off, Shipped-v3.7.0 record — the
first fact to carry an `origin` footer).

## Context for Next Session

**The entire vNext backlog (P1–P5) is now shipped** — the tool went 3.2.0 → 3.7.0 in this
run of sessions. Assessment status: gaps #1/#2/#3/#5 ✅, #6 ✅, #4 ◐ by-design — no open
High/Medium gaps remain. v3.7.0 is **not committed** — commit + push (fast-forward).
What's left is lower-priority polish + validation: the **"done" tier-state** tidy-up;
**running** the memory smoke test for real; exercising **archive/sweep + invariant-verify**
cadences on aged data; and **re-running the industry-alignment assessment** for a fresh
scorecard now that the backlog is clear. Also worth backfilling `origin` on the v3.3–3.6
shipped-record facts at the next review.

## Memory References
- Referenced: backlog-provenance-retrieval, no-code-markdown-only, upgrades-additive
- Created: provenance-retrieval-v370
- Reactivated: (none)
- Superseded: (none)
