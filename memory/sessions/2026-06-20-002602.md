# Session (2026-06-20T00:26:02Z)

**Agent:** Claude Code
**Type:** full

## What happened

Shipped **v4.13.0 (MINOR) — tool-provided (system) skills: provenance marker + upstream advisory.**

**Edge case (maintainer).** A target's AI had edited an agent-memory *built-in* (`memory-lint` in
`~/sandbox/simple-proxy`) without recognizing it as a tool-managed/system skill, so it didn't advise
upstreaming — the genuine fix nearly got stranded (built-ins are overwritten on upgrade). The existing
warn-before-overwrite only fires *during an upgrade*, which that session never ran. We agreed to mark
system skills so a target's AI recognizes one **at edit time** and routes the change correctly; upstream
pointer kept **generic** until the project's enterprise-GitHub move.

**Implemented.**
- **Marker:** added `provenance: agent-memory-builtin` to the frontmatter of the three shipped built-ins
  (`memory-lint`, `second-opinion`, `apply-critique`) + a one-line ⚠️ banner in each body. `hello-world`
  is a dogfood sample (never installed into targets) → deliberately not marked.
- **`SKILLS.md`:** new "Tool-provided (system) skills" section — before editing, check the frontmatter;
  if marked, don't edit in place: **fork** a local variant under a new name, or **upstream** a genuine
  fix to the agent-memory project (issue in production; maintainer advisory pre-release). Pointer kept
  generic, not a hard-coded URL.
- **`ENABLE.md` §5i:** install paragraph notes the marker; the "do-not-customize" note and the
  warn-before-overwrite both gain the upstream advisory (upgrade-time backstop).
- **`AGENTS.md`** (root + template): one-line pointer (substance stays in `SKILLS.md`, on-demand).
- **`.agent/schema.md`:** documents the optional `provenance` field.
- **`docs/DESIGN-skills-layer.md`:** status-header entry.

**Verification.**
- Adapter description extraction (`awk '/^description:/'`) excludes `provenance` for all three → adapters
  unaffected (they mirror only name + description; no regeneration needed).
- Frontmatter well-formed; `memory-lint` on this repo's memory: **0 errors, 0 warnings**.

**Lockstep (MINOR):** 3 built-in `SKILL.md`, `SKILLS.md`, `ENABLE.md` §5i, `AGENTS.md` (root + template),
`.agent/schema.md`, `docs/DESIGN-skills-layer.md`, `VERSION`→4.13.0, `CHANGELOG` 4.13.0 (Added/Changed),
`UPGRADE.md` 4.12.1→4.13.0 rung + table, `README` table, `continuity.md` status + new thread.
`DECAY.md` / `REVIEW.md` and the adapter recipe unchanged.

## Decisions Made

- **Frontmatter marker over a manifest.** A `provenance:` field in the committed `SKILL.md` is
  discoverable (the AI reads the file it's about to edit), self-documenting, and adapter-safe.
- **Edit-time advisory is the primary guard; upgrade-time warn is the backstop.** The simple-proxy case
  proved upgrade-time alone is insufficient (the session never upgraded).
- **Generic upstream pointer (no hard-coded URL).** The repo will move to enterprise GitHub; a stale URL
  is worse than "the agent-memory project / its maintainer." (Maintainer's explicit call.)
- **MINOR (4.13.0).** New additive convention + guidance; no memory-file shape change, adapters untouched.
- **Don't mark `hello-world`.** It's a dogfood sample, never installed into targets, so it isn't a
  system skill in any target.

## Memory References

- Created: ot-system-skill-provenance-v4130 (tier: active — shipped this session)
- Referenced: ot-memlint-dangling-crossfile-v4121, ot-enforce-sync-adapters-v4120, fresh-review-v4100,
  upgrades-additive, no-build-step-agent-run, backlog-real-work-dogfood, vision-agent-memory
