# Session (2026-08-13T22:58:59.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.33.1 (PATCH) — `[secret-material]`: ALL-CAPS enum constants are not credentials.**

**The trigger:** check 10's first field contact. The 2026-08-13 Mode B upgrades rolled v4.33.0
onto `Accenture/mercury` (scan clean, 175 sessions) and `Accenture/mercury-composable` (one
finding in 235 sessions) — and the one finding was a **false positive**: a 2026-07-09 session
log documenting Confluent's `bearer.auth.credentials.source` property, whose value there is
the enum constant `OAUTHBEARER` — a source *type*, not a credential. It was waived in place
during the upgrade and the class was queued for the detector; the maintainer directed the fix
be run immediately.

**The fix (detector, not waivers):** `_is_placeholder_value` / `is_placeholder_value` now treat
a value matching `^[A-Z][A-Z0-9_]{2,}$` as a config enum constant. Rationale: real credentials
carry mixed case/symbols; the famous uppercase-only shapes (e.g. AWS access-key ids) remain
covered by the value-shape patterns, which run independently of the assignment check. Mirror
test added to both suites (45 each, passing); runtime output parity re-verified byte-for-byte;
dogfood lint on this repo: 0 errors. The `4.33.0 → 4.33.1` rung is re-copy + stamp, with an
optional cleanup: a waiver added solely for this FP class can be dropped after upgrading.

**Housekeeping in the same sitting (pre-release):** removed the finished session worktree and
both merged local branches (`claude/…` from v4.32.1, `feat/…` from v4.33.0) at the maintainer's
direction; re-synced this checkout's adapters (stale since the v4.33.0 pull).

**Lockstep:** `VERSION` → 4.33.1, `CHANGELOG` (4.33.1 entry), `README` (row + 10-cap trim,
drops 4.28.2), `UPGRADE` (row + `4.33.0 → 4.33.1` rung). SKILL.md description unchanged →
adapters untouched; no docs-site change (the built-in-skills page describes check 10 without
enumerating value-exclusion internals).

## Memory References

- referenced: secret-redaction-lint-v4330 (the check this refines — its dogfood/rollout record
  is the evidence base for the FP class)
- created: secret-fp-enum-constants-v4331
