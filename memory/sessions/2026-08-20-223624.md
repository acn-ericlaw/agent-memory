# Session (2026-08-20T22:36:24.000Z)

**Agent:** Claude Code

## Summary

Investigated and shipped **v4.35.0 (MINOR) — target-state reconcile: enable/upgrade in
O(diff), not O(steps/rungs)**, from the maintainer's field report that a fresh Mode A
enable of a nearly-empty **AI-hackathon monorepo** took >10 minutes, with the direction
"compare the current state of a repo and the target state of the current agent-memory."

**Investigation (maintainer's diagnosis confirmed, grounded in numbers):** the protocol
was imperative and history-ordered while ~80% of its work is convergence to a declarative
target state — ENABLE.md had reached 989 lines prescribing ~60 files / ~600 KB one
read→fill→write round-trip at a time (most of it verbatim content that never needed to
enter model context); UPGRADE.md had reached 2,136 lines / 80 rungs, and Mode B walked
O(rungs-behind). Both costs grow with release velocity. The fix was latent in the design:
rungs were already required to be *idempotent* (reconciliation semantics executed one
version at a time), UPGRADE.md already carried a 4-row proto-manifest ("Source of truth
for re-synced files"), and the standing `ot-mode-b-automation-backlog` thread had already
drawn the boundary — script the mechanical parts, leave the merges to the agent. This
release realizes that thread and extends it to Mode A (recorded as a supersession).

**Shipped:**

- **`MANIFEST.md`** (tool-side, operator-only): the declarative target state — 40 rows
  (`Target | Source | Policy | Forge | Attrs`) under six policies (verbatim /
  verbatim-dir / seed-copy / sentinel-merge / seed-generate / stamp), row notes for the
  nuances (AGENTS.md source, sessions/ presence, .gitlab-ci.yml wiring), a **Semantic
  steps** table distilling the ladder's **14 non-mechanical migrations** (version-gated,
  each pointing at its rung), and a machine-format contract + tool-only complement.
- **`scripts/reconcile.py` + `.mjs`** (byte-parity twins; 25 mirror tests each; stdlib
  only, no subprocesses — forge and hooksPath read textually from `.git/config`).
  Dry-run by default (the consent artifact); `--apply` performs the mechanical policies
  in one pass and prints the judgment work-list (seed-generate files tagged with their
  ENABLE steps, hook activation, GitLab include wiring, renormalize note, semantic
  steps, the closing stamp); `--forge` override; `--check-manifest` release gate. Hard
  guarantees: never deletes; never touches an existing seed-copy/seed-generate file;
  never edits a pre-existing `.gitlab-ci.yml`; never writes `.agent/version.md` (uniform
  agent-stamps-last, rejected conditional auto-stamp); realpath escape guard; refuses
  the tool repo as target; exec bits preserved/repaired; caches excluded from dir syncs;
  Azure DevOps set installs only on positive detection.
- **`ENABLE.md`** restructured around a new **Reconcile Core** section (Mode A = consent
  → analysis → reconcile → generation; Mode B = reconcile → semantic steps → stamp;
  Mode C reconciles **only after** migration so `legacy/` preservation happens first);
  Steps 5–7 tagged `[reconcile-covered]` with all prose kept as spec + no-runtime
  fallback; Step 8 verify gains item 0 (dry-run reports done). **`UPGRADE.md`**: Mode B
  flow rewritten (ladder = per-version record + semantic detail; walking = fallback),
  release checklist gains manifest lockstep + `--check-manifest`, row + `4.34.2→4.35.0`
  rung (targets: version-stamp; accumulated managed-block drift applying is the feature,
  not a side effect). New `templates/memory/archive/INDEX.md` (seed-copy).
  `docs/DESIGN-reconcile.md` (cost anatomy, judgment boundary, rejected alternatives —
  per-version hashes, scripted user-CI wiring, auto-stamp, full automation — honest
  limits). Docs site: enable/upgrade guides + protocol-files reference.
- Lockstep: `VERSION`→4.35.0, `CHANGELOG`, `README` (row + 10-cap trim drops 4.32.0 —
  paragraph-glue checked this time — + repo tree + Mode-B flow line).

**Verified:** suites 25/25 both runtimes (16 at first build, +9 pinning the pre-ship
review's findings); dry-run and check-manifest output
byte-identical across runtimes; scratch end-to-end (fresh apply → converged; idempotent
re-run; drift re-copy; seed-copy preservation; seed-generate untouched; sentinel
add-only/dedup/order-preserving; forge filtering incl. unknown→GitHub+GitLab;
2.x-baseline detection; escape + self-target refusals); `memory-lint` 0 errors (the new
shipped thread briefly tripped `[continuity-bloat]` at 612 > 600 lines — resolved by the
in-session review below, back to 0 warnings). **Live
read-only probes:** the AI-hackathon repo (4.34.1) dry-run reproduced its pending rung
*exactly* (stale memory-lint built-in, waiver-drop semantic step, stamp) — the ladder's
prescription derived mechanically; mercury-composable (4.34.2) surfaced **11 managed
`.gitignore` entries no rung ever back-filled** (entries added to the template across
versions with no back-fill rung) — months invisible under the ladder, one `merge` line
under reconcile. Drift made visible is the safety half of the feature: the §5i
warn-before-clobber contract becomes *checked* on every run.

**Redaction note:** the motivating repo is referred to as "the AI-hackathon monorepo"
throughout committed files (maintainer direction — the working name is an organization
codename and stays out of this repo).

## Adversarial pre-ship review — findings & dispositions

2-lens review (release mechanics + claim sweep; reconcile-engine adversarial probing).
**Mechanics lens (returned first): FIX-FIRST, no blockers — all findings applied:**
(should-fix) the new shipped thread tripped `[continuity-bloat]` (612 > 600) and the log
claimed lint 0/0 → ran the review ritual in-session (below) + corrected the claim;
(should-fix) three stale "upgrades via the idempotent ladder" mechanism claims survived
(docs/guides/index.md card, docs/index.md, whitepaper) → reworded to
reconcile-against-target-state with the ladder as record; (should-fix)
`DESIGN-reconcile.md` was missing from the mkdocs nav → added; (should-fix) "install
manifest" named two files at once (`MANIFEST.md` vs `.agent/version.md`) → the three
docs-site captions for `.agent/version.md` now say "version stamp" (the installed file's
own self-title is untouched — it's installed content); (nit) DESIGN's "~48 rung walks" →
exact 44; (nit, pre-existing) ENABLE.md's stale built-in counts ("three tool-owned
skills"; Step 8 listing five) → fixed as a fold-in alongside this release's ENABLE edits.
Verified by the reviewer: all release numbers recounted true (40 rows / 14 semantic /
the then-16+16 tests (now 25+25 post-hardening) / 80 pre-release rungs, past-tense
framed / 989 pre-change ENABLE lines);
org-codename redaction clean ("AI-hackathon monorepo" everywhere); supersession
bookkeeping textbook; README 10-row trim clean (no glued prose); Python/Node outputs
byte-identical live; the mercury-composable 11-entry `.gitignore` drift probe reproduced
exactly.

**Engine lens (adversarial, every finding live-reproduced): FIX-FIRST — 2 blockers, both
fixed and pinned:** (blocker) a target **newer** than the tool checkout was silently
*downgraded* — the dry-run labeled newer content "drifted", apply reverted it to older
bytes, and the stamp item prescribed a version downgrade; the ENABLE.md Mode B stop rule
(`installed > current` → update the tool) is now mechanized as a hard error in both
runtimes. (blocker) mjs `safeTargetPath` used lexical `path.resolve` where py uses
`os.path.realpath` — a symlinked component inside the target let `--apply` write outside
it (live repro: files landed in an outside dir; a symlinked `DECAY.md` overwrote a victim
file, while py refused); mjs now resolves symlinks along the longest existing ancestor
(non-strict realpath), restoring parity, with symlink-escape tests in BOTH suites (py was
safe but unpinned). **Should-fixes, all applied:** py sentinel merges rewrote a CRLF user
file wholesale to LF (universal-newline reads — a whole-file mutation beyond add-only, and
a parity break) → `newline=""`, user bytes preserved, both runtimes identical; wrong-kind
target paths (a directory named `DECAY.md`) corrupted (py copied INTO it and "converged"
never, mjs raw EISDIR) → clean refusal; a non-UTF-8 sentinel target (py traceback; mjs
silently rewrote the user's line with U+FFFD) → clean refusal; `core.hooksPath` set to
another value (e.g. husky) was reported "currently unset" with an unconditional overwrite
instruction → now `currently: <value>` + arbitration wording, and trailing-slash
`.githooks/` counts as activated; `.git`-as-file (worktree/submodule pointer) got wrong
"init git first" advice → verify-by-hand guidance; `--target ~` was accepted (a scratch
$HOME took 80 files on apply) → refused (target-repo scope only). **Nits, applied:**
error-message path-resolution parity + `~`-expansion fixed in mjs; `N.x` stamps rejected
as malformed (the form is internal to the no-stamp 2.x baseline); the dry-run hint no
longer advises `--apply` when nothing is mechanical; `memory/sessions/` containing only
`.DS_Store` no longer counts as present; an undetermined forge is now called out in the
report notes. Suites 16 → **25 mirror tests per runtime**; parity re-verified including
the new error paths; both live probes re-run correct.

## Memory Review (2026-08-20, through 2026-08-20-223624)

- Trigger: size — `[continuity-bloat]` (612 lines > 600) after the v4.35.0 shipped
  thread landed; cadence not yet due (5 sessions since 2026-08-14).
- Applied events: `refresh-metadata` (deterministic) — 6 footers refreshed, uses-only
  (pre-commit-secret-guard-v4340, secret-fp-backtick-v4332, secret-fp-enum-constants-v4331,
  secret-redaction-lint-v4330, bp-greenfield, backlog-real-work-dogfood); no tier changes.
- Superseded: 1 — ot-mode-b-automation-backlog → bp-reconcile-target-state; archived
  promptly (truth-state) → memory/archive/2026-Q3.md + INDEX, flagged superseded.
- Archived (faded): 0 — no fact's recomputed sslu exceeds archive_window (20).
- Swept threads: 0 — oldest completed thread is 17 sessions old (< 20); none eligible.
- Archive-verify: pass — memory-lint 0 errors / 0 warnings after the move (600 lines,
  exactly at cap); no id in both places.
- Tier changes: 0.
- Invariants: not due (5 of 40 sessions since 2026-08-14); the standing
  `ot-reverify-invariants-20260814` human-gate thread remains open.
- Promoted core: 0.

## Memory References

- referenced: ot-mode-b-automation-backlog (realized + extended — superseded by this),
  backlog-real-work-dogfood (field-report flywheel), vision-agent-memory (adoption stays
  "point it at a repo"), no-build-step-agent-run (optional helper, agent-invoked),
  upgrades-additive (policy design honors the built-ins carve-out),
  never-pick-a-winner (drift → visible arbitration, never silent),
  bp-greenfield (related forward gap — greenfield speed is a step toward it)
- created: bp-reconcile-target-state
- Superseded: ot-mode-b-automation-backlog → bp-reconcile-target-state
