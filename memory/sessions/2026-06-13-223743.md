# Session (2026-06-13T22:37:43.000Z)

**Agent:** Claude Code
**User:** Implement all 5 recommendations from the agent-memory protocol field report (a real Node.js→Rust refactor of the enabled simple-proxy repo) as v3.2.0.

## What We Did

Acted on a field report produced by a *separate* Claude Code session that refactored
`~/sandbox/simple-proxy` from Node.js to Rust — itself a validation of the
self-contained-target design. Cross-checked every point against `DECAY.md`/`REVIEW.md`/
`ENABLE.md`/`AGENTS.md` before editing, and shipped all five fixes as **v3.2.0** (a
docs/protocol clarification release — no memory-file *shape* change):

1. **Session model** — defined a session as **one log-write** (a long conversation may
   produce several; the decay math counts log files), and made the title's `start`
   **best-effort** (`# Session (endZ)` persist time is the required stamp). Removes the
   fabricated-timestamp friction. (`AGENTS.md` ×2, `.agent/schema.md`.)
2. **Metadata ownership** — pinned it *and fixed a genuine contradiction* the report
   exposed: `ENABLE.md` Step 5b seeded new facts `tier: active` while `DECAY.md`/
   `AGENTS.md` said `working`. Now uniformly **born `working`**; agent seeds
   `id`/`created`/`tier`/`uses:1`, the review owns `uses`/`last_used`/`tier`.
   (`DECAY.md`, `.agent/schema.md`, `ENABLE.md`, `UPGRADE.md` 2.x rung.)
3. **Open-Thread archival** — surfaced the existing "leave `[x]` for the review to
   sweep; don't self-archive" rule to the agent. (`AGENTS.md` ×2, schema, continuity template.)
4. **Stack-fact altitude** — `continuity.md` → `## Stack & Tools` is now the canonical
   live home; `instructions.md` keeps a high-level descriptor and points there.
   (templates + schema + `ENABLE.md` 5a/5b.)
5. **Ritual reinforcement** — added an after-session checklist to both `AGENTS.md` and
   an optional, opt-in hook recipe (`docs/optional-ritual-hook.md`), keeping the core
   no-code.

Bumped `VERSION`→3.2.0; added the `UPGRADE.md` 3.1.0→3.2.0 rung + version-table rows
(UPGRADE/README); CHANGELOG 3.2.0 entry; archived the report at
`docs/assessments/2026-06-13-protocol-field-report.md`.

## Decisions Made

- All five recommendations were reasonable real-world feedback → implemented in full.
- Versioned as **3.2.0** (additive/clarifying, no shape change); already-enabled repos
  catch up via the new UPGRADE rung (re-sync DECAY/REVIEW/schema/AGENTS; don't rewrite
  existing fact metadata — the review reconciles tiers).
- Chose **born `working`** uniformly (matches the report's recommendation, DECAY §5,
  and what the review computes) over the inconsistent `active` seed.

## Context for Next Session

v3.2.0 is implemented but **not committed**. Files touched: `DECAY.md`, `AGENTS.md`,
`templates/AGENTS.md`, `templates/.agent/schema.md`, `ENABLE.md`, `UPGRADE.md`,
`VERSION`, `README.md`, `CHANGELOG.md`, `templates/memory/instructions.md`,
`templates/memory/continuity.md`, new `docs/optional-ritual-hook.md`, new
`docs/assessments/2026-06-13-protocol-field-report.md`, and this repo's `memory/`.
Note: `REVIEW.md` did **not** need changes. The vNext backlog (P1 supersession, etc.)
is unchanged. This session demonstrates the new log format (persist-time title, no
start). Remind the user to commit + push (fast-forward on `origin/main`).

## Memory References
- Referenced: backlog-real-work-dogfood, dogfood-target-repo-self-contained, dogfood-simple-proxy-enable, upgrades-additive, no-code-markdown-only
- Created: field-report-v320
- Reactivated: (none)
