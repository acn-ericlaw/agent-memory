# Session (2026-06-13T16:05:00.000Z - 2026-06-13T16:11:03.000Z)

**Agent:** Claude Code
**User:** Validate the v3.1.0 `.gitignore` propagation through a real Mode B version upgrade — "AI enable ~/sandbox/mercury-composable".

## What We Did

- Ran `ENABLE.md` against `~/sandbox/mercury-composable`. Detection → **Mode B
  (Already Ours)**, installed `3.0.0` < current `3.1.0` → in-place upgrade; ran the
  new `UPGRADE.md` 3.0.0→3.1.0 rung (user had pre-authorized the validation, so I
  proceeded without re-prompting yes/no, announcing the plan instead).
- The run surfaced **two genuine protocol gaps**, both fixed:
  1. **De-dup append.** mercury's pre-existing `.gitignore` already had `.kiro/`
     under an old `# AI infrastructure` heading but no sentinel. The original Step 7
     wording ("no sentinel → append the *full* block") would have duplicated `.kiro/`.
     Rewrote Step 7 **and** the UPGRADE rung to append only entries not already
     present anywhere in the file (de-duplicate; still add-only, never reorder).
  2. **`legacy/` contradiction.** mercury's `.gitignore` ignores `legacy/`
     ("optional to commit"), but `legacy/claude-code/CLAUDE.md` is actually tracked
     and the design treats `legacy/` as preserved/committed. Did **not** rewrite it
     (add-only); surfaced it as an Open Thread in mercury's `continuity.md` for the
     user to resolve (never pick a winner).
- Applied the corrected behavior to mercury: appended the sentinel-headed block with
  the 13 missing entries (omitted `.kiro/`); stamped `.agent/version.md` → 3.1.0
  (preserved `enabled_with: 2.0.0`, `mode: C`).

## Decisions Made

- Step 7 / UPGRADE-rung `.gitignore` logic is now **de-duplicating** in all cases —
  this corrects the still-uncommitted v3.1.0 feature in place (no extra version bump;
  it's a wording fix to an unreleased change).
- Cross-version `.gitignore` contradictions (like `legacy/`) are **surfaced as Open
  Threads in the target**, consistent with the "never pick a winner" invariant —
  upgrades stay add-only.

## Verification

mercury: `.kiro/` appears once (no dup), sentinel once, steering files + `memory/` +
`.agent/` NOT ignored, runtime dirs (`.claude/`, `.cursor/`, `.codeium/`, aider
history) ignored, all 14 template entries present → re-run is a no-op (idempotent),
no `/Users/` leak. `.claude/` was untracked (safe); `legacy/` tracked (the flagged
contradiction).

## Context for Next Session

v3.1.0 `.gitignore` propagation is validated on both paths now: fresh Mode A
(simple-proxy) and version-aware Mode B upgrade (mercury). Tool-repo changes from
this + the prior session are **uncommitted** — `ENABLE.md`, `UPGRADE.md`, `VERSION`,
`README.md`, `CHANGELOG.md`, `templates/.gitignore`, `memory/`. mercury's changes
(`.gitignore`, `.agent/version.md`, `continuity.md` Open Thread) are also uncommitted
in that separate repo. User's planned next step remains the simple-proxy refactor.

## Memory References
- Referenced: gitignore-propagation-v310, upgrades-additive, never-pick-a-winner, target-repo-scope-only
- Created: gitignore-v310-mercury-validation
- Reactivated: (none)
