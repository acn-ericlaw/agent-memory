# Session (2026-06-24T20:06:10.000Z)

**Agent:** Claude Code

**Lightweight:** validation-recording only. **v4.20.1 confirmed:** the maintainer re-cloned simple-proxy
fresh and ran Copilot CLI (Gemini) — it **self-inited**, running `.githooks/init.sh` as its first step
("explicit and straightforward") before summarizing, then gave positive bootstrap feedback. So a fresh
clone now self-initializes with **zero manual steps on both Claude Code and Copilot CLI** — the
untrained-user / fresh-clone adoption goal is met cross-vendor, closing the Copilot arc (v4.17.0 → 4.20.1).
Recorded the validation on `ritual-init-v4200`. No code change.

## Memory References

- Referenced (updated): `ritual-init-v4200` — recorded the fresh-clone Copilot self-init **validation**
  (resolves the prior "still prompt-adherence / untested" note)
