# Session (2026-06-13T02:22:00.000Z - 2026-06-13T04:36:05.000Z)

**Agent:** Claude Code
**User:** Set aside the SurrealDB knowledge-graph direction; tackle long-term memory design from a Claude.io insight doc (evolving memory: metadata, decay, tiers, review ritual, archive).

## What We Did

- Reviewed `~/Downloads/INSIGHT-evolving-memory.md` (the Claude.io sketch) against
  the repo's actual state and philosophy (no-code, markdown-only, cross-vendor).
- Surfaced three design risks in the sketch: (1) the floating-point `strength`
  function relies on LLM arithmetic and is non-deterministic across vendors;
  (2) per-session inline metadata editing is the most forgettable step and rots;
  (3) `continuity.md` isn't today's bloat source — completed `[x]` Open Threads
  and `sessions/` are.
- Settled two design forks with the user via structured questions:
  **deterministic integer rules** (drop the float strength) and **event-sourced**
  metadata (derived from session logs at review time, not hand-maintained).
- Wrote `DESIGN-evolving-memory.md` at repo root — a refined proposal. Key moves:
  stable kebab **fact IDs**, a `## Memory References` section in session logs as
  the event stream, tier decisions by counting session files (no formulas),
  `sessions/` reclassified as the **immutable event log**, and decay also
  sweeping completed Open Threads. Includes a 10-step implementation plan and 5
  open questions.

## Decisions Made

- SurrealDB knowledge-graph exploration is **set aside** (not deleted) in favor of
  the markdown-native evolving-memory layer.
- Scoring is deterministic integer rules; no floating-point `strength`.
- Usage metadata is event-sourced from session logs; `sessions/` is immutable and
  is never archived or rewritten (it's the projection's source of truth).
- This session produces the design doc only — no protocol/template/schema changes
  until the design is approved.

## Update — §11 resolved + versioning added

User reviewed the design and answered the open questions:
- **Fact IDs: yes.** **Upgrade: ENABLE.md Mode B (accepted).** **Auto-core: off
  (human-set only).** Default windows accepted.
- User added a requirement: **tool versioning** so a repo enabled by an older
  version can be upgraded in place.

Folded versioning into `DESIGN-evolving-memory.md` as §12: a root `VERSION`
(semver; 1.0.0 enable → 2.0.0 migration → 3.0.0 evolving memory, backward-compat),
a per-repo `.agent/version.md` install stamp (separate from verbatim schema.md),
and `UPGRADE.md` as the version ladder reached only via Mode B (the MIGRATE.md ←
Mode C pattern). Defined the version-aware Mode B flow and the 2.x→3.0.0 rung.
§11 is now "Resolved Decisions"; §10 plan updated with versioning steps.

One sub-decision left for the user: `UPGRADE.md` as a separate ladder doc (my
recommendation, forward-looking) vs. inlining the single current rung in ENABLE.md.

## Update — full implementation built (v3.0.0)

User chose separate `UPGRADE.md` + "build it all". Executed the entire §10 plan:

- **New root docs:** `VERSION` (3.0.0), `DECAY.md` (rules), `REVIEW.md` (ritual),
  `UPGRADE.md` (version ladder, operator-only).
- **New templates:** `templates/memory/decay-policy.md`, `templates/.agent/version.md`.
- **Edited templates:** `templates/memory/continuity.md` (Architectural Invariants +
  `last_review` + metadata note), `templates/.agent/schema.md` (metadata fields,
  `## Memory References`, new-file sections), `templates/AGENTS.md` (Before/During/After).
- **Protocol wiring:** `ENABLE.md` (version-aware Mode B → UPGRADE.md; Step 5e creates
  decay-policy/archive/version.md; Step 6 installs DECAY.md+REVIEW.md at target root;
  Step 8 verifies them; report + Notes updated), root `AGENTS.md` (same Before/During/
  After), root `CLAUDE.md` (architecture + lockstep note), `README.md` (Evolving Memory
  + Versioning sections, version table, repo-structure + modes updated).
- **Worked example:** `examples/evolving-memory-example/` (continuity before/after,
  session log w/ Memory References + review summary, archive/2026-Q2.md + INDEX.md,
  decay-policy.md, README).
- **Dogfood:** this repo now has `memory/decay-policy.md`, `memory/archive/INDEX.md`,
  `## Architectural Invariants` (5 core facts), `last_review`. Legacy facts grandfathered.

**Key design refinement (flagged):** DECAY.md/REVIEW.md install into every enabled
repo's root (the ritual runs in-repo), unlike MIGRATE.md/UPGRADE.md which are
operator-only. Verified: no leaked `/Users/` paths in content, no stray placeholders.

## Context for Next Session

v3.0.0 layer is built and dogfooded. Open: validate the protocol docs read
unambiguously; optionally backfill ids/metadata on this repo's legacy continuity
facts (or let the first review do it); run a real review once ~10 post-adoption
sessions accrue. Not committed yet — remind user to `git add` (incl. new root files,
templates, examples) and commit.

## Test drive — Mode B version-aware upgrade of ~/sandbox/mercury-composable

First real exercise of the v3.0.0 versioning path. Detection: memory/ + .agent/
present (OURS), **no `.agent/version.md`** → 2.x baseline; tool VERSION 3.0.0 →
`installed < current` → in-place upgrade. User chose "Proceed". Ran the UPGRADE.md
2.x→3.0.0 rung inside the target only:
- Installed `DECAY.md`, `REVIEW.md` (verbatim) at target root; re-synced
  `.agent/schema.md` + `AGENTS.md` from updated templates (diff confirmed target
  AGENTS.md was a clean v2 template copy — even modernized its session-log format).
- Created `memory/decay-policy.md`, `memory/archive/INDEX.md`, `.agent/version.md`
  (3.0.0 / enabled_with 2.0.0 / mode C).
- Backfilled ids + metadata footers on all continuity facts (uses:1, tier:active);
  added `## Architectural Invariants` (promoted 2 hard constraints from Key Decisions
  → tier:core: functions-decoupled-routes, typed-io-map-or-pojo); added `last_review`
  + an Upgrade History block.
- Untouched: source, `pom.xml`, `legacy/`, the 2 session logs (immutable), and the
  unchanged-in-3.0.0 bootstraps (CLAUDE/GEMINI/.cursorrules/.windsurfrules).
Verified: 14/14 files present, no unfilled placeholders, no `/Users/` leaks.
Idempotency confirmed: re-detect now reads installed==current → "up to date".
Target not committed (separate repo; offered to user).

**Validation outcome:** version-aware Mode B + UPGRADE.md work end-to-end on a real
pre-versioning repo. No protocol ambiguity hit during execution.

### Second test drive — ~/sandbox/rust/rust_event_bus_example (event_bus_demo)

Originally a **Mode A** fresh enable (2026-06-12), pre-versioning, **and not a git
repo** (no `.git`). Resolved a path typo (user wrote `rust_event_bus-example`;
actual is `..._example`). Flagged the no-git reversibility risk and ran the upgrade
**additive-only** (per user choice): added v3 files + metadata footers + an
Architectural Invariants section seeded from instructions.md (route-name-decoupling,
demo-not-framework, tier:core) **without moving anything out of Key Decisions**
(contrast with mercury, where 2 invariants were promoted). Stamped mode:A.
Verified 14/14 files, no placeholder/path leaks, source (Cargo/src) untouched,
idempotent. Confirms the upgrade path works for Mode A origins and non-git repos.
Two upgrade *styles* now exercised: promote-into-invariants (mercury) and
strictly-additive (rust).

### Repo housekeeping — tool repo made fully vendor-neutral-enabled

- Added root `.gitignore`: ignores vendor AI *runtime* dirs (`.kiro/`, `.claude/`,
  `.cursor/`, `.continue/`, `.windsurf/`, `.codeium/`, `.gemini/`, `.cline/`,
  `.roo/`, `.zed/`, `.codex/`) + aider local history + `.DS_Store`. Deliberately does
  NOT ignore steering files the tool ships (CLAUDE.md/GEMINI.md/AGENTS.md/.cursorrules/
  .windsurfrules/copilot) — verified via `git check-ignore`. Nothing was tracked, so
  no untracking needed.
- Added the three missing root bootstraps: `.cursorrules`, `.windsurfrules`,
  `.github/copilot-instructions.md` (dual-mode dispatcher voice, like the tool's own
  CLAUDE.md/GEMINI.md). Tool repo now has all six vendor bootstraps at root — which
  also makes the CLAUDE.md architecture + README repo-structure claims accurate.

## Memory References
- Referenced: target-repo-scope-only, never-delete-vendor-files, never-pick-a-winner, no-code-markdown-only, upgrades-additive
- Created: target-repo-scope-only, never-delete-vendor-files, never-pick-a-winner, no-code-markdown-only, upgrades-additive (all tier: core, Architectural Invariants)
- Reactivated: (none)

## Memory References
- Referenced: (n/a — evolving-memory layer not yet active in this repo)
- Created: (n/a — DESIGN doc, no continuity facts added)
