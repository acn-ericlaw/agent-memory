# Session (2026-06-14T02:26:19.000Z)

**Agent:** Claude Code
**User:** P3 (write-time contradiction check).

## What We Did

Shipped **P3 as v3.5.0**, closing the assessment's last open gap (#3). The
migration-time "surface contradictions, never pick a winner" rule (which only ran
during `MIGRATE.md`) now applies to **normal sessions**: when a fact is added or
materially rewritten, the agent first scans the facts it might conflict with and acts
on any contradiction instead of silently keeping both.

`DECAY.md` §10 defines the rule: a clear replacement → **supersede** (§9, the P1 path);
a genuine conflict → a `Contradiction:` Open Thread (never pick a winner); a clash with
a `core` invariant → stop and surface (don't let a new fact silently override an
invariant — that must be a deliberate, human-confirmed supersession). `REVIEW.md` gains
a **contradiction backstop** (the review already reads every fact, so it scans for
conflicts), and the after-session step in both `AGENTS.md` carries the before-adding
check.

This completes the truth-maintenance trio and reuses existing primitives end-to-end:
**P3** catches a conflict at write time → **P1** resolves it by supersession (or P3
raises a thread) → **P2** periodically re-checks the never-decay facts.

## Decisions Made

- P3 is a behavioral rule + a review backstop — **no new metadata, fields, or knobs**,
  so MINOR → **3.5.0** with a docs-resync `UPGRADE.md` rung.
- Resolution prefers **supersession** (clean replacement) and falls back to an Open
  Thread only for genuine unresolved conflict — consistent with `never-pick-a-winner`.
- Reused the existing rest-versioning supersession in the example as the
  contradiction-resolved-by-supersession demonstration (README note), rather than
  fabricating a second scenario.

## Files Changed

`DECAY.md` (new §10), `REVIEW.md` (contradiction backstop), `AGENTS.md` ×2 (before-adding
check), `VERSION`→3.5.0, `UPGRADE.md` (3.4.0→3.5.0 rung + tables), `README`/`CHANGELOG`,
assessment (gap #3 ⬜→✅, Truth-maintenance scorecard ◐→✅), example README note.
Dogfooded: tool continuity (status, P3 checked off, Shipped-v3.5.0 record).

## Context for Next Session

v3.5.0 implemented, **not committed** — commit + push (fast-forward on `origin/main`).
All three "High/Medium-High/Medium" assessment gaps (#1 supersession, #2 invariant-
verify, #3 contradiction) are now ✅. Remaining backlog: **P4** (memory eval), **P5**
(provenance/retrieval — partly inherent via event-sourcing), the **"done" tier-state**
tidy-up, and validating the **archive/sweep + invariant-verify** cadences live (need
aged data or a tight-window throwaway repo). Worth re-running the industry-alignment
assessment soon to re-measure now that gaps #1–#3 are closed.

## Memory References
- Referenced: backlog-contradiction-check, never-pick-a-winner, supersession-v330, invariant-verify-v340, no-code-markdown-only
- Created: contradiction-check-v350
- Reactivated: (none)
- Superseded: (none)
