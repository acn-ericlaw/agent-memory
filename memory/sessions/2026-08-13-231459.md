# Session (2026-08-13T23:14:59.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.33.2 (PATCH) — `[secret-material]`: backtick is a value delimiter.**

**The trigger — the rung's own verify step, minutes after v4.33.1 released:** while executing
the `4.33.0 → 4.33.1` bump on `mercury-composable`, dropping the FP waiver and re-running lint
per rung step 2, the same line flagged again. Root cause: the v4.33.1 enum-constant exclusion
was verified against the *bare* assignment form, but the real field line is **markdown inline
code** — in a backticked span the closing backtick rode into the captured value (the regex's
value class excluded quotes and whitespace but not backticks), so the fullmatch ALL-CAPS rule
never matched. The 4.33.1 fixture simply didn't use the real-world form.

**The fix:** backtick joins the quote/value delimiter set in the credential-assignment pattern
(both runtimes; a backticked span now captures the bare value). A backticked *mixed-case*
literal still flags — negative control added. The mirror enum test now uses the **exact field
line form** plus that control (45 tests each, passing). Verified three ways: both suites, tool
dogfood (0 errors, byte-parity), and **against the live target** — zero `[secret-material]`
findings on mercury-composable with no waiver present.

**Lesson recorded:** when a fix is motivated by a specific field line, the regression fixture
must quote that line *verbatim* — the paraphrased (bare) form validated a fix the real
(markdown) form defeated. The rung-verify-against-live-target step is what caught it, twice in
one day the trigger layer earned its keep.

**Lockstep:** `VERSION` → 4.33.2, `CHANGELOG`, `README` (row + 10-cap trim, drops 4.28.3),
`UPGRADE` (row + `4.33.1 → 4.33.2` rung — re-copy, drop the FP waiver for real this time,
stamp). SKILL.md description unchanged → adapters untouched; no docs-site change. Target bumps
to 4.33.2 (mercury, mercury-composable) follow once this merges.

## Memory References

- referenced: secret-fp-enum-constants-v4331 (the fix this completes — its rung's verify step
  surfaced the residual), secret-redaction-lint-v4330 (the check under refinement)
- created: secret-fp-backtick-v4332
