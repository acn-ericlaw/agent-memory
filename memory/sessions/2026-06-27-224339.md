# Session (2026-06-27T22:43:39.000Z)

**Agent:** Claude Code

## Summary

**apply-critique** cycle on the v4.22.0 milestone. A fresh-context reviewer (clean memory,
different vendor) returned a critique via `review-scratch/critique-2026-06-27-223653.md`. Parsed,
planned a bounded set of fixes, applied, gated, recorded. The four fixes fold into the **unreleased**
v4.22.0 — **no version bump** (the release isn't pushed; per the one-version-per-release policy, fixes
to a pending version stay in it).

**Applied (3 of 4 actionable + 1 validated):**
1. **Version consolidation** — reviewer agreed it's sound (id slugs are opaque handles; relabeled
   threads are fine). **No change.**
2. **`MERGE.md` Tier 2 — removed AI discretion on supersession.** The old "one side genuinely
   supersedes the other" let the AI declare a winner = a disguised winner-pick. Now: a semantic clash is
   **always** keep-both + Contradiction **unless the human explicitly instructs** a supersession. The
   `[ ]`→`[x]` race is reframed as a mechanical transition (not a substance clash). Strengthens
   `never-pick-a-winner`.
3. **`memory-lint` check 7 — rescoped** from `memory/**/*.md` to **live top-level `memory/*.md`**
   (continuity/instructions/vision/decay-policy/smoke-test); `sessions/` + `archive/` **excluded**. They
   legitimately *quote* conflict markers (a session log pasting a diff/terminal block), so scanning them
   would false-positive. Latent bug — it didn't fire here only because this session's markers sit inline
   in backticks, not at line-start. Fixed in both runtimes (Python glob non-recursive; Node top-level
   readdir) + 2 new tests each (sessions/ and archive/ markers ignored) → **22/22 at parity**.
4a. **`REVIEW.md` step 9 — codified the review-block gotcha**: don't list archived ids under
   `## Memory References` (re-arms the over-archival guard → false reactivation); they go in the
   `## Memory Review` block. Prevents the next agent repeating the mistake I hit live.
4b. **`.githooks/README.md` — hook-fatigue note**: a feat/memory commit split trips the advisory on the
   code commit (expected/benign); prefer an atomic commit to avoid it.

**Rejected/none:** item 1 (no change needed). The reviewer's false-negative note (a botched resolution
leaving a lone `=======` turns the prior line into an H1) is acknowledged — skipping `=======` remains
the correct trade-off; no action.

**Deterministic gate (the point of apply-critique):** Python `unittest` 22/22; Node `--test` 22/22;
full output byte-identical; `memory-lint` on this repo 0 errors / 0 warnings. All fixes are doc/skill
changes; the skill change (check 7) is covered by the test suites.

**Files touched:** `MERGE.md`, `agent-skills/memory-lint/scripts/{memory-lint.py,.mjs,test_*.py,test_*.mjs}`,
`agent-skills/memory-lint/SKILL.md`, `REVIEW.md`, `.githooks/README.md`.

**Not done (maintainer's call):** these fixes are committed-pending — they should join the v4.22.0 commits
before pushing (amend/extend the feat commit, or add a follow-up commit; still v4.22.0). README/CHANGELOG.

## Cross-vendor validation (loop closed)

After the fixes were applied, the maintainer carried them back to the **same fresh-context reviewer
(Gemini 3.1 Pro, different vendor, clean memory)** for a re-review. Gemini confirmed all four fixes
correctly address the critique — Tier-2 discretion removed, check-7 rescoped (with parity tests),
REVIEW.md gotcha codified, hook-fatigue documented — with **no new issues**, and judged v4.22.0 ready
to commit + push. It also correctly applied **lightweight mode** (its session was read-only → no
session log). This closes the `second-opinion` → `apply-critique` loop **end-to-end across vendors**:
Claude built + applied; Gemini critiqued + re-validated. The fresh reviewer caught a latent bug (the
check-7 false-positive) the in-session author would have shipped.

## Memory References
- Created: apply-critique-v4220-fixes
- Referenced: merge-conflict-protocol-v4250 (MERGE.md Tier 2 tightened), continuity-merge-friendly-v4240 (check 7 rescoped), never-pick-a-winner (the invariant the MERGE.md fix reinforces)
