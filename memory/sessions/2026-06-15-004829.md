# Session (2026-06-15T00:48:29.000Z)

**Agent:** Claude Code
**User:** Why are the AI bootstrap files still slightly different from each other?

## What We Did

A follow-on to the `CLAUDE.md` vendor-neutrality fix. Diagnosed two causes of the
non-uniformity: (1) my earlier `CLAUDE.md` rewrite over-enriched it (a mode-branch + a
`vision/instructions/continuity/sessions` read-list) versus its terser siblings; (2) older
drift — the read-order (`1. instructions 2. continuity 3. sessions`) was duplicated across
~10 pointer files (root + templates), which is exactly why it never picked up `vision.md`
for v4.0.0.

Chose **minimal + DRY**: standardized all 10 pointers to one parallel form — project
one-liner + "read `AGENTS.md` first (the hub: modes + read order)" + identify-as-vendor —
and made **`AGENTS.md` the single source of the read-order**. Pointers no longer list
files. They differ only by vendor name, comment syntax (`.md` vs plain dotfiles), and the
`AGENTS.md` path (copilot uses `../`). Updated `.agent/schema.md`'s Bootstrap-Files section.

## Decisions Made

- Read-order lives **only in `AGENTS.md`**; pointers route there and never duplicate it —
  a read-list change now touches one file, not ten (this retroactively fixed the v4.0.0
  `vision.md` miss in the template pointers).
- Kept the documented two-tier nuance: `CLAUDE.md`/`GEMINI.md` carry the
  `{{PROJECT_ONELINE}}` header (eager-load context); the dotfiles stay plain. Within each
  tier they are now perfectly parallel.
- Root pointers note the dual mode (the tool itself); template pointers don't (targets are
  single-mode) — an intentional, not accidental, difference.
- **No version bump** — pure doc-consistency; `VERSION` stays 4.0.0. Existing enabled
  repos' pointers still work (AGENTS.md is authoritative); new enables get the leaner form.

## Context for Next Session

Bootstrap pointers are uniform + DRY. Not committed yet — commit + push (fast-forward).
Natural next step still: **upgrade simple-proxy + mercury to 4.0.0** to exercise the
Vision-bootstrap rung end-to-end. Remaining Blueprint gaps: `bp-greenfield`, `bp-multi-user`.

## Memory References
- Referenced: vbdi-shipped-v400, no-code-markdown-only
- Created: (none — doc-consistency refactor; no new tracked facts)
- Reactivated: (none)
- Superseded: (none)
