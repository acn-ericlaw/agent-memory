# Session (2026-06-16T20:15:31.000Z)

**Agent:** Claude Code

## Summary

Ran the memory review ritual on this repo's own `memory/`, triggered by cadence (11 session
logs since `last_review`, > `review_every` 10) and size (`continuity.md` was 529 lines >
`continuity_max_lines` 300). Full-rebuild recompute of `uses`/`last_used` from all 35
session logs' `## Memory References`; re-tiered every fact per `DECAY.md` §5; archived the
superseded invariant + 5 faded threads; stamped `last_review`. Invariant verification was
**not due** (11 < `verify_invariants_every` 20) and was skipped; `last_invariant_check` left
unchanged at `2026-06-15 | through 2026-06-15-231502`.

## What happened

- **Full rebuild.** Recomputed `uses` = distinct session logs naming each id, `last_used` =
  latest such session, scanning every `memory/sessions/*.md` `## Memory References` (excluding
  this review's own log). Notable bumps: `no-code-markdown-only` 15→18, `upgrades-additive`
  12→15, `never-pick-a-winner` 4→7, `skills-layer-v410` 1→6, `sync-adapters-v420` 1→5,
  `bp-multi-user` 1→3. The `backlog-*` facts gained the `2026-06-13-024537.md` Created event
  (that log carries two `## Memory References` blocks — both now counted).

- **Re-tier (DECAY §5, first match wins).** 8 skills-layer ship facts went **working→active**
  (re-referenced after creation); `lightweight-skills-v440` stays **working** (uses 1,
  created this window). Several v3.x/VBDI facts aged **active→archive-candidate**
  (memory-smoke-test-v360, provenance-retrieval-v370, vbdi-shipped-v400, bp-vbdi-loop,
  bp-altitude-drift, bp-skills-layer, dogfood-target-repo-self-contained, backlog-memory-eval,
  backlog-provenance-retrieval, gitignore-propagation-v310). Unchecked Open Threads pinned
  active (ot-realworld-v411-findings, bp-greenfield, bp-multi-user, bp-sdlc-overlay,
  vbdi-lifecycle-direction, backlog-real-work-dogfood — the last at sslu 25 but pinned because
  `- [ ]` incomplete).

- **Archived — superseded (1).** `no-code-markdown-only` → moved to `archive/2026-Q2.md`
  flagged "superseded" (superseded-by `no-build-step-agent-run`), links preserved, promptly
  per §9. Removed from the Architectural Invariants section.

- **Archived — faded (5).** `sessions_since_last_used > archive_window` (20):
  `gitignore-v310-mercury-validation` (26), `field-report-v320` (25),
  `dogfood-simple-proxy-enable` (25), `backlog-supersession` (22),
  `backlog-invariant-verify-cadence` (21). These are the early v3.1–v3.4 completed `[x]`
  threads — the lean-up step. Moved to `archive/2026-Q2.md` + INDEX lines added.

- **Boundary cases kept (not archived).** `supersession-v330`, `invariant-verify-v340`,
  `contradiction-check-v350`, `backlog-contradiction-check` all sit at sslu **20 = archive_window**
  (rule 8 needs **>** 20) → stay **archive-candidate**; they sweep next review if untouched.

- **Stamp.** `last_review` → `2026-06-16 | through 2026-06-16-201531`. `continuity.md`
  529 → 489 lines.

## Notes / open items

- **Ledger-vs-footer drift (surfaced, not fixed).** `ot-reverify-invariants-20260615` had
  footer `uses: 1` but the rebuild yields **0**: it was named only in session *prose* and a
  `**Closed:**` line, never in any `## Memory References` ledger (its origin session
  `2026-06-15-231502.md` recorded `bp-skills-layer` Created but not this thread). Set the
  footer to the honest `uses: 0`; kept the fact (recently-completed `[x]`, not sweep-eligible).
  Same root cause for `ot-drift-blueprint-baseline-20260615` (named under a `**Resolved:**`
  line in `2026-06-16-001342.md` → counts as uses 1). Pattern worth noting: the previous
  review raised both threads inside its `## Memory Review` block without a matching `Created:`
  ledger entry, so a strict rebuild can't see them. Not a correctness bug in this ritual —
  just a provenance gap in how those two threads were first logged.

- **Drift backstop → new Open Thread.** Raised `ot-drift-blueprint-baseline-v440`: the
  Blueprint subsection header still reads "Current State (**v4.1.1**)" while Project State
  `status` is now **v4.4.0** (advanced through v4.2.0 / v4.3.x / v4.4.0 — all skills-layer
  work). Same class of drift the prior review fixed (v3.7.0→v4.1.1). Surfaced, not auto-fixed.

- **`vision-agent-memory` footer recomputed (completed by the reviewer).** It lives in
  `memory/vision.md` (tier core, the Vision); the rebuild shows **uses 15 / last_used
  2026-06-16** (independently re-confirmed), but the subagent's edits were scoped to
  `continuity.md` + `archive/` + this log, so it flagged the stale footer (`uses: 1`,
  `last_used: 2026-06-14`). The reviewer then updated `memory/vision.md`'s footer to
  `uses: 15 | last_used: 2026-06-16` — so the review's usage recompute now covers the Vision too.

- No smoke test run (per instructions). No commit made (per instructions).

## Memory Review (2026-06-16, through 2026-06-16-201531)
- Reactivated:   0
- Superseded:    1  (no-code-markdown-only → no-build-step-agent-run; archived flagged superseded)
- Archived:      6  facts → memory/archive/2026-Q2.md  (faded 5: gitignore-v310-mercury-validation,
                    field-report-v320, dogfood-simple-proxy-enable, backlog-supersession,
                    backlog-invariant-verify-cadence; superseded 1: no-code-markdown-only)
- Swept threads: (same 5 as the faded set — completed v3.1–v3.4 `[x]` threads, sslu 21–26 > archive_window)
- Tier changes:  18  (8 working→active [skills ship facts]; 10 active→archive-candidate
                    [memory-smoke-test-v360, provenance-retrieval-v370, vbdi-shipped-v400,
                    bp-vbdi-loop, bp-altitude-drift, bp-skills-layer, dogfood-target-repo-self-contained,
                    backlog-memory-eval, backlog-provenance-retrieval, gitignore-propagation-v310]).
                    (The 6 archived facts moved out; the 2 top open-thread footers went working→active/archive-candidate.)
- Invariants:    not due — 11 sessions since last_invariant_check (< verify_invariants_every 20); skipped, tracker unchanged.
- Promoted core: 0  (auto-core off; core is human-set)
- Drift:         1 flagged (not fixed) — Blueprint baseline label v4.1.1 vs status v4.4.0; raised as ot-drift-blueprint-baseline-v440.
- Contradiction: 0
- Notes: continuity.md 529 → 489 lines. last_review stamped 2026-06-16 | through 2026-06-16-201531.
         Provenance gap noted on ot-reverify-invariants-20260615 (rebuild uses=0; never in a ## Memory References ledger).

## Memory References
- Referenced: target-repo-scope-only, never-delete-vendor-files, never-pick-a-winner,
  no-build-step-agent-run, upgrades-additive, vision-agent-memory
- Created: ot-drift-blueprint-baseline-v440 (tier: working)
- Superseded: no-code-markdown-only (archived this review — superseded-by no-build-step-agent-run; terminal)
- Reactivated: (none)
