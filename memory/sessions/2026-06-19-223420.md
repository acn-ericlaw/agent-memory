# Session (2026-06-19T22:34:20Z)

**Agent:** Claude Code
**Type:** full

## What happened

Fixed `ot-review-step6-prose` (the archival livelock found in the 2026-06-19 review) and shipped it as
**v4.11.1 (PATCH)**.

**Diagnosis first.** Grepped the docs: the buggy raw-grep instruction lived **only** in `REVIEW.md`
step 6 (the CHANGELOG hit is immutable v4.8.0 history; the continuity hit was the Open Thread itself).
Proved the deterministic path was already correct — `memref_ids` (line-anchored since v4.10.1) does
**not** count a fact named only in `## What happened` / `## Memory Review`; it returns only the real
`## Memory References` ids. So the verifier script needed **no change** — the bug was purely the ritual's
full-text grep.

**Fix (doc + tests only):**
- `REVIEW.md` step 6 rewritten: a "use" is a `## Memory References` entry (§2 / DECAY §2), not a prose
  mention. `memory-lint` is the **preferred** archival check (Memory-References-only ⇒ immune to the
  trap); the **by-hand fallback** now counts only hits *inside* a `## Memory References` block and
  explicitly ignores `## Memory Review` / `## What happened` mentions — with a note naming the livelock.
- Added `memref_ids` regression tests to **both** `test_memory_lint.py` and `test_memory_lint.mjs`:
  (1) a fact in prose / a review summary is not counted; (2) the block is bounded at the next heading.
  Locks in that the deterministic path can't regress into the livelock.

**Verification:** both suites 7/7 (Python + Node); `memory-lint` parity byte-identical, `0 errors,
0 warnings`. No behavior change to the verifier (proven — same output as before).

**Lockstep (PATCH):** `VERSION`→4.11.1; `CHANGELOG` 4.11.1 (Fixed); `UPGRADE.md` 4.11.0→4.11.1 rung +
table (re-copy `REVIEW.md` + the two test files; scripts unchanged); `README` table; `continuity.md`
status line + `ot-review-step6-prose` closed `[x]`.

## Decisions Made

- **Fix the ritual, not the script.** The deterministic tool (`memref_ids`) was already immune; the
  defect was the human-facing step-6 grep instruction. So the fix is REVIEW.md wording + a regression
  test that pins the tool's correctness — not a code change to a working verifier.
- **Lean on `memory-lint` as the authoritative archival check** rather than a raw grep. The grep stays
  only as a no-runtime fallback, now correctly scoped. Consistent with the v4.11.0 "deterministic check
  shouldn't depend on the runtime" stance and the v4.10.1 "don't let prose fool the parser" lesson.
- **PATCH, not MINOR.** A ritual-wording bug fix + tests, no new capability or shape change → 4.11.1.

## Memory References

- Referenced: ot-review-step6-prose (fixed this session), lint-skill-v490, vision-agent-memory
