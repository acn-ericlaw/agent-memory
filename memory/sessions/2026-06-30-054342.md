# Session (2026-06-30T05:43:42Z)

**Agent:** Claude Code

## Summary

**Shipped v4.28.0 (MINOR) — co-author convention cleanup: stable agent identity + one trailer.**
From a real-world finding: dogfooding the v4.27.0 PR convention on **mercury-composable**, a
squash-merged PR (#126) surfaced two imperfections in how the `Co-Authored-By` self-identification
*renders* (the attribution itself worked — human + AI both credited, Verified).

**(1) Identity churn.** The convention said "identify yourself *the same way you do in session
logs*," but session logs use the stable agent name `Claude Code` while the commit/PR trailer was
the **model version** `Claude Opus 4.8` — so it wasn't "the same," and it fragments the co-author
across model releases (Opus 4.8 → 4.9 → …). Fix: specify the **stable agent name** — the actual AI
collaborator (`Claude Code`, `Gemini CLI`, …), kept vendor-neutral via the `<your agent name>`
placeholder — explicitly **not** a model version. Per the maintainer: "use agent name so it is a
faithful representation of the AI collaborator."

**(2) Trailer pile-up on squash.** Every commit carries the trailer (often harness-injected), so a
squash stacked ~9 identical inline `Co-Authored-By` lines **plus** the consolidated one GitHub
appends after the `---------` separator. Convention now says: collapse to a **single** trailer on
squash — GitHub's appended consolidated line is canonical; trim the redundant inline repeats. Both
points are **advisory** — agent-memory can't suppress the vendor harness trailer or GitHub's squash
template; the lever is the convention wording + the PR-footer (which the agent controls).

**Built (lockstep):** `AGENTS.md` (root + `templates/`) commit-trailer note (stable name + squash
guidance); both `.github/pull_request_template.md` footer comments (name the AI collaborator's stable
agent name, not a model version); `VERSION`→4.28.0; `CHANGELOG`; `README` (row + recent-releases
table re-trimmed to its 10-cap, dropping 4.22.3); `UPGRADE` (row at the ascending bottom + a
`4.27.0 → 4.28.0` rung); docs site (`getting-started` tip). No memory-shape/skill/adapter change.

Landing via a **PR** (the third dogfood of the What/Why convention). The PR footer will use
`Co-Authored-By: Claude Code` per this very change.

## Memory References
- Created: coauthor-stable-identity-v4280
- Referenced: pr-what-why-convention-v4270 (refines)
