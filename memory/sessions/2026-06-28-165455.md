# Session (2026-06-28T16:54:55.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.24.0 (MINOR) — decay-policy retune + a review-cadence/size advisory in `memory-lint`.**

**The signal (evidence-driven, not a guess):** the maintainer asked for new decay parameters "based on the
mercury-composable learning," after the just-completed v4.23.2 review left continuity at 490 lines — still
over the old `continuity_max_lines: 300`. I measured both enabled repos:

- **agent-memory** (this tool): 121 sessions, continuity **490 lines / 24 facts** *right after a clean review*.
- **mercury-composable** (real product repo): 61 sessions, **585 lines / 41 facts, 0 archived** — the cadence
  review had **never fired in the field**.

**Four findings → fixes:**
1. `continuity_max_lines: 300` was permanently-red on both (alert fatigue) and conflates non-decaying
   structural sections (Vision, Invariants, Key Decisions, Blueprint) with the decaying region → raised to **600**.
2. No count-based trigger, though fact-count is the verbosity/velocity-independent signal → added
   **`continuity_max_facts: 30`** as the primary lean signal.
3. **Nothing enforced the review cadence** (mercury's smoking gun: 0 archived after 61 sessions) → added a
   `memory-lint` advisory. *This is the keystone — parameters alone can't fix a review that never runs.*
4. `verify_invariants_every: 20` = near-daily human re-confirms at burst velocity (10–20 sessions/day) →
   raised to **40** (anti-fatigue).

**Built:**
- **`memory-lint` advisory check (8), both runtimes at parity + mirror tests (29 each, all green):**
  `[review-overdue]` (fires when `sessions_since_last_review ≥ review_every`, read from the `last_review`
  Project-State stamp; never-reviewed ⇒ all sessions count) and `[continuity-bloat]` (> `continuity_max_facts`
  or `continuity_max_lines`). Advisory (WARN) — a review is a ritual, never a hard gate — but rides every
  lint run + the CI floor, so a lapsed review can't hide.
- **Retuned defaults** in `templates/memory/decay-policy.md` + this repo's: `continuity_max_facts: 30`,
  `continuity_max_lines: 300→600`, `verify_invariants_every: 20→40`. `working/active/archive_window` +
  `review_every` unchanged (they work; bloat came from reviews not running, not the windows).
- **Docs lockstep:** `REVIEW.md` (size trigger: facts primary + lint advisories), `SKILL.md` (check 8),
  `AGENTS.md` (root+template review-cadence note), `.agent/schema.md` (windows list). `VERSION`→4.24.0,
  `CHANGELOG`, `README` (table +4.24.0 / −4.20.3, still 10), `UPGRADE` (row + `4.23.2 → 4.24.0` rung,
  merge-additive so custom-tuned repos keep their knobs). Skill description unchanged → adapters untouched.

**Verified:** py + node test suites 29/29 each; `python3` vs `node` lint output byte-identical (parity);
`memory-lint` on this repo **0 errors, 0 warnings** (24 facts < 30, 490 lines < 600, 0 sessions since review).

**Honest scope note:** the `[review-overdue]`/`[continuity-bloat]` advisories are *validated by unit tests*
but not yet *seen firing in the wild* — the natural proving ground is mercury-composable (overdue: 61
sessions, 0 archived), which on its next `4.24.0` upgrade + lint should light up `[review-overdue]` and a
`[continuity-bloat]` on facts. Left for the mercury team to confirm.

## Memory References
- Created: decay-policy-retune-v4240
- Referenced: vision-agent-memory (serves)
