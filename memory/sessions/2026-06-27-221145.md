# Session (2026-06-27T22:11:45.000Z)

**Agent:** Claude Code

## Summary

**Invariant verification — human gate completed.** Walked the maintainer through the five
`core` Architectural Invariants and the Vision **one by one** (the gate raised by the
2026-06-27 review, `ot-reverify-invariants-20260627`). For each I presented the current
statement, its ADR rationale, and an honest read on whether recent work stressed it.

**Outcome: all 6 confirmed; none superseded.**

1. `target-repo-scope-only` (ADR-0001) — confirmed; reinforced by the v4.23.0 advisory's
   explicit "what I will NOT touch."
2. `never-delete-vendor-files` (ADR-0002) — confirmed; the Antigravity adapter *kept*
   `.gemini/commands` rather than removing it (same preservation instinct).
3. `never-pick-a-winner` (ADR-0003) — confirmed; **strengthened** — now the backbone of
   `MERGE.md` (v4.25.0), and the review's over-archival guard enforced it live this session.
4. `no-build-step-agent-run` (ADR-0006) — confirmed; the most-pressured invariant (growing
   helper-script count) but the line held — every script stays agent/human/CI-invoked, never
   tool-run. Watch-item logged: a future *auto-executed* script would be the real test.
5. `upgrades-additive` (ADR-0005) — confirmed **with a substance-preserving wording fix**:
   added `sync-adapters` to the managed-built-ins example list (it became a tool-managed
   built-in in v4.18.0). Updated in both the continuity invariant text **and** ADR-0005.
6. Vision (`vision-agent-memory`) — confirmed unchanged; materially *advanced* this session
   (the multi-contributor pillar via v4.24.0 merge-friendliness + v4.25.0 MERGE.md); non-goals
   held as guardrails (rejected the resolver-engine).

Checked off `ot-reverify-invariants-20260627`. `last_invariant_check` was already stamped
2026-06-27 by the review. No supersessions.

## Memory References
- Referenced (re-confirmed): target-repo-scope-only, never-delete-vendor-files, never-pick-a-winner, no-build-step-agent-run, upgrades-additive, vision-agent-memory
- Updated (substance): upgrades-additive (added sync-adapters to the managed-built-ins list; ADR-0005 synced)
