# Session (2026-07-27T21:19:35.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.32.0 (MINOR) — Azure DevOps forge support**, implementing the plan recorded in
`ot-azure-devops-forge-next` (maintainer-approved; real field installation behind it — the
trigger gate the Bitbucket deferral respected).

**The design leans into the forge's shape rather than fighting it:**

- **Own-pipeline model** — `templates/.azuredevops/agent-memory-ci.yml` is a complete
  pipeline, so an existing `azure-pipelines.yml` is **never edited** (structurally avoids the
  whole add-only-hazard class the GitLab release had to guard: no include splicing, no stage
  checks). Same two checks; native tri-state advisory (`##vso[task.logissue type=warning]` +
  `task.complete result=SucceededWithIssues` → "partially succeeded", orange;
  `AGENT_MEMORY_STRICT=1` fails the run — no `continueOnError` needed since the script owns
  the result, which keeps strict mode a true red); `fetchDepth: 0` (ADO defaults to shallow=1
  since sprint 209); PR-validation builds diff against `HEAD^1` (merge commit's target
  parent).
- **PR template** — `templates/.azuredevops/pull_request_template.md` (auto-applies;
  default-branch-read; `.azuredevops/` first in ADO's search order; 4000-char cap noted).
- **The honest limit carried everywhere:** activation is not file-driven — a pipeline is a
  *resource*; enable/upgrade REPORT the one-time
  `az pipelines create … --yml-path .azuredevops/agent-memory-ci.yml --skip-first-run`
  (Contributors-level) and run it **only at explicit user direction**; Azure Repos ignores
  YAML `pr:` — PR-time validation documented as the optional Build Validation branch policy
  (admin; "Optional" = notify-only); Microsoft-hosted agents need the parallelism grant.
- **Unknown-forge policy tightened:** GitHub + GitLab sets only — the ADO set installs on
  positive detection (an inert pipeline from a blind install is pure noise).
- **Third squash branch** (AGENTS.md ×2): ADO drops trailers AND has no message-template
  mechanism (unlike GitLab's `%{all_commits}`) — durable record is the PR-description footer;
  re-add via "Customize merge commit message" at completion.
- **Lockstep:** ENABLE (detection item 8 + Step 6 install bullets + honest-limit paragraph +
  Step 8/9 + Next-steps activation line + write-scope), `.githooks/README.md` + `init.sh`,
  DESIGN-ritual-triggers v4.32.0 amendment, docs site ×6 pages + optional-ritual-hook header,
  `VERSION`→4.32.0, `CHANGELOG`, `README` (row + 10-cap trim, drops 4.27.0; file tree),
  `UPGRADE` (row + `4.31.0→4.32.0` rung).

## Adversarial pre-ship review — findings & dispositions

3-lens workflow (ADO tech with web verification / protocol coherence / claim sweep). **The
forge mechanics all held** — the v4.31.0 error class was hunted explicitly and everything
verified: `##vso` tri-state semantics, `AGENT_MEMORY_STRICT` variable mapping, the
`HEAD^1`-is-target-tip merge assumption, `fetchDepth: 0`, the `az pipelines create` flags,
`pr:`-ignored-on-Azure-Repos, template paths/precedence/4000-char cap, squash mechanics.

**1 blocker (fixed):** README's *v4.31.0* row still carried the original `%{co_authored_by}`
falsehood the v4.31.0 review had corrected everywhere else — the fix batch had missed that one
surface. Corrected to re-add-at-merge / `%{all_commits}`.

Should-fixes, all applied: activation command now states the **push-first** precondition
everywhere (az binds to the server-side YAML path; running early drops the user into the
CLI's interactive flow) — ENABLE Step 6 + Step 9 + rung + template header; enable/upgrade now
**seed a `- [ ] (forge) …awaiting one-time activation` Open Thread** in the target's
continuity so the inert-floor state can't be silently forgotten; status line's leading
version bumped (was still v4.31.0); `.githooks/README.md` + ENABLE CI-floor lead + docs
index/whitepaper ×3 forge-qualified (ADO ≠ zero-config; PR-time runs need the Build
Validation policy); REVIEW.md's two-forge enumeration → three; push-builds-check-tip-only
limit disclosed (template header + DESIGN amendment); parallelism gloss corrected ×4 (free
grant = request form; subscription link = paid path). Nits applied: `git rev-parse --verify
--quiet` base-ref hardening — **back-ported to the GitLab twin and GitHub workflow** (same
latent wart: bare `rev-parse` echoes the unresolvable arg, so a parentless HEAD printed
"ritual check: ok" instead of the skip message; rung step 3 now re-copies the existing forge
CI file); rung verify step split (all-targets clause vs ADO-only clause); "auto-applies…
default branch" wording de-garbled; ENABLE intro/heading version labels; GitHub workflow
header names both siblings; this dispositions section filled (was a placeholder, flagged by
the review itself).

## Memory References

- referenced: ot-azure-devops-forge-next (the plan this implements — marked shipped this
  session; its substance updated in place, no separate successor fact),
  gitlab-forge-support-v4310 (the forge seam + the review-lesson this release inherits),
  no-build-step-agent-run (the activation command is user-run, never tool-run)
- created: (none — the shipped thread is the fact)
