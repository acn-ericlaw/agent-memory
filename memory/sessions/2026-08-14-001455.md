# Session (2026-08-14T00:14:55.000Z)

**Agent:** Claude Code

## Summary

**Reviewed GitHub Copilot's v4.33.3 security hardening at the maintainer's direction — verdict:
agree on all four fixes — and folded one regression fix into the still-untagged release.**

Multi-agent continuity honored: read Copilot's session log (2026-08-13-235301) in full before
responding, then re-verified its claims independently rather than taking the log's word: 46/46
mirror tests both runtimes, byte-identical output, dogfood 0 errors, all three forge wrappers'
strict/advisory branches inspected. Endorsed findings, in order of weight:

1. the `--strict` wrapper fix corrects a real pre-existing bug — warning-only findings exited 0,
   so the advertised `AGENT_MEMORY_STRICT` gate had been inert for advisories since v4.24.0;
2. key-scoping the ALL-CAPS enum exemption closes the uppercase-secret bypass my v4.33.1 global
   rule created, while the motivating field FP stays fixed (verified live);
3. quoted JSON/YAML keys + Authorization headers were true bypasses, now flagged without value
   echo, and anchored placeholder matching closes the substring-laundering hole;
4. the Mode C redaction/triage mandate covers the one unguarded path to committed leaks.

**The regression (found by probing the live targets — the step the review skipped; the
verbatim-fixture lesson from v4.33.2 applied as a review technique):** the tightened
`TEMPLATE_VALUE_RE` accepted only bare `${IDENTIFIER}`, so standard default-value / dotted
env-var forms were re-flagged — two live FPs on mercury-composable's committed memory, on
`${REDIS_PASSWORD:}`, the repo's own documented safe pattern for secrets. With the now-working
`--strict` wrappers those would have turned its CI orange on the next push. **Folded fix
(maintainer-approved, pre-tag):** the `${…}` alternative accepts any brace-delimited reference
(`[^{}\s]+`) — still a full-value anchored match, so partially-literal values keep flagging;
the mercury-composable line joined the mirror fixtures verbatim. Re-verified: 46/46 both
runtimes, byte parity, dogfood 0 errors, **both live targets probe zero `[secret-material]`
findings**. Records updated in place (CHANGELOG / README / UPGRADE rung / the v4333 continuity
thread) — no new version minted, since 4.33.3 was merged but never tagged and no target had
consumed it.

Watch-items surfaced to the maintainer (field-triggered, no action now): bare
`authorization: <word≥8>` prose can false-positive, and the enum-dimension suffix list
(`source|type|mode|mechanism|strategy`) may eventually want `provider`/`method`.

## Memory References

- referenced: secret-review-hardening-v4333 (the patch under review — fold recorded in its
  thread), secret-fp-backtick-v4332 (its verbatim-fixture lesson became this review's live-probe
  technique), secret-redaction-lint-v4330 (the check whose hardening was reviewed)
- created: (none — the fold is recorded in secret-review-hardening-v4333)
