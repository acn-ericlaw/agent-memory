# Session (2026-08-26T00:02:08.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.38.1 (PATCH) — scanner-neutral guidance constant** ([PR #26](https://github.com/acn-ericlaw/agent-memory/pull/26)),
from a downstream field handoff (2026-08-25): an enterprise security pipeline (Snyk) in a
Java-framework deployment rejected builds, flagging memory-lint's consolidated guidance
constant as a hardcoded secret. That detector class keys on identifier-contains-SECRET +
string-literal assignment; the constant's old name (`SECRET_GUIDANCE`, from the 4.34.1
once-per-run change) carried exactly that shape while its value is prose — a pure false
positive, but enterprise waiver workflows are slow and per-installation, so the fix landed
at the source.

**Verified before implementing** (every handoff claim probed against the live tree):
exactly 3 occurrences per runtime (definition + two consumers), no bare `GUIDANCE`
collision, no other flagged-shape identifiers in shipped scripts/hooks (remaining grep
hits were comments and a value-side test fixture), and the "no canonical token literals"
claim holds — only regex fragments and runtime-constructed dummy prefixes exist, never
complete tokens.

**Shipped:**

- Renamed the constant `GUIDANCE` in both runtimes, with a comment recording the
  scanner-neutral constraint. Behavior-neutral, proven: lint output byte-identical
  before/after in both runtimes AND across runtimes (three empty diffs on the live
  memory layer) — matching the downstream repo's own verification.
- **Script-hygiene self-check per suite (53 ×2):** scans every shipped `.py`/`.mjs` in
  the skill's script directory for the flagged identifier shape; trigger words assembled
  at runtime so the suites never commit the shape (the same construction the suites use
  for dummy secrets). **Red-verified first:** against the pre-rename tree each suite
  caught exactly the two motivating definitions and nothing else, then went green after
  the rename — the verbatim-fixture doctrine applied to an identifier-shape artifact.
- `SKILLS.md` (Authoring) house rule: keep SECRET/TOKEN/PASSWORD/KEY-class words out of
  prose-constant identifiers in shipped scripts — the tool's own subject matter makes it
  prone to exactly this scanner class (the downstream's suggestion, adopted).
- Lockstep: VERSION 4.38.1, CHANGELOG entry, UPGRADE ladder row + `4.38.0 → 4.38.1` rung,
  README release row (table re-trimmed to 10). No MANIFEST change — `agent-skills/
  memory-lint/` is `verbatim-dir`, so the rename propagates by plain re-copy and the
  downstream's interim local edit converges to zero diff, exactly as their handoff
  anticipated. No semantic step; SKILL.md untouched → no adapter re-sync.
- Gates: memory-lint suites 53/53 ×2, reconcile suites 38/38 ×2, `--check-manifest` OK
  (17 semantic rows gated ≤ 4.38.1).

One process note: the new python test first landed inside `TestScanFilesMode` (appended
after its last method) and the loader couldn't find it under the expected class — moved to
its own `TestShippedScriptHygiene` class, matching the suite's one-class-per-concern
layout.

**Post-merge, same working session:** upgraded mercury and mercury-composable to 4.38.1
at Eric's direction (their own logs record it). The first live reconcile surfaced a rung
doc gap — step 1 named only the four script files while `SKILLS.md` had also drifted (the
house rule landed there); the rung now names it, keeping the no-runtime hand-walk faithful
to what the manifest actually converges. mercury-composable's fork verified again as
`ok — sanctioned consumer fork`.

## Memory References

- secret-fp-guidance-ident-v4381 — created (tier: working): the v4.38.1 ship record
- upgrades-additive — the tool-managed built-ins overwrite exception is exactly why the
  downstream's interim edit converges on upgrade; consulted to confirm the convergence
  story in the release notes
- secret-test-fixtures-env — mirrored its runtime-construction approach so the new
  hygiene test never commits the scanner-triggering shape itself
- backlog-real-work-dogfood — the field-report flywheel exercised end-to-end again
  (downstream handoff → verified → PATCH release)
