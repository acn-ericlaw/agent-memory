# Session (2026-06-24T15:55:06.000Z)

**Agent:** Claude Code

## What happened

Shipped **v4.17.0 (MINOR) — GitHub Copilot CLI skills adapter** (a 5th adapter target),
then dogfood-upgraded `~/sandbox/simple-proxy` to consume it.

**Trigger (real-work dogfood).** The maintainer reported that **GitHub Copilot CLI could not
discover a newly created skill** in `~/sandbox/simple-proxy`. Root cause: the skills layer had
**four** adapter targets (Claude/Gemini/Cursor/Kiro) but **none for Copilot**, so a skill authored
in the neutral `agent-skills/` layer had no Copilot-native discovery path.

**Research (grounded, not from memory).** Confirmed against current GitHub docs that **Copilot CLI
has a native agent-skills feature** following the **open Agent Skills standard** — project skills
live under **`.github/skills/<name>/SKILL.md`** (also reads `.claude/skills/`, `.agents/skills/`),
same `SKILL.md` frontmatter (`name` lowercase-hyphens + `description`), **auto-matched by
`description` AND invokable as `/<name>`**. So Copilot is the same shape as the Claude/Kiro adapters
— this mirrors the **v4.5.0 Kiro** addition exactly.

**Implemented (lockstep, mirrors 4.5.0).** `.github/skills/<name>/SKILL.md` is the 5th adapter;
`sync skill adapters` now writes **five**. Touched: `SKILLS.md` (recipe bullet + trigger-semantics
+ five-count + never-commit note), `AGENTS.md` (root + template adapter list), `templates/.agent/schema.md`
(adapter list — also backfilled the missing Kiro entry), `ENABLE.md` (5h reference + Step 8 completeness
assertion now requires five + Notes + report), `UPGRADE.md` (version table row + standing-sync section
four→five + new `4.16.1→4.17.0` rung), `MIGRATE.md` (detection table + B2 + GitHub Copilot section now
promotes `.github/skills/` + `.agents/skills/`), `templates/.github/copilot-instructions.md` (now points
Copilot at the skills layer — directly addresses the discovery gap), `README.md` (version + detection +
skills-promoted), `docs/DESIGN-skills-layer.md` (status header + all-vendor line + adapter table + trigger
note), `CHANGELOG.md`, `VERSION`→4.17.0, and both `.gitignore` (root + template).

**Key design subtlety — path-scoped gitignore.** Unlike the other four adapter dirs (`.claude/`,
`.gemini/`, `.cursor/`, `.kiro/`) which are ignored **wholesale**, Copilot's adapter lives at
`.github/skills/` and **`.github/` cannot be ignored wholesale** — it holds the tracked
`copilot-instructions.md` and `workflows/`. So `.github/skills/` is ignored **path-scoped**. Verified
both repos: `.github/skills/` ignored, `.github/copilot-instructions.md` stays tracked.

**No new ADR** — adding a vendor adapter is not a new *architecture decision*; it's another
realization of the existing vendor-neutral architecture (same call as the Kiro 4.5.0 addition, which
created no ADR). The ADR upkeep trigger does not fire.

**Dogfood validation.** Ran the 5-adapter sync on the tool itself (4 skills → 20 adapters; special
chars — em-dash + apostrophe in `apply-critique` — embed cleanly in YAML + TOML). Then upgraded
`~/sandbox/simple-proxy` via the new rung (re-synced AGENTS.md/schema.md/SKILLS.md/copilot-instructions.md,
added `.github/skills/` path-scoped to its `.gitignore`, ran the 5-adapter sync → 20 adapters, stamped
**4.17.0**). `memory-lint` on simple-proxy: **0 errors** (12 overdue = pre-existing review-cadence,
unrelated). simple-proxy's pending diff also still carries the uncommitted 4.14.0→4.16.1 changes from
earlier this session — both upgrades commit together.

**Not committed** — left for the maintainer (deliberate, human-initiated). The tool's own
`.github/skills/` adapters are gitignored (not in the diff).

**Follow-up refinement (same session, folded into the still-uncommitted v4.17.0).** The maintainer's
GitHub Copilot teammate reviewed the v4.17.0 diff and flagged two things: (1) the Copilot steering
file points to `AGENTS.md` rather than front-loading the read list — and Copilot's **Ask/Plan** modes
don't follow that pointer chain (they don't proactively open files), so the required reading is
unreliable; (2) session upkeep (decay/review/session logs) is Claude-Code-native — Copilot won't do
it automatically. **Owned:** my v4.17.0 rung had re-synced simple-proxy's *front-loaded*
copilot-instructions (it listed `instructions.md`/`continuity.md`/`sessions/`) down to the tool's
thinner pointer template — a regression. **Fix:** rewrote `templates/.github/copilot-instructions.md`
to **front-load the explicit `memory/` read list** (`AGENTS.md`, `instructions.md`, `continuity.md`,
`vision.md`, recent `sessions/`) + a **"session upkeep is manual in Copilot"** note (sets the
expectation; can't auto-fix without a Copilot Stop-hook equivalent), keeping the skills pointer.
Deliberate, **Copilot-scoped** reversal of the single-source-read-order convention (reliability over
DRY; Claude/Gemini/Kiro keep thin pointers — they proactively load `AGENTS.md`). Re-synced
simple-proxy; folded into v4.17.0 (no new version — nothing released yet). Other review points: skills
location (point 2) is already resolved by v4.17.0's `.github/skills/` adapter; the flat-`memory.md`
note (point 5) is N/A for simple-proxy (it uses `memory/`).

**Point #4 — optional Copilot `sessionEnd` hook (maintainer asked if it's possible).** Confirmed
(current GitHub docs): Copilot CLI **has hooks** — events `sessionStart`/`sessionEnd`/
`userPromptSubmitted`/`preToolUse`/`postToolUse`/`errorOccurred`, JSON config (`version: 1`) in
repo-level `.github/hooks/` or user-level `~/.copilot/hooks/`. `sessionEnd` is the analog of Claude's
`Stop`. Documented it in `docs/optional-ritual-hook.md` (advisory, opt-in, not ENABLE-installed) and
dropped a working `.github/hooks/agent-memory-session.json` into `~/sandbox/simple-proxy` (tracked —
`.github/hooks/` isn't gitignored, unlike `.github/skills/`). Validated: valid JSON, runs, exits 0,
fires the nudge when the tree changed without a fresh session log. It only *nudges* — can't write the
log (no hook can); makes the skip **visible**, matching the tool's "prompted, not enforced" stance.

**Cross-vendor validation (2026-06-24) — v4.17.0 confirmed by a different vendor.** GitHub Copilot CLI
(Gemini 3.1 Pro), in **Agent mode**, bootstrapped agent-memory in `~/sandbox/simple-proxy` and authored
a new `greeting` skill end-to-end: read the AGENTS.md→memory pointer chain, created
`agent-skills/greeting/SKILL.md`, ran `sync skill adapters`, **generated all 5 vendor adapters** (incl.
`.github/skills/greeting/SKILL.md`), and **recognized the Copilot adapter** — "behaves exactly as
documented." Also praised the `date -u` filename guardrail (v4.16.1) and the single-line/quote-free
description rule. **One friction point → fix:** Copilot loads `.github/skills/` (and hooks) at **init**,
so the freshly-synced `/greeting` slash command wasn't live until a `/restart` / skills rescan → added a
**hot-reload caveat** to `SKILLS.md`'s `sync skill adapters` op (re-synced to simple-proxy). simple-proxy
now carries the Copilot-authored `greeting` skill + its 5 adapters (a demo artifact). Note: the test was
**Agent mode** (pointer chain followed fine); the front-loaded steering still matters for Ask/Plan.

**Point #4 resolved as conformant behavior (Copilot's own clarification).** Copilot stated its Ask/Plan
modes are **read-only** and *do* read `memory/` for accuracy, but write **no session log** unless it
transitions to Agent mode and changes files. That is **exactly the protocol's lightweight-mode rule**
(read-only session → no log; v4.7.1/v4.10.3) — conformant, not a gap. So I **corrected** the
`copilot-instructions.md` note: dropped the inaccurate "session upkeep is manual in Copilot" framing and
replaced it with the lightweight-mode logging rule (log on tracked-file change; read-only = no log).
Also softened the optional-hook doc's "non-agentic" framing to "backstop." Net: Copilot is fully
lightweight-mode-conformant across all three modes (Agent logs; Ask/Plan correctly don't).

**Authoring follow-through friction (2026-06-24).** The maintainer observed Copilot **does not reliably
run `sync skill adapters` after authoring a skill** — had to prompt it (and then `/restart` to load the
new `/<name>`, the already-documented hot-reload caveat). Earlier the same vendor self-claimed it synced
autonomously → behavior is **non-deterministic**. The tool can't force a mid-session sync
(`no-build-step-agent-run`; auto-sync is guaranteed only at enable/upgrade, v4.12.0). Mitigation:
strengthened the **authoring convention** to an explicit **3-step action** (write → `sync skill adapters`
→ reload the runtime, "not done after step 1") in both the on-demand `SKILLS.md` and the always-read
`AGENTS.md` (root + template); re-synced to simple-proxy. Improves adherence odds; doesn't guarantee it. Also shipped an **opt-in
Copilot `postToolUse` auto-sync hook** (documented in `docs/optional-ritual-hook.md` + dropped into
simple-proxy as `.github/hooks/agent-memory-autosync.json`) that regenerates the `.github/skills/`
adapter after each tool use — so the user needn't remember to sync. **Convenience-scoped:** Copilot
adapter only, no prune, duplicates the recipe in bash (→ local opt-in, not canonical; the agent-run
`sync skill adapters` and enable/upgrade auto-sync stay authoritative; the tool still ships **no sync
script**, no-code held); `/restart` still loads it. Validated: deletes + regenerates the greeting
adapter, exit 0, special chars (em-dash/apostrophe) clean. The maintainer will test it live in Copilot.

## Memory References

- Created: `copilot-adapter-v4170`
- Referenced: `kiro-adapter-v450` (the precedent this mirrors), `skills-layer-v410`,
  `backlog-real-work-dogfood` (real-work dogfood fed the tool), `upgrades-additive` (the 5th adapter
  is additive; the gitignore change is add-only/path-scoped)
