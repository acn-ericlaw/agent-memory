# Session (2026-06-17T18:16:07.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.6.0 (MINOR)** — a vendor-neutral **commit-attribution** convention in `AGENTS.md`,
prompted by the maintainer's observation that Claude Code adds the deliberate, human-gated
`Co-Authored-By:` trailer automatically while Kiro needed a per-machine hook to do the same.
Encode it once in the shared layer → every vendor follows, no per-vendor setup.

## What happened

- **Observation.** Claude Code has the deliberate-commit + co-author trailer baked into its
  harness; Kiro replicated it via an enterprise hook + steering; Gemini/Cursor do nothing by
  default. The behavior was vendor-by-vendor — exactly the inconsistency the shared layer exists
  to remove.
- **Decision (maintainer, via AskUserQuestion): "Add it (soft, vendor-neutral)."**
- **Change.** `AGENTS.md` (root + template) "After Every Session" step 4 + checklist now extend
  the existing **"identify yourself"** principle (already true for session logs) to **commits**:
  *commits are deliberate and human-initiated; when you commit at the human's direction, identify
  yourself the way you do in session logs — e.g. a `Co-Authored-By: <your agent name>` trailer.*
  **Soft** — guides, doesn't prescribe git workflow; a no-op for runtimes (Claude Code) that
  already do it.
- **Why it's on-vision.** Serves `bp-multi-user` (multi-contributor traceability) + provenance:
  authorship is now consistently traceable across vendors without each vendor needing its own
  hook. A clean "encode once vs. N per-vendor configs" demonstration.
- **Scope.** `AGENTS.md` is installed into targets, so this is target-facing → **MINOR**.
  `VERSION`→4.6.0; `UPGRADE.md` 4.5.2→4.6.0 rung (re-sync `AGENTS.md`) + table; `README`/`CHANGELOG`.
  `SKILLS.md`/`DECAY.md`/`REVIEW.md` unchanged.

## Notes / open items

- Existing enabled targets pick this up on their next "AI enable" (Mode B re-sync of `AGENTS.md`).
- Demo tomorrow (2026-06-18) test-drives the "AI enable" upgrade — this rung (4.5.2→4.6.0) will be
  part of the ladder a target climbs.
- Standing thread `ot-realworld-v411-findings` still open (teammate's client-enablement report).

## Memory References

- Referenced: vision-agent-memory, bp-multi-user, no-build-step-agent-run, kiro-hooks-edgecase-v452
- Created: commit-attribution-v460 (tier: working)
