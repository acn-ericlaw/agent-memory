# Session (2026-07-05T02:03:13.000Z)

**Agent:** Claude Code
**Lightweight:** single-fix session; no `continuity.md` facts touched.

## Summary

Actioned a field feedback report from a mercury-composable session
(`/tmp/agent-memory-continuity-size-note.md`): `memory-lint`'s `[continuity-bloat]`
fact-count check compared `continuity_max_facts` against **all** continuity footers,
including `tier: core` invariants and pinned `- [ ]` open threads — entries that can
never decay or be archived. On a mature repo (41 footers / 14 core / 11 open threads)
the warning stayed red even immediately after a fully correct `REVIEW.md` ritual,
turning the primary lean signal into permanent noise → alarm fatigue (the exact
"nobody archives" failure v4.24.0 set out to fix). The code disagreed with
`decay-policy.md`, which documents the cap as a "count of **decaying** facts/threads."

**Fix (v4.28.2):** `check_continuity_health` now excludes `tier: core` + pinned ids
before comparing against the cap. A repo with 14 core + 11 open threads + 16 working
facts reads `16 < 30` (clean) instead of `41 > 30`. Warning text now reads
"decay-eligible facts". Both the Python and Node runtimes updated at parity (each gains
an optional `pinned` arg, default empty — backward compatible; call site passes the
already-computed set), with a mirror regression test
(`test_fact_bloat_excludes_core_and_pinned`) added to both suites (35/35 pass each).
Live lint on both runtimes produces byte-identical output on this repo.

Two review rounds folded in before shipping (GitHub Copilot solution review): (1) the
first pass fixed only Python — extended to the Node mirror + tests, restoring the
cross-runtime parity contract; (2) release lockstep — CHANGELOG (also **backfilled the
missing 4.28.1 section**, sourced from commit `9d918ab`), README recent-releases table
(added 4.28.2 + the never-added 4.28.1, trimmed to the 10-most-recent cap), UPGRADE.md
capability row + `4.28.1 → 4.28.2` rung. Also fixed a **pre-existing 4.28.0/4.28.1
ordering swap** in the UPGRADE.md capability table and rung sections so the ladder is
contiguous (each rung's target = the next rung's source).

Commits: `c32e07b` (fix) — the 8 code/doc files (→ 4.28.2).

## Memory References
(none — no `continuity.md` facts referenced or created; a personal auto-memory
covering this fix's lesson may be recorded outside this repo's ledger)
