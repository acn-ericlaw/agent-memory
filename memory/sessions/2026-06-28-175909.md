# Session (2026-06-28T17:59:09.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.26.0 (MINOR) — `refresh-metadata` (7th built-in) + a `memory-lint` `[stale-metadata]`
advisory.** Closes the skipped-re-tier gap that a cross-vendor field test exposed.

**The signal:** Copilot / Gemini 3.1 Pro committed + pushed the v4.25.0 mercury upgrade and, seeing the
`[review-overdue]` advisory, **ran the review unprompted** — a strong cross-vendor validation:
- used `archive-fact` (didn't hand-edit) → no truncation;
- the **over-archival guard caught a premature archive** (`adr-pattern-adopted`, sslu 17 ≤ 20) → it reverted;
- put archived ids in `## Memory Review`, not `## Memory References` → no livelock.

**But** it did Step 4 (archive) + Step 5 (sweep) and **skipped Step 2 (apply events) + Step 3 (re-tier)** —
leaving stale `last_used`/`uses`/`tier` footers on the ~31 facts that stayed. A capable agent silently did
half the ritual and would've reported success.

**The pattern (now 3×):** truncation (botched write) → `archive-fact`; never-fired review (skipped trigger)
→ `[review-overdue]`; half-done ritual (skipped steps) → this. *Multi-step agent rituals get partially
executed.* The robust fix is always: mechanize the deterministic part, flag the gap via lint, leave only
judgment to the agent.

**Refines the earlier "reject auto-fix" stance:** the real boundary is **judgment vs. arithmetic**. Deciding
*what to archive* = judgment (stays with the agent, `never-pick-a-winner`). Recomputing metadata = arithmetic
(REVIEW.md's "full rebuild," already called deterministic + reproducible) → safe to mechanize.

**Built:**
- `agent-skills/refresh-metadata/` — recompute `last_used`/`uses`/`tier` for every fact from the
  `## Memory References` log; read-into-memory-write-once; `core`/`superseded`/never-referenced untouched;
  clamps at `archive-candidate` (never archives — that's `archive-fact`). `--dry-run`, idempotent.
- `memory-lint` check (9) `[stale-metadata]` — stored tier ≠ recomputed tier → flag (steps 2–3 skipped).
- Both Python + Node at parity; mirror tests (memory-lint 33, archive-fact 6, refresh-metadata 5).

**Dogfooded:** `[stale-metadata]` flagged **11 stale footers on THIS repo** — my own earlier reviews this
session skipped re-tiering too (universal, not vendor-specific). `refresh-metadata` cleared all 11 →
`memory-lint` 0 errors, 0 warnings.

**Lockstep:** skill + tests, memory-lint + tests, `REVIEW.md` (steps 2–3 lead with the helper), `ENABLE.md`
§5i (7 built-ins), `README`/`ADR`/continuity built-in lists, adapters (8 skills → 48), `VERSION`→4.26.0,
`CHANGELOG`, `UPGRADE` (row + `4.25.0 → 4.26.0` rung).

**mercury:** its review is still half-done (metadata pass missing). Copilot offered to complete it; once
mercury upgrades to 4.26.0, `[stale-metadata]` will pinpoint exactly which footers need the refresh — left
for the mercury team.

## Memory References
- Created: refresh-metadata-builtin-v4260
- Referenced: vision-agent-memory (serves)
