# Session (2026-06-28T03:51:22.000Z)

**Agent:** Claude Code

*(lightweight)* Refreshed the stale header docstring in `.githooks/post-commit` — it still read
`(v4.19.0)` and described the old "auto-stub + nudge" without the per-session window. Updated to note
the per-session auto-stub (v4.22.1) + 30-min window (v4.22.3) and the AGENT_MEMORY_SESSION_WINDOW_MINUTES
override. **Logic + inline comments were already correct — header-only.** Flagged by mercury-composable's
agent during its v4.23.0 validation (which empirically confirmed the window behavior). Comment-only — no
behavior/protocol change, so **no version bump** (bumping would force every target into a no-op upgrade
for a docstring). Docs-only, no memory-relevant event.

## Memory References
- (none)
