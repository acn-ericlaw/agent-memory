# Session (2026-06-16T00:21:34.000Z)

**Agent:** Claude Code

## Summary

Closed the invariant-verification gate (`ot-reverify-invariants-20260615`) raised by the
2026-06-15 review. The maintainer adjudicated: four invariants + the Vision still hold;
**`no-code-markdown-only` is superseded** because the v4.1.x skills layer can bundle helper
`scripts/`, making the absolute "markdown-only" wording false. Memory-only change (the tool's
own dogfood continuity) — no protocol/template/VERSION change.

## What happened

- **Superseded `no-code-markdown-only` → `no-build-step-agent-run`** (DECAY §9). New `core`
  invariant: *"The tool itself runs no code and needs none (no install, no daemon). The
  markdown files are the product and the agent is the runtime. A skill MAY bundle optional
  helper scripts, but those are invoked by the agent/vendor at the user's direction, never
  executed by the tool."* Old fact marked `tier: superseded | superseded-by:
  no-build-step-agent-run`, left in place for the next review to archive (flagged superseded).
- **Closed `ot-reverify-invariants-20260615`** `[x]`: confirmed `target-repo-scope-only`,
  `never-delete-vendor-files`, `never-pick-a-winner`, `upgrades-additive`, and the Vision;
  superseded the one above.
- **Rationale:** this is a real truth-state change, not a rubber-stamp — the skills layer
  (the work that triggered the review) refined the tool's defining "no-code" principle. The
  intent is preserved (the *tool* runs no code); the absolute "markdown-only" is relaxed for
  agent-invoked skill scripts.

## Notes / open items

- **Downstream wording (minor, not changed):** the same "markdown-only / 100% markdown"
  phrasing appears in `README.md` (Evolving Memory / philosophy) and the Vision non-goals
  ("Never requires code — markdown-only"). The Vision was **confirmed as-is** by the
  maintainer and "never *requires* code" still holds (scripts are optional); README prose is
  downstream. Flag only — align if/when desired; not auto-edited.
- **Review:** not re-run (last_review through 2026-06-15-231502; +3 sessions < review_every
  10). The new superseded fact will be archived at the next review (§9, promptly). continuity
  remains over the 300-line cap (known; leans as history ages out).
- Reminder: commit `memory/`.

## Memory References

- **Superseded:** `no-code-markdown-only` → `no-build-step-agent-run` (archived-superseded at next review)
- **Created:** `no-build-step-agent-run` (core invariant; supersedes no-code-markdown-only)
- **Referenced:** `target-repo-scope-only`, `never-delete-vendor-files`, `never-pick-a-winner`,
  `upgrades-additive`, `vision-agent-memory` (all confirmed still-holding), `skills-layer-v411-fixes`
