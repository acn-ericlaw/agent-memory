# Session (2026-06-17T18:55:04.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.7.1 (PATCH)** — refined the v4.7.0 lightweight mode so the skip is keyed to the
**objective "did a file change?"** test, not a subjective "is this trivial?" judgment. From the
maintainer: *"trivial" is a judgment call and both AI and human can be wrong*, so any file change
must still be recorded.

## What happened

- **Maintainer adjustment.** Distinguish **read-only** from **trivial**: a read-only session (no
  file changes) can skip the log; a trivial change (e.g. one line of code) must still be recorded —
  because misjudging "trivial" would drop a change that actually mattered out of the ledger.
- **Three-tier rule (rewritten in `AGENTS.md`, root + template):**
  1. **Read-only** (no file changes — orientation, Q&A, exploration) → **no session log**.
  2. **Any file change, however small**, with no memory-relevant event → a **one-line lite log**
     (never skipped on a "felt trivial" call; the git diff anchors it).
  3. **Memory-relevant event** (fact / decision / Open Thread / state change, or Vision / Blueprint
     / invariant / supersession) → the **full** ritual.
- **Why this is better.** The gate is now objective and verifiable (file changed y/n) rather than a
  subjective judgment that both AI and human get wrong. Safer for the ledger; still lightweight.
- **Scope.** `AGENTS.md` is target-facing → **PATCH** (refines v4.7.0). `VERSION`→4.7.1;
  `UPGRADE.md` 4.7.0→4.7.1 rung + table; `README`/`CHANGELOG`. `SKILLS.md`/`DECAY.md`/`REVIEW.md`
  unchanged.

## Notes / open items

- Appended the refinement to `lightweight-mode-v470` (same feature, sharpened) rather than minting
  a near-duplicate fact.
- Standing thread `ot-realworld-v411-findings` still open (teammate's client-enablement report).

## Memory References

- Referenced: lightweight-mode-v470, vision-agent-memory, no-build-step-agent-run
