# Session (2026-06-13T01:11:48.000Z - 2026-06-13T01:27:13.000Z)

**Agent:** Claude Code
**User:** Implement open threads 1+3: source-of-truth/version-drift guidance and post-report verification step.

## What We Did

1. **ENABLE.md Step 4 — version drift guidance.** Added a 7th "determine" item:
   read version from the canonical build manifest only (pom.xml, package.json,
   Cargo.toml, pyproject.toml, *.gemspec); if other files reference a different
   version, log an Open Thread and leave the fix to the user.

2. **ENABLE.md new Step 8 — Verify.** Inserted a pre-report sanity-check step:
   confirm all expected files exist, grep for unfilled `{{` placeholders, spot-check
   Mode C integration faithfulness, and validate session file title format. Any
   unresolvable issue gets logged as an Open Thread and noted in the report.
   Renumbered old Step 8 → Step 9, old Step 9 → Step 10.

3. **CLAUDE.md** — updated "9-step protocol" reference to "10-step".

## Decisions Made

- Verification placed *before* the report (new Step 8) so the report always
  describes a correct state, not an intended one.
- Version drift is surfaced as an Open Thread, never silently fixed — drift
  may be intentional (e.g. historical feature-introduction notes) and the user
  must decide.

## Context for Next Session

Open threads 1 and 3 from the mercury-composable test drive are now closed.
Remaining open design threads: git-aware migration (MIGRATE.md `git mv`),
monorepo guidance. Real-world test threads (Mode C with real Cursor+Aider repo,
Continue.dev) still pending.
