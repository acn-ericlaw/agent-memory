# Session (2026-07-12T03:07:10.000Z)

**Agent:** Claude Code

## Summary

Reviewed a **GitHub Copilot assessment of v4.29.0** (`/tmp/agent-memory-4.29.0-assessment.md`)
and shipped the fix it motivated as **v4.29.1 (PATCH)**.

**Review verdict:** accurate and fair — scope, strengths, and honest limits all match what
shipped. Two of its claims were verified **empirically in this very session**:

1. **The import mechanism works** — this session's context contained `AGENTS.md` +
   `memory/instructions.md`/`continuity.md`/`vision.md` (including hours-old content) via the
   root `CLAUDE.md` imports, with no explicit read. The 4.29.0 rung's verify step, passing live.
2. **The "Dogfood Finding" (template bleed-through) is real and cross-vendor.** When a file
   under `templates/` was touched, the Claude Code runtime injected `templates/AGENTS.md` +
   `templates/memory/instructions.md`/`continuity.md`/`vision.md` into context as
   instruction-flavored material — exactly the **import closure of `templates/CLAUDE.md`**
   (imports resolve relative to the containing file, so `@memory/instructions.md` in a template
   pulls the template stub). Copilot saw the same in its runtime. v4.29.0 didn't create the
   directory-scoped auto-load behavior, but its import block amplified it from one stray file to
   the whole template memory set: `{{PROJECT_NAME}}` junk, "last_session: (none yet)",
   conflicting "Identify yourself as…" identity lines. Tool-repo-only — targets have no
   `templates/`.

Report gaps noted: it understates Class-2 coverage (AGENTS.md-native runtimes — Codex, Kiro,
Antigravity — auto-load the hub already), and its follow-up #3 (keep Cursor/Kiro gated on field
complaints) was already recorded pre-assessment (`ot-before-session-cursor-kiro-backlog`).

## Shipped v4.29.1 — `{{BOOTSTRAP_IMPORTS}}` placeholder (maintainer-directed)

- `templates/CLAUDE.md` + `templates/GEMINI.md`: live import lines → a single
  `{{BOOTSTRAP_IMPORTS}}` placeholder (established `{{UPPER_SNAKE_CASE}}` mechanism). The tool
  repo now carries **no live imports under `templates/`**.
- `ENABLE.md` Step 6: defines the per-vendor literal blocks (Claude `@path` / Gemini
  `@./path.md`) in fenced code (fences are never import-parsed) and expands the placeholder at
  install — **installed output byte-identical to v4.29.0's**.
- `UPGRADE.md`: 4.29.1 row + rung (targets: version-stamp only) **and an amendment to the
  4.29.0 rung** — it had said "merge the import block from `templates/CLAUDE.md`", which after
  this patch would copy the placeholder literally; it now points at the ENABLE.md Step 6
  literal blocks.
- `VERSION` → 4.29.1; `CHANGELOG` (Fixed/Changed + honest residual); `README` row (4.25.0
  trimmed for the 10-cap).
- **Honest residual:** a runtime that auto-loads a nested `AGENTS.md` directly may still
  surface `templates/AGENTS.md` itself — pre-4.29.0 runtime behavior; the patch removes the
  amplification (the memory-stub pull-in), which is the part markdown controls.
- Root `CLAUDE.md`/`GEMINI.md` keep live imports — that is the v4.29.0 feature, dogfooded.

## Memory References

- referenced: bp-before-session-presence (the patch protects its mechanism from a dogfood
  hazard), backlog-real-work-dogfood (cross-vendor dogfooding surfaced the finding)
- created: template-import-bleed-v4291
