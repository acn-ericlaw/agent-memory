# Session (2026-09-10T15:25:43.338Z)

**Agent:** Claude Code

## Graduation day — agent-memory joins the Mercury family

Eric's announcement: "As agent-memory has proven itself in multiple field installations, we
are promoting it to the mercury family of advanced software foundation for Human-AI
collaboration." Target repo `~/sandbox/mercury-go` = `Accenture/mercury-go`, a repurposed
repository (its README, authored today, says the GoLang Mercury port is no longer needed since
the Rust twin engine exists, and that the repo becomes the home for agent-memory). Directives:
mark the graduation date (2026-09-10) and the official status in the docs, white paper and
deck; **keep the version number** (v4.39.2 — no release, no `UPGRADE.md` rung); transfer all
commits to the new repo.

This is the "official enterprise-GitHub step" that the personal-research framing had been
waiting for since 2026-06-18 (`ot-enterprise-github-migration`), so the standing rule "keep
company references out of committed files" is satisfied, not broken: README, docs site,
whitepaper, deck and CHANGELOG now name Accenture and the Mercury family explicitly.

### What changed (docs / collateral)

- **URLs** — every `github.com/acn-ericlaw/agent-memory` → `github.com/Accenture/mercury-go`
  and `acn-ericlaw.github.io/agent-memory` → `accenture.github.io/mercury-go` (mkdocs
  `site_url`/`repo_url`/`repo_name`/social link; `docs/reference/protocol-files.md` 11 links;
  `docs/getting-started.md` + README clone lines now `git clone …/mercury-go agent-memory` so
  the folder name and every `cd agent-memory` stay valid; deck home button). The README's
  `your-org` placeholders — the old hedge — are gone. The memory-lint test fixtures that quote
  the old remote as a benign line were left alone (fixtures, not claims).
- **Official-status wording** — README callout under the intro; `docs/index.md` Status note
  (was "independent research … not an official Accenture product"); mkdocs footer copyright;
  CHANGELOG preamble note (status change, not a release; enabled repos need no action).
- **Whitepaper v1.7** (2026-09-10): Status line in the header (repository + Apache-2.0); §8
  gains a Graduation bullet (promotion changed the status, not the tool — a small confirmation
  of "point it at a repo"); mercury-composable noted as a sibling; References gains #9 (the
  Mercury family: both engines, the wrappers, mercury-go).
- **Deck**: title/kicker carry "Mercury family"; slide 1 gets a `.pill` graduation line; the
  Proof slide gets the graduation paragraph; the closing foot carries the official repo URL;
  home button → `accenture.github.io/mercury-go/`. Title slide verified in the browser pane.
- Not touched on purpose: `VERSION`, `UPGRADE.md`, `templates/`, `MANIFEST.md` (no installed
  artifact references the repo URL, so targets are unaffected); `LICENSE` ("Copyright 2026
  Eric Law") — whether Accenture takes the copyright line is Eric's legal decision, flagged.

### Memory

- `github-origin-git-ops` → superseded by `github-origin-mercury-go` (origin is now
  `Accenture/mercury-go`; acn-ericlaw is the pre-graduation home, older PR links resolve there).
- `ot-enterprise-github-migration` closed (set 2026-06-18, done 2026-09-10).
- New thread `ot-graduation-followups` (ops): enable Pages on mercury-go (gh-pages / root, the
  mercury-composable precedent); fate of `acn-ericlaw/agent-memory` and its site (archive +
  pointer vs GitHub-native transfer — the commit transfer left stars/issues/PRs behind);
  LICENSE copyright holder; repo description/homepage metadata + the unrelated `dev` branch; the
  mercury-composable/mercury thread `ot-agent-memory-attribution-locators`, whose trigger
  ("Eric announces the promotion") has now fired — the official URL is known.

### Transfer (direct to `main` — Eric's one-time exception for the graduation step)

Eric first ruled "since mercury-go is an official repo, a PR is required"; on the rationale
that GitHub cannot compare two unrelated histories (the PR branch would have to carry the
join) and that a squash would collapse 235 commits into one while a rebase would rewrite every
SHA the 23 tags point at, Eric allowed `main` for this step only: "understand the rationale —
for this graduation step, you can use the main branch." **Future changes to the org home go
through PRs.**

Commit transfer, not GitHub repo transfer (the target already existed with its own two
placeholder commits). In `~/sandbox/mercury-go`: fetch agent-memory's `main` + tags +
`gh-pages` from the local clone; reset local `main` to agent-memory's tip so the agent-memory
history is the first-parent line; merge the placeholder `origin/main` with
`--allow-unrelated-histories` (its README + Java `.gitignore` resolved in agent-memory's favor —
the placeholder text survives as history, nothing force-pushed: `origin/main` is an ancestor
of the result, so the push is a fast-forward); push `main`, all 23 tags and `gh-pages`; leave
the unrelated `dev` branch untouched; run `bash .githooks/init.sh` for the fresh clone. The
stale local work branches (`pr-27`, `v4.39.0-merge-scale`) are not transferred. Repo state
pre-transfer: no branch protection, no rulesets, Pages not enabled, license undetected
(LICENSE arrives with the history).

**Archive divergence point (same day, this repository only):** after the transfer completed
and `accenture.github.io/mercury-go/` went live, this pre-graduation repository received a
redirection notice at the top of `README.md` and `docs/index.md` pointing to the official
home. No further content updates land here; the live memory continues in
`Accenture/mercury-go` (whose copy of this log carries the post-transfer outcome).

Later the same day — field installations still track this repository, so Eric deferred the
archive and will notify them: the README banner gained the two-line remote switch
(`git remote set-url origin …/mercury-go.git && git pull --ff-only` — identical SHAs, no
re-clone, targets untouched), and the old site now **auto-forwards** every page to the same path
on the official site (`overrides/main.html` theme override: path-preserving `location.replace`
+ a no-JS meta refresh; the deck forwards via a head script).

## Memory References

- github-origin-git-ops (superseded)
- github-origin-mercury-go (created, tier: working)
- ot-enterprise-github-migration (closed)
- ot-graduation-followups (created, tier: working)
- ot-publish-github-done
- Superseded: github-origin-git-ops → github-origin-mercury-go
