# Session (2026-06-29T18:27:15.000Z)

**Agent:** Claude Code

## Summary

**Doc housekeeping (memory-neutral) — fixed version-list ordering, surfaced by maintainer review.**

- **`UPGRADE.md` version table** was inconsistent: ascending `1.0.0 → 4.21.0`, then a descending
  block `4.27.0 → 4.22.0`, plus local inversions (`4.14.1`/`4.14.0`, `4.16.x`/`4.15.0`). Sorted the
  whole 63-row table ascending by semver. Verified a **pure reorder** (identical line multiset,
  1499 lines, no content added/removed).
- **`UPGRADE.md` rungs** — the `4.26.1 → 4.27.0` rung I added during the v4.27.0 work was misplaced
  *above* `4.26.0 → 4.26.1`; rungs ascend (newest at the bottom), so I swapped them back.
- **`README.md` version table** held 11 rows after the v4.27.0 add; the documented rule is the
  **10 most recent**, so I dropped the oldest (`4.22.2`). Now `4.27.0 → 4.22.3`, descending.

Both regressions trace to the v4.27.0 edit (rows added without re-sorting `UPGRADE` / trimming
`README`). Landing via a **PR** as a validation test of the new What/Why convention. No memory-file
shape change; no `VERSION` bump (pure doc ordering).

## Memory References
(none — memory-neutral doc-ordering fix)
