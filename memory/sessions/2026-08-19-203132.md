# Session (2026-08-19T20:31:32.000Z)

**Agent:** Claude Code

## Summary

Reviewed and shipped the fix for a **field issue from the mercury-composable team**
(`agent-memory-issue-self-knob-false-positive.md`, 2026-08-19) as **v4.34.2 (PATCH)**.

**The issue:** the pre-commit guard's own blocking message prints
`AGENT_MEMORY_SECRET_GUARD=advisory` — and any memory file *documenting* that guidance (their
case: a session log describing a v4.34.0 hook regression test) was itself flagged
`[secret-material]` as a `credential-assignment`. The tool taught a phrase, then blocked
commits that quoted it.

**Review verdict:** confirmed by live reproduction on both runtimes at v4.34.1; the
reporter's root-cause analysis was **code-accurate** (key contains `SECRET`; `advisory`
meets the 8-char value floor; none of `_is_placeholder_value`'s four exemption paths apply —
the enum rule requires a `source|type|mode|mechanism|strategy` key-suffix AND an ALL-CAPS
value), and their proposed fix shape was endorsed as-is, including their correctly-rejected
alternatives.

**The v4.33.2 lesson paid for itself again:** testing the guard's guidance line **verbatim**
before writing fixtures caught that the naive exact-match exemption misses the actual line —
its closing paren rides into the captured value (`advisory)`), exactly the capture behavior
v4.33.2 fixed for backticks. The shipped exemption therefore tolerates trailing `).,`
punctuation on the knob value while staying token-constrained.

**Shipped:**

- `_is_placeholder_value` (py + mjs, at parity): key `AGENT_MEMORY_SECRET_GUARD`
  (case-insensitive) + value `advisory`/`enforcing` (+ trailing `).,`) → exempt. Any other
  value under that key **still flags** (no smuggling envelope); non-echo preserved.
- Mirror suites: one verbatim pinning test each (**50/50 both runtimes**) — the guard's
  printed guidance line, the field repro line, prose `enforcing.`, backticked inline form all
  pass; an opaque runtime-constructed value flags and is never echoed.
- **Secondary question resolved as a doc note (maintainer's conservative option):** AWS's
  canonical doc-example keys still flag **by design** — one contract (redact or *visibly*
  waive via `lint:allow-secret-material`), no invisible built-in whitelist. Recorded in the
  memory-lint `SKILL.md` alongside the knob exemption note. Description unchanged → adapters
  untouched.
- Re-probes: this repo full lint **0/0**; mercury-composable with the fixed script shows
  **0 `[secret-material]`** (their other advisories are their own review cadence — their
  installed copy also shows 0 committed findings, confirming the FP hit staged-only content
  at commit time, which the verbatim fixtures now pin).
- Lockstep: `VERSION`→4.34.2, `CHANGELOG`, `README` (row + 10-cap trim, drops 4.31.0),
  `UPGRADE` (row + `4.34.1→4.34.2` rung — re-copy skill files; drop waivers added for this
  FP class).

## Adversarial pre-ship review — findings & dispositions

2-lens workflow (detector adversarial probing with live runs / release mechanics + claim
sweep). **No blockers** — the reviewer confirmed no smuggling envelope: opaque values under
the knob key, keys merely containing the knob name, `advisory` under *other* credential keys,
`${…:-opaque}` fallbacks, and concatenations all still flag, byte-identical across runtimes;
the fixtures pin the hook's guidance line character-for-character; an end-to-end scratch-repo
hook test passed knob guidance and blocked an opaque value. Six findings, all applied:
(should-fix) the README 10-cap trim deleted the "When you \"AI enable\"…" paragraph opener —
it shared a physical line with the dropped 4.31.0 row via a **pre-existing glue defect from
the v4.34.1 trim**, now repaired properly (blank line + opener restored); (should-fix) the
status line still read v4.34.1 → bumped; (nit) the rung's verify example
`<any-opaque-string>` was itself exempt as a template shape (a literal paste would falsely
fail the verify) → replaced with a concrete opaque dummy; (nit → adopted as code) the
git-config opt-down spelling `agent-memory.secretguard` would re-create the FP class if ever
documented in assignment form → the exemption now covers both spellings with the same value
constraint (fixture line added, both suites; SKILL.md note names both); (nit) CHANGELOG
gained the sibling-convention closing `Targets:` line. Re-validated after fixes: 50/50 both
suites, git-config probe exempts documented values / flags opaque on both runtimes, repo
lint 0/0.

## Memory References

- referenced: secret-redaction-lint-v4330 (the check this refines),
  pre-commit-secret-guard-v4340 (the guard whose own message caused the FP),
  secret-fp-enum-constants-v4331 (the honest-signal detector-fix doctrine this follows),
  secret-fp-backtick-v4332 (the verbatim-fixture lesson that caught the paren variant),
  backlog-real-work-dogfood (field-report flywheel)
- created: secret-fp-self-knob-v4342
