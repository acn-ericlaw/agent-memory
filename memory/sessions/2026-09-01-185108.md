# Session (2026-09-01T18:51:08.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.39.0 (MINOR) — merge-scale memory: threads as files**, from Eric's field
report that `continuity.md` merge conflicts became regular business as team adoption grew
(the hotspots: `## Open Threads` and the `last_session` scalar). Design record:
`docs/DESIGN-merge-scale.md`; realizes part of the `bp-multi-user` Blueprint gap.

**PR #27 reviewed, absorbed, closed (Eric-gated).** Roland Heusser's merge-driver PR
targeted the same pain. Verified against the live branch before judging (three findings,
each reproduced): (1) whole-file `git merge-file --union` silently merged a Tier 2
same-thread edit into two checkbox lines sharing one footer — no conflict, no
Contradiction, invisible to lint's id-keyed footer map; (2) dateless Project State scalars
(`status`) resolved as a direction-dependent silent ours-wins — `never-pick-a-winner` in
mechanical form; (3) `|| true` on merge-file masked the empty-output truncation path.
Closed with the full evidence + credit; absorbed his `[duplicate-state-key]` check
(essentially verbatim, Co-Authored-By trailer) and his merge-rehearsal test pattern.
Lesson: a merge driver that "applies the mechanical rules" must classify conflict hunks or
it over-applies rule 1 to the one case that must reach a human; structure beat machinery
here (Eric's minimalist preference decided the direction — the chosen layout has no
runtime code, no per-clone registration, no partial-coverage mode).

**Shipped:**

- **One thread per file** — `memory/open-threads/thread-<id>.md`; filename = the fact id
  (stable identity, never timestamp-churned), content = exactly the thread's bullet block.
  No index file: the directory is the index (an index line per thread would recreate the
  add/add conflict one line at a time). Same-thread edits still conflict per-file — the
  preserved Tier 2 human gate, pinned by a mutation-checked rehearsal test.
- **`last_session` dropped** (derivable: newest session log + its `**Agent:**` header);
  **archive union-merge** (`memory/archive/*.md merge=union`, git-native, `[both]`/
  `[over-archived]` backstop the resurrect case); **reviews serialized** (REVIEW.md).
- **Scripts:** all three built-in pairs treat thread files as fact surfaces —
  memory-lint (new `[thread-file]`, `[duplicate-id]`, `[duplicate-state-key]`; 67 tests
  ×2, live output byte-identical across runtimes), refresh-metadata (in-place footer
  refresh; 6 ×2), archive-fact (sweep moves the block + deletes the file; 9 ×2).
- **Contract test** `tests/test_thread_layout_merge.sh` — 4 real-git-merge cases:
  different-threads clean, same-thread conflicts, archive union keeps both, and the
  mutation check (stripping the union attribute restores the conflict).
- **Lockstep:** DECAY §1/§12, REVIEW, MERGE, schema, ENABLE 5b/5c/7b + thread-location
  phrases, both PROTOCOL copies (activation reads open-threads; close ritual creates
  thread files; no last_session), all four bootstrap "still scan" notes, templates
  (continuity pointer note, open-threads/.gitkeep, gitattributes union block), MANIFEST
  (open-threads row + 2 semantic steps incl. the mandated protocol-propagation row),
  UPGRADE ladder row + `4.38.1 → 4.39.0` rung, VERSION, CHANGELOG, README (table
  re-trimmed to 10), MIGRATE §D, smoke-tests, example fixture layout notes (real fixtures
  left unfalsified), mkdocs nav.
- **Dogfood migration:** all 23 footered threads extracted verbatim to files; 12 legacy
  footerless items minted ids per the 3.0.0 backfill rule (origin omitted — their true
  origins predate the ids); the vbdi group note and bp-multi-user progress folded into
  their files; stale "### Shipped —" group tombstones dropped (content lives in archive/
  logs); the durable "origin is GitHub" line extracted from the done publish thread into
  Key Decisions (`github-origin-git-ops`); `status` cut 1,779 → 427 chars (schema
  compliance); continuity 387 → ~130 lines.
- Gates: lint 0 errors + cross-runtime byte parity on the live tree; suites 67/6/9 ×2;
  reconcile 38 ×2 + `--check-manifest` OK (19 semantic rows ≤ 4.39.0); both hook/merge
  contract tests PASS.

Deferred by design (recorded in the design doc): millisecond session filenames (BSD date
portability; MERGE.md carries the +1s rename rule instead) and a scalars-only merge
driver (pre-scoped in PR #27's close comment; build only on renewed field pain).

## Memory References

- Referenced: bp-multi-user (partially realized — progress noted in its thread file),
  vision-agent-memory (serves), never-pick-a-winner (drove all three PR #27 findings),
  no-build-step-agent-run (structure-over-machinery decision), ot-protocol-text-propagation
  (the propagation obligation row shipped again), backlog-real-work-dogfood (field-report
  flywheel: adoption pain → design → release)
- Created: merge-scale-v4390 (tier: working — the ship record),
  github-origin-git-ops (tier: working — extracted from ot-publish-github-done),
  ot-dogfood-backfill-optional, ot-examples-mode-b-fixture, ot-rerun-industry-assessment,
  ot-surrealdb-set-aside, ot-test-cursor-aider-migration, ot-test-claude-jsonl-migration,
  ot-test-continue-migration, ot-example-continue-migration, ot-disable-protocol,
  ot-publish-github-done, ot-enterprise-github-migration, ot-claude-md-sync
  (tier: working — legacy threads' ids minted at the thread migration, 3.0.0 backfill rule)
