# Session (2026-06-16T22:18:32.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.5.0 — Kiro support**, after investigating the impact of a target repo on Amazon's
**Kiro IDE** (the maintainer's own project; matters for evangelizing the tool to leadership).
The investigation found Kiro converges on the two open standards this tool already bets on, so
the change is small and additive: a 4th skills adapter target + Kiro in the Mode C detection /
migration path. No special handling for **Kiro Powers**.

## What happened

- **Investigation (web-confirmed against kiro.dev docs).**
  - **Memory layer works as-is:** Kiro auto-reads a root `AGENTS.md` (the open AGENTS.md
    standard — *always included*, no inclusion mode), so the bootstrap needs **no pointer file**.
    A clean external validation of the "AGENTS.md is the hub" bet.
  - **Skills layer already conformant:** Kiro Agent Skills follow the **open Agent Skills
    standard** — workspace skills at `.kiro/skills/<name>/SKILL.md`, frontmatter `name` +
    `description` (≤1024 chars) — i.e. the same shape as our neutral skill and our Claude adapter.
  - **Kiro Powers:** partner-published bundles (MCP servers + steering + validation hooks),
    one-click install — *not* a per-repo authored artifact. A Power that "consumes skills"
    consumes open-standard skills, which ours already are. **The tool never emits a Power → no
    special treatment.** Confirmed the maintainer's hypothesis.

- **Shipped (additive MINOR, v4.4.0 → v4.5.0).**
  - `SKILLS.md`: added the **Kiro** adapter to the recipe (`.kiro/skills/<name>/SKILL.md`,
    identical to the Claude adapter); `sync skill adapters` now writes **four** adapters.
  - `AGENTS.md` (root + template): adapter list now includes `.kiro/skills/`; root migrated-
    vendors list adds Kiro.
  - `MIGRATE.md`: **Kiro added to the Detection Table** + a per-vendor **Kiro** protocol
    (`.kiro/steering/*.md` → `instructions.md`; `.kiro/skills/` → promote to `agent-skills/`;
    `.kiro/specs/` → preserve verbatim under `legacy/kiro/specs/`, never auto-flatten). B2 +
    Claude-skills sections + the "vendor dirs serve double duty" note now name `.kiro/`.
  - `ENABLE.md`: Step 5h recipe pointer, Step 8 verify, the report template, and the scope list
    all now include the 4th (`.kiro/skills/`) adapter.
  - `UPGRADE.md`: standing **Skills adapter check** scans `.kiro/skills/<name>/SKILL.md`;
    version table row + a new **4.4.0 → 4.5.0** rung.
  - `templates/.gitignore`: comment now names `.kiro/skills/` among adapters — **no entry
    change** (`.kiro/` was already in the v3.1.0 managed block; it doubles as the adapter target).
  - `docs/DESIGN-skills-layer.md`: status line + recipe table (Kiro row) + Option A dirs.
  - `VERSION` → 4.5.0; `README` (version table + detection table + skills-promoted bullet);
    `CHANGELOG` (v4.5.0 entry). `DECAY.md` / `REVIEW.md` unchanged.

- **Dogfood.** Regenerated the `hello-world` Kiro adapter locally at
  `.kiro/skills/hello-world/SKILL.md` (gitignored, per-machine) — proves the recipe end-to-end;
  confirmed `.kiro/` is git-ignored so the adapter won't be committed.

## Notes / open items

- The whitepaper (`docs/agent-memory-whitepaper.md`, v1.1) + deck describe **v4.1.1** — left
  as point-in-time artifacts; not retrofitted to v4.5.0 (separate decision if leadership wants
  refreshed collateral).
- Historical UPGRADE/README rows + the 4.0.0→4.1.0 rung still read "Claude/Gemini/Cursor" by
  design (they describe that era; the live recipe is `SKILLS.md` + Step 5h, now four adapters).
- Standing thread `ot-realworld-v411-findings` still open (teammate's client-enablement report).

## Memory References

- Referenced: vision-agent-memory, never-pick-a-winner, never-delete-vendor-files,
  target-repo-scope-only, upgrades-additive, no-build-step-agent-run
- Created: kiro-adapter-v450 (tier: working)
