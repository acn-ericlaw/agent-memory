# Session (2026-06-20T18:06:05.000Z)

**Agent:** Claude Code
**User:** Post-review refinements to the v4.14.0 ADR ledger (committed `3f251d0`, not yet pushed).

## What We Did

Two maintainer-review refinements to the just-committed ADR ledger, folded into v4.14.0
(unreleased — no version bump):

1. **ADR back-pointer is now human-visible.** Moved the continuity→ADR cross-link out of the
   hidden HTML footer (`| adr: ADR-NNNN`) and into the **visible invariant title** —
   e.g. `Target-repo scope only (ADR-0001)`. The footer field was invisible in any rendered/GitHub
   view, defeating the human-governance purpose. Applied to all 5 live invariants + the archived
   `no-code-markdown-only` (ADR-0004); the `adr:` footer field is **removed** (no redundancy). The
   `formalizes:` link in `docs/ADR.md` + the visible tag keep the trace grep-resolvable both ways.
2. **A fresh agent must not extra-scan the ADR.** Added explicit guidance at the point of exposure
   (the `## Architectural Invariants` note in `continuity.md`, which IS read every session) and in
   `AGENTS.md` (root + template) + `.agent/schema.md`: the `(ADR-NNNN)` tag is a **pointer for
   humans**, the invariant text in `continuity.md` is **authoritative**, and `docs/ADR.md` is read
   **on demand only** — never opened to orient. Reinforces the existing "not in the per-session read
   path" rule, now also guarding against a tag-triggered scan.

Reconciled the documented convention everywhere (all unpushed v4.14.0): `docs/ADR.md` header,
`.agent/schema.md`, `AGENTS.md` (root + template), `CHANGELOG`, `README` + `UPGRADE` table rows,
and the `UPGRADE` 4.14.0 rung. `memory-lint` re-run clean (0/0); no `| adr:` footer fields remain;
all 6 `formalizes:` ids resolve; visible `(ADR-NNNN)` tags present on all 6.

## Decisions Made

- ADR cross-link representation = **visible `(ADR-NNNN)` tag in the invariant title**, not a hidden
  footer field (human-governance discoverability).
- The tag is a **human pointer, not an agent read-cue**; the invariant text stays authoritative;
  `docs/ADR.md` is on-demand only — explicit anti-scan guidance added where the agent sees the tag.
- Folded into **v4.14.0** (unreleased, unpushed) rather than a new PATCH — same feature's final form.

## Context for Next Session

v4.14.0 now reflects the final ADR design. Still **not pushed** (origin = GitHub
`acn-ericlaw/agent-memory`). Awaiting the maintainer's go to `git push origin main`. This was a
documentation/representation refinement — no fact truth-state change.

## Memory References
- Referenced: adr-ledger-v4140, target-repo-scope-only, never-delete-vendor-files, never-pick-a-winner,
  upgrades-additive, no-build-step-agent-run, no-code-markdown-only
