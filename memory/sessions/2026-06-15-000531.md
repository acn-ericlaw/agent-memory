# Session (2026-06-15T00:05:31.000Z)

**Agent:** Claude Code
**User:** Studied an independently-drafted cognitive framework, integrated it as the VBDI design, defined + confirmed the project Vision, derived the Blueprint.

## What We Did

- **Assessed** a generalized cognitive framework the user drafted with another AI (no
  knowledge of this tool), preserved at `docs/agent-cognitive-framework.md`. Verdict:
  strong fit — its *cognitive loop* sits on top of our *memory loop* (which it confirms we
  already fully implement). Net-new is only two primitives, **Vision** and **Blueprint**;
  everything else maps to existing artifacts (Current State=continuity, Design=Key
  Decisions/Invariants, Feedback=REVIEW). Integrate by *mapping*, not rebuilding.
- **Resolved terminology:** the framework's "Mission" (= current reality) → **Current
  State** (avoids the "mission = purpose" collision).
- **Designed the integration:** `docs/DESIGN-vbdi-lifecycle.md` — the loop, the mapping,
  the 2 lightweight artifacts, how Vision/Blueprint plug into decay/§9/§10/P2/P4/P5,
  determinism, hard parts, open questions.
- **Defined the Vision** (`memory/vision.md`, maintainer-articulated + confirmed):
  lightweight, vendor-neutral AI memory for human–AI collaboration → deterministic
  delivery, near-zero drift; multi-contributor on one enabled repo; generic cognitive
  memory + feedback loop spanning greenfield *and* brownfield.
- **Derived + locked the Blueprint** — 4 typed `(blueprint)` Open Threads (gaps to the
  Vision): build the VBDI loop, greenfield path, altitude drift-detection, multi-user
  hardening. The backward memory layer is *not* a gap (done); all gaps are forward.

## Decisions Made

- Adopt the cognitive framework as the conceptual backbone for VBDI, integrated the
  lightweight way (map don't duplicate; only Vision file + Blueprint typed-thread are new).
- **No version change this commit** — this is design + dogfood (the tool's *own* Vision/
  Blueprint), not shipping the VBDI feature into templates/protocol. That ships under the
  `bp-vbdi-loop` gap; **3.8.0 vs 4.0.0** decided then.
- Vision = `core` but invariant-verified (a vision can go stale).

## Context for Next Session

**First VBDI loop bootstrapped on the tool itself** — Current State (`continuity.md`,
v3.7.0) → Vision (`memory/vision.md`) → Blueprint (4 `bp-*` threads) → Design
(`docs/DESIGN-vbdi-lifecycle.md`) → Implementation (next) → Feedback (REVIEW). The
test-drive validated the loop feels light. **Next:** implement the VBDI feature into the
templates/protocol (the `bp-vbdi-loop` gap) — settle 3.8.0 vs 4.0.0 and the design's other
open questions (ENABLE seeds a Vision stub? Blueprint shape?) at that point. Lower-priority
loose ends unchanged (run the smoke tests; aged-data validation of archive/sweep +
invariant-verify; backfill `origin` on v3.3–3.6 facts at next review; re-run the assessment).

## Memory References
- Referenced: vbdi-lifecycle-direction, no-code-markdown-only, upgrades-additive
- Created: vision-agent-memory, bp-vbdi-loop, bp-greenfield, bp-altitude-drift, bp-multi-user
- Reactivated: (none)
- Superseded: (none)
