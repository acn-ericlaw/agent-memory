# Session (2026-08-14T02:17:12.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.34.0 (MINOR) — pre-commit secret guard: prevention on both surfaces (memory +
config files).**

**The gap, in two parts.** (1) Timing: the ritual redaction rule fires when an *agent* writes;
the CI floor fires on *push* — by which time the remote already has the secret and redaction
is not un-leaking. A human teammate committing directly met no automated check at the one
moment prevention is possible. (2) Surface — the maintainer's follow-up detail reframed the
v4.33.x incident: its *origin* was never a memory file. The credentials entered the repo
inside a **Postman JSON and an OpenShift YAML** (project root and `src/`), and the session-log
leak was downstream contamination when a dry-run rendered them. Guarding memory/ alone treats
the symptom.

**Investigated before building** (maintainer asked for viability): ran the current detector
over **all 661 committed json/yml/yaml/properties files across both mercury repos** — 6
findings, 0 real secrets, all in four deterministic FP classes. Verdict: viable, with the
placeholder rules tuned first. Maintainer scoped via decision prompts: fold into the
uncommitted v4.34.0; extend the CI floor too; add a committed ignore file; widest extension
set (+ `.env*`, `.toml`, `.ini`).

**Shipped:**

- **`.githooks/pre-commit`** scans the **staged content** (index via `git show :path`; only
  what THIS commit stages) of `memory/**.md` (full profile) and of config files
  (`.json`/`.yml`/`.yaml`/`.properties`/`.toml`/`.ini`/`.env*` — **credential-class only**:
  config files legitimately carry contact emails and paths). **Enforcing by default** —
  findings block the commit (maintainer decision, made mid-release on reviewing the whitepaper
  wording: "the pre-commit is not an opt-in. We should enforce it" — and the analysis agrees:
  with an advisory default, an inattentive commit + push still lands the secret on the remote,
  the exact incident profile; blocking at pre-commit is the only default that stops it, secrets
  being the one category with irreversible after-the-fact cost). The `AGENT_MEMORY_SECRET_GUARD`
  env knob (or the `agent-memory.secretguard` git-config key) opts the guard down to advisory;
  `--no-verify` one-off bypass; merge commits skipped; python3→node fallback;
  never-initialized clones fall through to CI. The CI floor stays advisory-by-default
  (`AGENT_MEMORY_STRICT=1` gates).
- **`memory-lint --scan-files FILE...`** (both runtimes): the standalone credential-class mode
  behind the hook and the CI wrappers — paths as given, exit 1 on findings, values never
  echoed.
- **Changed-config scan on all three forge CI wrappers** — the push-time sibling, same
  advisory/STRICT semantics, honoring the same exemptions.
- **`.agent/secret-scan-ignore`** (committed, human-audited stub installed at enable/upgrade):
  shell-glob exemptions for formats that can't carry the inline waiver (JSON has no comments; a
  `.properties` same-line comment corrupts the value). Config files only — never `memory/`.
- **Probe-tuned detector refinements** (6 → 0 across the 661-file corpus, both repos re-scanned
  clean through the real CLI in both runtimes): single-brace `{PLACEHOLDER}` + GitHub-Actions
  `${{ … }}` template forms; `demo`/`test` placeholder words; a template default that is itself
  provably a placeholder is safe while credential-shaped fallbacks keep flagging (v4.33.4 rule
  preserved — its fixture still passes); dotted lowercase route refs exempt from the
  Authorization check; `;` joins the value delimiters (JAAS terminators); and a **Postman
  split-pair pattern** (`"key"`/`"value"`) for the literal incident artifact class. Mirror
  suites 46 → **49 per runtime**, byte parity re-verified.

**Verification:** 49/49 both runtimes; hook exercised across fourteen scratch-repo paths
(default-block / advisory opt-down via env and via git-config / waiver line / ignore-file
exemption / clean config incl. route + demo-default / index-vs-worktree / no-staged fast path /
node-only fallback / `--no-verify` / `.env` staged / Postman JSON / memory-md regression); all
three CI YAMLs machine-validated + the GH scan block simulated live against this repo's own
changed files; dogfood lint 0 errors. Best verification anecdote: the check flagged **this
release's own memory records** — the literal env-knob assignment form in continuity + this log
matched the credential-assignment pattern — and the fix was rewording the docs to break the
key/value adjacency, not waiving: the guard polices its own release notes.

**Doctrine refinement recorded:** the trigger layer is now "advisory by default; the
pre-commit secret guard alone *enforces*" (was "advisory, never blocks") — a deliberate,
scoped exception, maintainer-decided. DESIGN-ritual-triggers §7.3 had recommended the
pre-commit half at v4.19.0 — secrets' irreversible after-the-fact cost is what finally
justified both its existence and its enforcing default.

**Lockstep:** hook + `.githooks/README.md` + `init.sh`, `templates/.agent/secret-scan-ignore`
(new), `memory-lint` runtimes + tests + SKILL.md body (frontmatter unchanged → adapters
untouched), three forge CI files, `AGENTS.md` ×2, `ENABLE.md` (5e stub + Step 6 hooks/CI +
chmod), DESIGN amendment, whitepaper (×2 sections + header: doc v1.3, describes v4.34.0, dated
2026-08-14, draft-status line retired — the paper is stable), docs-site reliability +
built-in-skills pages, the
presentation deck (version stamps + triggers card now lead with the enforcing secret guard),
`VERSION` → 4.34.0, `CHANGELOG`, `README` (row + 10-cap trim, drops 4.29.1), `UPGRADE` (row +
`4.33.4 → 4.34.0` rung).

## Memory References

- referenced: secret-redaction-lint-v4330 (the DLP arc this completes — write-time and
  push-time layers existed; this adds commit-time on both surfaces), secret-review-hardening-v4333
  (the `AGENT_MEMORY_STRICT` knob and script-owned-result wrapper pattern this reuses),
  secret-template-default-v4334 (its non-empty-default rule is preserved by the new
  placeholder-fallback refinement — verbatim fixture still passing)
- created: pre-commit-secret-guard-v4340
