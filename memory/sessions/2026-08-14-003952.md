# Session (2026-08-14T00:39:52.000Z)

**Agent:** GitHub Copilot

## Summary

Shipped **v4.33.4 (PATCH) — `[secret-material]`: reject non-empty template defaults.**

Claude Code's v4.33.3 live-target fix correctly restored empty-default and dotted environment
references, but post-release review found that accepting every brace-delimited `${...}` form
also exempted literal fallbacks that may themselves contain rendered secrets. The maintainer
confirmed the intended boundary: `${NAME}` and `${NAME:}` are safe references; a non-empty
fallback must be rejected.

Both runtimes now recognize only bare or dotted reference names with an optional trailing
colon. The exact fallback bypass joined the mirrored negative fixtures, while the live-target
empty-default and dotted forms remain covered as safe. Python and Node suites pass 46 tests
each and produce byte-identical dogfood output. Lockstep: scripts/tests, `VERSION`, `CHANGELOG`,
`README`, `UPGRADE`, and continuity. SKILL.md description is unchanged, so adapters remain
untouched.

## Memory References

- referenced: secret-review-hardening-v4333, secret-redaction-lint-v4330,
  vision-agent-memory
- created: secret-template-default-v4334
