# Session (2026-06-24T20:15:00.000Z)

**Agent:** Claude Code

**Shipped v4.20.2 (PATCH) — Windows line-ending hardening.** Added a **`.gitattributes`** (root +
`templates/`) pinning `*.sh` + `.githooks/*` to **LF**.

**Trigger.** A Copilot (Gemini) Windows-feasibility check correctly said the bootstrap works on Windows
via Git Bash/WSL (Git for Windows runs `.githooks/post-commit` through its bundled bash; `init.sh` falls
back bash→node→python). **But it missed the CRLF trap:** with no `.gitattributes`, a Windows clone
(`core.autocrlf=true`, the Git-for-Windows default) rewrites `*.sh` + `.githooks/*` to CRLF on checkout,
and bash then fails — `bad interpreter: /usr/bin/env bash^M` — silently disabling the hook + `init.sh`.
Verified neither repo had a `.gitattributes`.

**Fix.** `*.sh text eol=lf` + `.githooks/* text eol=lf`. Installed/merged into targets additively (new
ENABLE **Step 7b** + a `4.20.1 → 4.20.2` rung), same discipline as the `.gitignore` managed block. Existing
scripts are already LF (authored on macOS), so it's purely forward-looking for Windows checkouts; no
content churn.

**Lockstep.** `.gitattributes` (root + template, new), `ENABLE.md` (Step 7b), `UPGRADE.md` (rung + table),
`CHANGELOG`, `README`, `docs/DESIGN-ritual-triggers.md` (Windows note), `VERSION`→4.20.2. Added
`.gitattributes` to `~/sandbox/simple-proxy` too. `AGENTS.md`/`DECAY`/`REVIEW`/`SKILLS` unchanged.

**Net.** The bootstrap + ritual triggers are now **robust on Windows (Git Bash / WSL)**, not
luck-of-the-default — closing the last cross-platform gap in the Copilot arc.

## Memory References

- Referenced: `ritual-init-v4200` (the fresh-clone init this hardens for Windows), `vision-agent-memory`,
  `backlog-real-work-dogfood` (the Copilot Windows-feasibility check that surfaced it)
