# Session (2026-07-06T19:03:44.000Z)

**Agent:** Claude Code
**Lightweight:** doc-only convention fix; no `continuity.md` facts touched.

## Summary

Actioned a **third** `mercury-composable` field report
(`/tmp/agent-memory-coauthor-trailer-note.md`): `Co-Authored-By` trailer duplication. The v4.28.0
attribution guidance assumed the agent *fully authors* the commit message, but the agent co-authors it
**with its harness**, which often injects its own (model-version) `Co-Authored-By`. A conscientious
agent then appended a second stable-name trailer → duplicate co-authors for one collaborator;
squash-merges compounded it.

**Verified empirically first.** My own agent-memory commits this session carry a single `Claude Code`
trailer (this harness didn't inject one). But mercury's history confirmed the bug and worse: commit
`67102a87` has `Claude Code` + `Claude Opus 4.8` (same email); `112cfd94` mixes Opus 4.8 + Sonnet 5;
`aba225eb` accumulated **55** trailer lines. Key insight: identity must key on **email**, not display
name — `Claude Code` / `Claude Opus 4.8` / `Gemini CLI` are one collaborator at one `noreply@…`.

**Reviewed, then implemented option (a)** — doc fix only; enforcement deferred (user's call):
- `AGENTS.md` (root + `templates/`): reframed the mental model (co-author *with* the harness → treat
  its message as the base and **reconcile**, don't blindly append); stated the invariant (**at most
  one `Co-Authored-By` per collaborator, matched on email**); added a 3-branch resolution tree
  (harness-unsuppressable → that's your one; you-control → exactly one stable-name + drop the dup;
  never both) and forge-aware squash guidance (canonical trailer once, in the PR footer).
- PR-template footer comment (root + `templates/`) + `docs/getting-started.md`: same one-per-
  collaborator-by-email rule.
- **Deferred** (deliberate): an auto-dedup `commit-msg` **hook** would *rewrite* commit messages — a
  departure from the tool's never-mutate-your-commits stance. If the doc fix doesn't hold in the
  field, the tool-consistent next step is an *observational* `memory-lint` advisory (flag >1 AI
  trailer at one email), not a mutating hook.

**Lockstep:** VERSION → 4.28.4; CHANGELOG; README (trimmed to 10); UPGRADE.md capability row +
`4.28.3 → 4.28.4` rung. memory-lint tests still 36/36 both runtimes (doc-only change). Not committed.

## Memory References
(none — no `continuity.md` facts referenced or created. Third mercury field report in the v4.28.x
arc, after v4.28.2 (fact count) and v4.28.3 (line message). See personal memory on the enforcement
boundary — prefer observational over mutating.)
