# Session (2026-06-15T00:28:37.000Z)

**Agent:** Claude Code
**User:** Ship the VBDI feature into the templates/protocol + the AI-enable sequence, with a bootstrap for upgrading existing repos (which have no Vision/Blueprint).

## What We Did

Shipped the **forward cognitive layer (VBDI) as v4.0.0** — the `bp-vbdi-loop` Blueprint
gap, turned into real artifacts. The cognitive loop (Current State → Vision → Blueprint →
Design → Implementation → Feedback) now lives in the protocol on top of the memory
substrate. Implemented per `docs/DESIGN-vbdi-lifecycle.md`, the lightweight way (only
Vision + Blueprint are new; the rest maps to existing artifacts).

- **New:** `templates/memory/vision.md` (the north star). **Blueprint** = typed
  `(blueprint)` Open Threads (`→ serves: <vision-id>`); no new file. **Altitude trace** =
  `id` links Implementation→Design→Blueprint→Vision.
- **Protocol:** `DECAY.md` (§10 altitude-drift bullet + new §12 "The forward layer"),
  `REVIEW.md` (Vision in invariant-verify step 6 + altitude drift in the backstop),
  `.agent/schema.md` (vision.md + Blueprint/`serves:`), `AGENTS.md` ×2 (a "cognitive loop"
  section + Vision in the session read-list), `ENABLE.md` (Step 5g generate + Step 8
  verify + Step 9 report).
- **Bootstrap (the upgrade concern):** both fresh-enable and the `UPGRADE.md` 3.7.0→4.0.0
  rung create a **⚠️ DRAFT** `memory/vision.md` — pre-filling **only** the current-state
  context, leaving target/success/non-goals as prompts — and raise a `(vision-bootstrap)`
  human gate. The Vision is **never fabricated**; the Blueprint waits for the confirmed
  target. Until confirmed, drift-detection is advisory.
- `VERSION`→4.0.0; `UPGRADE.md` rung + version tables; `README`/`CHANGELOG`.
- **Vendor-neutrality fix (maintainer caught it in review).** Root `CLAUDE.md` was a thick
  119-line guide while `GEMINI.md` was a 10-line pointer — and the Architecture map lived
  *only* in `CLAUDE.md` (Claude-only), a drift from the vendor-neutral Vision. Thinned
  `CLAUDE.md` to a pointer; moved the project guide + architecture into vendor-neutral
  `memory/instructions.md`. (The tool now dogfoods its own thin-pointer design.)

## Decisions Made

- **4.0.0** (maintainer-locked) — marks the new forward layer (counterpart to 3.0.0's
  backward layer), though it's technically additive/backward-compatible.
- **Context-only bootstrap** (maintainer-locked) — pre-fill the safe current-state, never
  infer the aspiration; gate it for the human. Same principle as "User Preferences: never infer."
- The tool dogfoods its own (already-confirmed) Vision + Blueprint; this session closes
  `bp-vbdi-loop` and `bp-altitude-drift`.

## Context for Next Session

VBDI v4.0.0 is shipped, **not committed yet** — commit + push (fast-forward). **Natural
next test-drive:** upgrade `~/sandbox/simple-proxy` and `~/sandbox/mercury-composable`
3.7.0→4.0.0 — this **exercises the Vision-bootstrap rung** end-to-end (each should get a
DRAFT Vision + `(vision-bootstrap)` gate, no fabrication). Remaining Blueprint gaps:
`bp-greenfield` (a dedicated greenfield enable flow — the Vision substrate now exists, but
no distinct path) and `bp-multi-user` (harden simultaneous contributors). Lower-priority
loose ends unchanged. Worth re-running the industry-alignment assessment now that the
forward layer exists.

## Memory References
- Referenced: vbdi-lifecycle-direction, vision-agent-memory, bp-vbdi-loop, bp-altitude-drift, no-code-markdown-only, upgrades-additive
- Created: vbdi-shipped-v400
- Reactivated: (none)
- Superseded: (none)
