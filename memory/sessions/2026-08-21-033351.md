# Session (2026-08-21T03:33:51.458Z)

**Agent:** Codex

## Summary

Resolved the protocol-pointer commit's rebase conflicts after `main` advanced through
v4.35.0 target-state reconcile and v4.36.0 composable hook dispatchers. Renumbered the
pending release to v4.37.0 and integrated the relocation with both upstream architectures.

The reconciler now orders `memory/PROTOCOL.md` before the root shim, labels hook
preservation and protocol relocation as PRE-APPLY work, refuses every write until the
protocol/shim boundary converges, and requires `--pre-apply-complete` after the listed
inspection, preservation, provenance, and hash checks. Hook drift is confirmable after
local behavior is extracted; protocol/shim drift must be resolved before apply. Enforcement
follows live activation state, so current stamps cannot bypass root recovery and fresh custom
protocols or current hook/managed-fragment drift still require confirmation.

## Verification

- Python and Node reconcile suites: 32/32 each.
- Python and Node manifest checks: 43 sources and 16 semantic steps, clean.
- Python and Node memory-lint: zero errors; existing advisory metadata warnings remain.
- Memory-lint contract suites: 50/50 each.
- Hook dispatcher contract and shell syntax: pass.
- `mkdocs build --strict`: pass.
- Maintainer-run v4.36.0 → v4.37.0 target-Git walkthrough: committed-byte recovery,
  zero-write refusal, protocol-before-shim convergence, customized-hook survival,
  confirmed apply, and repeated zero-change apply all pass.
- Final fresh adversarial, quality, and security reviews: clean.
- Textual conflict-marker scan and staged/unstaged whitespace checks: clean.

Git still reports the 13 originally conflicted files as unmerged because enterprise policy
prevents Codex from writing the index or continuing the rebase; the maintainer must mark the
resolved files and run `git rebase --continue`.

## Memory References

- protocol-pointer-enterprise-activation
- bp-reconcile-target-state
- git-hook-fragment-dispatch
