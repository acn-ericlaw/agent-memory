# Session (2026-06-28T03:25:39.000Z)

**Agent:** Claude Code

## Summary

Shipped **v4.23.0 (MINOR)** — added **`harvest-knowledge`**, a 5th built-in skill, surfaced while
test-driving the mercury-composable upgrade.

**The gap:** the curious harvest (v4.22.0, `ENABLE.md` Step 4b) seeds memory from the team's docs **once**,
at enable. A *living* repo (mercury's exact case) keeps adding ADRs / design specs / decision-log entries,
with no recurring way to fold them into memory. The v4.22.0 work had bolted a one-time "re-harvest" onto a
Mode B **upgrade rung** — the wrong home.

**Maintainer's decision:** leave the enable-time harvest a **fresh-enable (Mode A) event**, and make the
recurring need a **skill**.

**Built:** `agent-skills/harvest-knowledge/SKILL.md` (`provenance: agent-memory-builtin`, no-code/agent-run).
Re-scans human-authored docs (recursive `docs/` trees + root sweep — same net as Step 4b) and distills
**durable** facts into the **neutral, shared** `memory/` **additively**: map-don't-mirror; check-existing
-first so a re-run doesn't duplicate; conflicts → a `Contradiction` thread (`never-pick-a-winner`); genuine
replacements → supersession; budget-with-disclosure → a `(knowledge-harvest)` Open Thread.

**Is it `/init`?** Answered no, and designed against it: a vendor `/init` does deep **code** analysis and
(re)writes/overwrites a **vendor** steering file; `harvest-knowledge` reads **human-authored knowledge**,
writes **neutral shared memory**, and is **additive + repeatable**. Borrow `/init`'s analysis muscle, but
the output goes to `memory/`, never a vendor file. (Its enable-time cousin Step 4b + the v4.22.0
deep-analysis option are the `/init`-adjacent paths; this is the recurring, knowledge-doc-focused sibling.)

**Wiring (lockstep):** `ENABLE.md` §5i ("five" built-ins) + Step 8 verify list; `upgrades-additive`
invariant + ADR-0005 (added to the managed-built-ins list); removed the inline Mode B re-harvest from the
`4.21.0→4.22.0` rung (→ the skill); README (repo-structure tree + version table +4.23.0 / −4.20.0);
`CHANGELOG`; `UPGRADE` (table row + `4.22.4→4.23.0` rung); `VERSION`→4.23.0. Adapters synced — 6 skills →
36 adapters; `harvest-knowledge` 6/6, description quote-free (TOML/MDC-safe). `memory-lint` OK.

**Note:** not yet applied to mercury — mercury was upgraded to 4.22.4 (uncommitted, left for the user); a
later 4.22.4→4.23.0 upgrade would install this skill there too.

## Memory References
- Created: harvest-knowledge-skill-v4230
- Referenced: knowledge-harvest-curious-v4220 (the enable-time harvest this complements); vision-agent-memory (serves)
