# Session (2026-06-20T17:41:56.000Z)

**Agent:** Claude Code
**User:** Polish — introduce the Architecture Decision Record (ADR) concept; examine VBDI to organize key architectural principles as ADR.

## What We Did

Shipped **v4.14.0 (MINOR)** — an **optional, human-facing Architecture Decision Record log**
at the VBDI **Design** altitude. Examined the VBDI framework (`docs/agent-cognitive-framework.md`,
`docs/DESIGN-vbdi-lifecycle.md` §4) and confirmed ADRs are the missing *durable, atomic, dated,
status-tracked, never-deleted* form of the Design altitude — which until now was realized only by
`continuity.md`'s `## Architectural Invariants`/`## Key Decisions` and the long-form
`docs/DESIGN-*.md`. Built `docs/ADR.md` (single-file decision log, newest-first), **seeded with
the 5 standing Architectural Invariants + the superseded `no-code-markdown-only` decision**
(ADR-0001…0006), each carrying Status · Date · Abstract · Rationale-with-consequences and
`formalizes:`/`serves:` trace links.

> **Maintainer review (same session):** spotted that `no-code-markdown-only` — the prior decision
> ADR-0006 (`no-build-step-agent-run`) replaced — was a real ADR that was never recorded. Inserted it
> as **ADR-0004** (`Status: Superseded by ADR-0006`, the canonical term — a successor exists, so not
> merely "Deprecated"; flagged for the maintainer's re-review), renumbered `upgrades-additive` 0004→0005
> and `no-build-step-agent-run` 0005→0006, updated the two live footers + the archived fact's `adr:`
> cross-link. `memory-lint` re-run clean.

Design forks settled with the maintainer before building (plan-gated):
- **Terminology:** corrected to "Architecture **Decision** Record" (maintainer flagged memory drift
  from "Design"); status vocab uses canonical `Superseded`/**`Deprecated`** (not "retired").
- **Shape:** single `docs/ADR.md`, newest-on-top (the decision-log variant — lightest, no tooling
  dependency, fits no-code/agent-as-runtime). Noted to the maintainer this is the minority vs. the
  file-per-record `adr-tools`/MADR convention, accepted deliberately.
- **Rationale captures consequences/trade-offs** (folds in the canonical "Consequences" section).
- **Install scope:** tool-only + documented as an **optional** convention; ENABLE/UPGRADE do **not**
  auto-create a file in targets. **Seed set:** the 5 invariants only (minimalist — the maintainer is
  "obsessed with minimalist," ADR is for human IT governance, keep it bare-minimum + token-efficient).
- **Token efficiency (decisive constraint):** the ADR log is human-facing, read **on demand**, and
  **never** added to the per-session agent read path — zero default token cost, like `docs/DESIGN-*.md`.
- **Map, don't duplicate:** `continuity.md` keeps the live *what* (with `id`); the ADR carries the
  durable *why*; they cross-link by `id` (`formalizes:` ↔ `adr:`). No constraint text restated.

Lockstep edits (additive): `docs/ADR.md` (new); `.agent/schema.md` (optional section);
`AGENTS.md` + `templates/AGENTS.md` (one on-demand clause in the VBDI section, explicitly not in the
read path); `DECAY.md` §12 (the *Design* primitive + supersede/deprecate-never-delete lifecycle);
`docs/DESIGN-vbdi-lifecycle.md` §4 Design row; `continuity.md` (5 invariant footers get `adr: ADR-000N`);
`README.md` (version row + Repo Structure `docs/` entry); `VERSION`→4.14.0; `UPGRADE.md` (rung + table);
`CHANGELOG.md`. `REVIEW.md`/skills/scripts untouched.

## Decisions Made

- ADR = the durable, never-deleted record of the VBDI **Design** altitude; lifecycle mirrors `DECAY.md` §9.
- Single-file `docs/ADR.md`, newest-first; status `Proposed → Accepted → Superseded/Deprecated`.
- Optional + not auto-installed; on-demand only; never in the per-session read path (token efficiency).
- Map-don't-duplicate cross-linking (`formalizes:` ↔ `adr:`); no `memory-lint` ADR check this cut.

## Verification

- `memory-lint` (Python): **0 errors, 0 warnings** — the new `adr:` footer fields parse cleanly.
- Trace check: all 5 `formalizes:` ids in `docs/ADR.md` resolve to real continuity invariant ids;
  each invariant footer carries the matching `adr: ADR-000N`.
- No-drift check: ADR is **not** in the AGENTS.md Before-session read list (only the on-demand note).
- Lockstep: `VERSION`=4.14.0; UPGRADE rung + table row; CHANGELOG entry; schema section all present.

## Context for Next Session

Additive MINOR, dogfooded on this repo only — not yet validated on a real target (no target gets a
`docs/ADR.md` unless its team adopts one). A possible future increment: a `memory-lint` check that ADR
`formalizes:`/supersession links resolve (intentionally out of scope here to avoid bloat). Commit pending
(human-initiated).

## Memory References
- Referenced: vision-agent-memory, no-build-step-agent-run, upgrades-additive, never-pick-a-winner,
  never-delete-vendor-files, target-repo-scope-only, vbdi-lifecycle-direction
- Created: adr-ledger-v4140
