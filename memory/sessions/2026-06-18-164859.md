# Session (2026-06-18T16:48:59.000Z)

**Agent:** Claude Code

## Summary

Reviewed an external brainstorming artifact — the **Agent Interchange Format (AIF)** draft
(architecture paper + snapshot/critique specs) — for fit with agent-memory. Verdict: the core
insight (**fresh-context second opinion at milestones/risk points**) is strong and on-brand,
but should be **adopted as the tool's own feature, not imported as a "format/protocol/standard"**
(that branding cuts against the lightweight Vision non-goals). Maintainer confirmed the
direction: **fold into the skills layer + VBDI**, not a standalone spec. Drafted the design doc
and registered the Blueprint gap. No implementation yet — design is under review (human gate).

## What happened

- **Grounded the review** against vision, invariants, the skills layer, and the v4.8/v4.9
  decay-miscount episode before opining (not a rubber stamp).
- **Key finding — net-new surface is tiny.** ~70% of the AIF snapshot is already derivable from
  `continuity.md` + session logs. Net-new is three things: (1) the **security advisory** as a
  trust-boundary event on *export* (the clearest new value — strengthens `target-repo-scope-only`
  from "what the tool touches" to "what the human exports"); (2) the **fresh-context handoff
  ritual** as a named skill; (3) the **critique artifact shape**. Everything else is reuse.
- **The v4.8/v4.9 episode is the design's anchor in both directions** — it proves fresh review
  surfaces what the in-session agent misses *and* that the reviewer can be confidently wrong
  (Copilot over-archived). Load-bearing rule baked into the design: **critique is advisory,
  gated by deterministic checks (`memory-lint`/build-tests) + human**; `apply-critique` never
  auto-merges. We already ran this pattern ad-hoc, learned where it breaks, and built the guard.
- **Wrote** `docs/DESIGN-fresh-context-review.md` (sibling to the other DESIGN-*.md; house
  style — Why / Principles / mapping / the design / invariants table / non-goals / open
  questions / dogfood). Proposes a **skill pair**: `second-opinion` (generate snapshot *from*
  memory, show advisory, wait for ack) + `apply-critique` (parse, plan ≤N scoped actions,
  validate, summarize applied/rejected). Optional, not auto-installed (like `memory-lint`).
- **Registered Blueprint gap** `bp-fresh-context-review` (serves `vision-agent-memory`).

## Notes / open items

- **Hard rule for build time:** no parallel hand-maintained state file — the snapshot is
  *derived + transient scratch* from continuity + sessions.
- **Forks to settle (design §10) before building:** naming; one skill vs. pair; scratch
  location (gitignored); snapshot delivery (file vs. clipboard); advisory wording + the
  "acknowledge" gate; ENABLE behavior (tool-local opt-in vs. offered to targets); version rung
  (additive MINOR, e.g. v4.10.0).
- **Compliance flags for any repo-landed AIF content:** must follow no-absolute-home-paths and
  the personal-research no-company-references rule (the AIF paper's ref #8 names an enterprise
  context — can't come in as-is).
- Implementation is **gated** — awaiting the maintainer's decisions on the §10 forks.

## Memory References

- Referenced: vision-agent-memory, review-verify-v480, lint-skill-v490, skills-layer-v410, no-build-step-agent-run, target-repo-scope-only
- Created: bp-fresh-context-review (tier: active — Blueprint gap)
